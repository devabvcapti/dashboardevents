---
phase: 02-import-pipeline-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - abvcap-congress/package.json
  - abvcap-congress/lib/supabase-server.ts
  - abvcap-congress/lib/supabase-browser.ts
  - abvcap-congress/lib/auth.ts
  - abvcap-congress/middleware.ts
  - abvcap-congress/app/login/page.tsx
  - abvcap-congress/app/login/login-form.tsx
  - abvcap-congress/app/api/auth/login/route.ts
  - abvcap-congress/app/api/auth/logout/route.ts
  - abvcap-congress/.env.local.example
autonomous: false
user_setup:
  - service: supabase-auth
    why: "Provisionamento manual do usuário admin"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project Settings → API → anon public key"
    dashboard_config:
      - task: "Criar usuário admin via Dashboard"
        location: "Supabase Dashboard → Authentication → Users → Add user (email + password)"
      - task: "Setar app_metadata.role = 'admin' para o usuário"
        location: "Supabase Dashboard → Authentication → Users → [usuário] → Edit → Raw user meta data → adicionar `{ \"role\": \"admin\" }` em app_metadata, OU executar via SQL: UPDATE auth.users SET raw_app_meta_data = raw_app_meta_data || '{\"role\":\"admin\"}'::jsonb WHERE email='<admin@email>';"

requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Acessar /dashboard sem estar autenticado redireciona para /login"
    - "Login com email+password válidos de um usuário com app_metadata.role='admin' redireciona para /dashboard"
    - "Login com credenciais de usuário SEM app_metadata.role='admin' é rejeitado (mensagem clara em PT-BR)"
    - "Sessão persiste após reload via cookie httpOnly+sameSite=lax"
    - "Botão logout limpa a sessão e redireciona para /login"
  artifacts:
    - path: "abvcap-congress/lib/supabase-server.ts"
      provides: "Cliente Supabase server-side com @supabase/ssr (cookies)"
      contains: "createServerClient"
    - path: "abvcap-congress/lib/supabase-browser.ts"
      provides: "Cliente Supabase browser-side (anon key) para o form de login"
      contains: "createBrowserClient"
    - path: "abvcap-congress/middleware.ts"
      provides: "Proteção de /dashboard e /api (exceto /api/auth/*)"
      contains: "export async function middleware"
    - path: "abvcap-congress/app/login/page.tsx"
      provides: "Página de login"
      contains: "LoginForm"
    - path: "abvcap-congress/lib/auth.ts"
      provides: "Helpers requireAdmin() e getCurrentUser() para Route Handlers e Server Components"
      contains: "requireAdmin"
  key_links:
    - from: "abvcap-congress/middleware.ts"
      to: "supabase auth session via cookies"
      via: "createServerClient with NextRequest/NextResponse cookies adapter"
      pattern: "createServerClient"
    - from: "abvcap-congress/app/login/login-form.tsx"
      to: "/api/auth/login"
      via: "fetch POST com {email, password}"
      pattern: "fetch.*api/auth/login"
    - from: "abvcap-congress/app/api/auth/login/route.ts"
      to: "supabase.auth.signInWithPassword + app_metadata.role check"
      via: "verificação após signIn"
      pattern: "app_metadata"
---

<objective>
Implementar autenticação Supabase Auth com gating por `app_metadata.role === "admin"`, usando `@supabase/ssr` (não o deprecated `@supabase/auth-helpers-nextjs`). Cobre AUTH-01 (acesso protegido por login), AUTH-02 (admin gating) e AUTH-03 (sessão persistente via cookie).

Purpose: Sem auth não há controle de quem importa dados sensíveis (PII de participantes) na Phase 2 Plan 03/04. Esta é uma dependência hard para qualquer rota administrativa do dashboard.

Output: Middleware Next.js 16 protegendo /dashboard e /api/* (exceto /api/auth/*), página /login funcional, helpers para Server Components/Route Handlers, e cookie de sessão httpOnly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-import-pipeline-auth/CONTEXT.md
@abvcap-congress/CLAUDE.md
@abvcap-congress/AGENTS.md
@abvcap-congress/package.json
@abvcap-congress/lib/supabase.ts
@abvcap-congress/app/layout.tsx
@abvcap-congress/app/dashboard/layout.tsx
@abvcap-congress/app/dashboard/page.tsx

<interfaces>
<!-- @supabase/ssr é a biblioteca oficial para Next.js App Router (substitui o deprecated @supabase/auth-helpers-nextjs).
     Docs relevantes: https://supabase.com/docs/guides/auth/server-side/nextjs (consultar via Context7 MCP se necessário).
     IMPORTANTE: O AGENTS.md/CLAUDE.md do projeto avisa que esta é "NOT the Next.js you know" — usar Next.js 16 + React 19.
     Para qualquer dúvida de API, ler `node_modules/next/dist/docs/` antes. -->

Key APIs that will be created:

```typescript
// lib/supabase-server.ts
export function createSupabaseServerClient(): SupabaseClient<Database>
// usa cookies() de 'next/headers' (Next.js 16) + createServerClient de @supabase/ssr

// lib/supabase-browser.ts
export function createSupabaseBrowserClient(): SupabaseClient<Database>
// usa createBrowserClient de @supabase/ssr; ANON key (NEXT_PUBLIC_)

// lib/auth.ts
export async function getCurrentUser(): Promise<{ id: string; email: string; isAdmin: boolean } | null>
export async function requireAdmin(): Promise<{ id: string; email: string }>
// requireAdmin throws/redirects 401 se não admin

// middleware.ts
export async function middleware(req: NextRequest): Promise<NextResponse>
export const config = { matcher: [...] }
```

Existing lib/supabase.ts (server-side service role) — NÃO MUDAR. Ele continua sendo usado para queries de dados via service role. Auth usa um cliente DIFERENTE (anon key via @supabase/ssr).
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar @supabase/ssr e criar clientes Supabase (server + browser)</name>
  <files>abvcap-congress/package.json, abvcap-congress/lib/supabase-server.ts, abvcap-congress/lib/supabase-browser.ts, abvcap-congress/.env.local.example</files>
  <read_first>
    - abvcap-congress/package.json (confirmar versões atuais; React 19, Next 16)
    - abvcap-congress/lib/supabase.ts (cliente service role existente — não mexer; criar arquivos SEPARADOS)
    - abvcap-congress/CLAUDE.md e abvcap-congress/AGENTS.md (avisos sobre Next.js 16 breaking changes)
    - node_modules/next/dist/docs/ (CONFERIR docs locais para uso de `cookies()` de `next/headers` em Next.js 16 antes de codar)
  </read_first>
  <action>
1. Instalar dependência via PowerShell na pasta `abvcap-congress/`:
   ```powershell
   npm install @supabase/ssr@latest
   ```

2. Criar `abvcap-congress/lib/supabase-server.ts`:
   ```typescript
   import { createServerClient, type CookieOptions } from '@supabase/ssr'
   import { cookies } from 'next/headers'
   import type { Database } from './database.types'

   /**
    * Cliente Supabase server-side com cookies de sessão (Next.js 16 App Router).
    * Usa ANON key — auth é feito via Supabase Auth, não service role.
    * Para queries de dados administrativos use `getSupabase()` de `./supabase.ts`.
    */
   export async function createSupabaseServerClient() {
     const cookieStore = await cookies()
     const url = process.env.NEXT_PUBLIC_SUPABASE_URL
     const anonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY
     if (!url || !anonKey) {
       throw new Error(
         'Supabase Auth não configurado. Preencha NEXT_PUBLIC_SUPABASE_URL e NEXT_PUBLIC_SUPABASE_ANON_KEY em .env.local.'
       )
     }
     return createServerClient<Database>(url, anonKey, {
       cookies: {
         getAll() {
           return cookieStore.getAll()
         },
         setAll(cookiesToSet) {
           try {
             cookiesToSet.forEach(({ name, value, options }) => {
               cookieStore.set(name, value, {
                 ...options,
                 httpOnly: true,
                 sameSite: 'lax',
                 secure: process.env.NODE_ENV === 'production',
               } as CookieOptions)
             })
           } catch {
             // Server Components não podem set cookies; middleware faz isso.
           }
         },
       },
     })
   }
   ```

3. Criar `abvcap-congress/lib/supabase-browser.ts`:
   ```typescript
   'use client'

   import { createBrowserClient } from '@supabase/ssr'
   import type { Database } from './database.types'

   export function createSupabaseBrowserClient() {
     const url = process.env.NEXT_PUBLIC_SUPABASE_URL
     const anonKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY
     if (!url || !anonKey) {
       throw new Error('Supabase Auth não configurado no client.')
     }
     return createBrowserClient<Database>(url, anonKey)
   }
   ```

4. Atualizar `abvcap-congress/.env.local.example` (criar se não existir) adicionando:
   ```
   NEXT_PUBLIC_SUPABASE_URL=https://kcgwyzvwxmmygfdsetgd.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_public_key
   SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_from_dashboard
   ```
  </action>
  <verify>
    <automated>cd abvcap-congress && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `abvcap-congress/package.json` contém literalmente `"@supabase/ssr":` em `dependencies`.
    - `abvcap-congress/lib/supabase-server.ts` existe e contém literalmente `createServerClient<Database>`.
    - `abvcap-congress/lib/supabase-server.ts` contém literalmente `httpOnly: true` e `sameSite: 'lax'`.
    - `abvcap-congress/lib/supabase-browser.ts` existe e contém literalmente `'use client'` na primeira linha.
    - `abvcap-congress/lib/supabase-browser.ts` contém literalmente `createBrowserClient<Database>`.
    - `abvcap-congress/.env.local.example` contém literalmente `NEXT_PUBLIC_SUPABASE_ANON_KEY=`.
    - `abvcap-congress/lib/supabase.ts` NÃO foi modificado (grep deve mostrar conteúdo idêntico ao Phase 1: `process.env.SUPABASE_SERVICE_ROLE_KEY` ainda presente).
    - `npx tsc --noEmit` retorna 0 erros.
  </acceptance_criteria>
  <done>
@supabase/ssr instalado. Dois clientes Supabase distintos criados: server (com cookies, anon key) e browser (anon key). lib/supabase.ts (service role) intacto. .env.local.example documenta as três variáveis necessárias.
  </done>
</task>

<task type="auto">
  <name>Task 2: Middleware + helpers auth + login route handlers</name>
  <files>abvcap-congress/middleware.ts, abvcap-congress/lib/auth.ts, abvcap-congress/app/api/auth/login/route.ts, abvcap-congress/app/api/auth/logout/route.ts</files>
  <read_first>
    - abvcap-congress/lib/supabase-server.ts (criado em Task 1 — assinatura de createSupabaseServerClient)
    - node_modules/next/dist/docs/ (procurar especificamente por documentação de `middleware` em Next.js 16; NextRequest/NextResponse podem ter mudanças)
    - .planning/phases/02-import-pipeline-auth/CONTEXT.md (decisões de Auth: cookie httpOnly+sameSite=lax, gating por app_metadata.role==="admin")
  </read_first>
  <action>
1. Criar `abvcap-congress/middleware.ts` (precisa estar na raiz do projeto, ao lado de `app/`):
   ```typescript
   import { NextResponse, type NextRequest } from 'next/server'
   import { createServerClient, type CookieOptions } from '@supabase/ssr'
   import type { Database } from './lib/database.types'

   /**
    * Middleware: refresca sessão Supabase e protege rotas.
    * Rotas protegidas: /dashboard/**, /api/** EXCETO /api/auth/**.
    * Rotas públicas: /login, /api/auth/**, /, /_next/**.
    */
   export async function middleware(req: NextRequest) {
     let response = NextResponse.next({ request: req })

     const supabase = createServerClient<Database>(
       process.env.NEXT_PUBLIC_SUPABASE_URL!,
       process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
       {
         cookies: {
           getAll() {
             return req.cookies.getAll()
           },
           setAll(cookiesToSet) {
             cookiesToSet.forEach(({ name, value }) => req.cookies.set(name, value))
             response = NextResponse.next({ request: req })
             cookiesToSet.forEach(({ name, value, options }) => {
               response.cookies.set(name, value, {
                 ...options,
                 httpOnly: true,
                 sameSite: 'lax',
                 secure: process.env.NODE_ENV === 'production',
               } as CookieOptions)
             })
           },
         },
       }
     )

     const { data: { user } } = await supabase.auth.getUser()

     const path = req.nextUrl.pathname
     const isProtected =
       path.startsWith('/dashboard') ||
       (path.startsWith('/api') && !path.startsWith('/api/auth'))

     if (isProtected) {
       if (!user) {
         const loginUrl = new URL('/login', req.url)
         loginUrl.searchParams.set('redirect', path)
         return NextResponse.redirect(loginUrl)
       }
       const role = (user.app_metadata as { role?: string } | null)?.role
       if (role !== 'admin') {
         // Não-admin: logout forçado e redirect
         await supabase.auth.signOut()
         const loginUrl = new URL('/login', req.url)
         loginUrl.searchParams.set('error', 'forbidden')
         return NextResponse.redirect(loginUrl)
       }
     }

     return response
   }

   export const config = {
     matcher: [
       /*
        * Match all paths except:
        * - _next/static, _next/image
        * - favicon, public assets
        */
       '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
     ],
   }
   ```

2. Criar `abvcap-congress/lib/auth.ts`:
   ```typescript
   import { redirect } from 'next/navigation'
   import { createSupabaseServerClient } from './supabase-server'

   export interface SessionUser {
     id: string
     email: string
     isAdmin: boolean
   }

   export async function getCurrentUser(): Promise<SessionUser | null> {
     const supabase = await createSupabaseServerClient()
     const { data: { user } } = await supabase.auth.getUser()
     if (!user) return null
     const role = (user.app_metadata as { role?: string } | null)?.role
     return {
       id: user.id,
       email: user.email ?? '',
       isAdmin: role === 'admin',
     }
   }

   /**
    * Para uso em Server Components e Route Handlers.
    * Redireciona para /login se não autenticado, ou throw 403 se não admin.
    */
   export async function requireAdmin(): Promise<SessionUser> {
     const user = await getCurrentUser()
     if (!user) redirect('/login')
     if (!user.isAdmin) {
       throw new Response('Forbidden: admin required', { status: 403 })
     }
     return user
   }
   ```

3. Criar `abvcap-congress/app/api/auth/login/route.ts`:
   ```typescript
   import { NextResponse } from 'next/server'
   import { createSupabaseServerClient } from '@/lib/supabase-server'
   import { z } from 'zod'

   const Body = z.object({
     email: z.string().email('Email inválido'),
     password: z.string().min(1, 'Senha obrigatória'),
   })

   export async function POST(req: Request) {
     const json = await req.json().catch(() => null)
     const parsed = Body.safeParse(json)
     if (!parsed.success) {
       return NextResponse.json(
         { error: parsed.error.issues[0]?.message ?? 'Payload inválido' },
         { status: 400 }
       )
     }

     const supabase = await createSupabaseServerClient()
     const { data, error } = await supabase.auth.signInWithPassword({
       email: parsed.data.email,
       password: parsed.data.password,
     })

     if (error || !data.user) {
       return NextResponse.json(
         { error: 'Email ou senha inválidos.' },
         { status: 401 }
       )
     }

     const role = (data.user.app_metadata as { role?: string } | null)?.role
     if (role !== 'admin') {
       await supabase.auth.signOut()
       return NextResponse.json(
         { error: 'Acesso restrito a administradores.' },
         { status: 403 }
       )
     }

     return NextResponse.json({ ok: true, redirect: '/dashboard' })
   }
   ```

4. Criar `abvcap-congress/app/api/auth/logout/route.ts`:
   ```typescript
   import { NextResponse } from 'next/server'
   import { createSupabaseServerClient } from '@/lib/supabase-server'

   export async function POST() {
     const supabase = await createSupabaseServerClient()
     await supabase.auth.signOut()
     return NextResponse.json({ ok: true })
   }
   ```

5. Instalar `zod` se ainda não estiver presente:
   ```powershell
   npm install zod@latest
   ```

   (Verificar primeiro em package.json — pode ser que já esteja transitivamente; se não, instalar.)
  </action>
  <verify>
    <automated>cd abvcap-congress && npx tsc --noEmit && npm run lint --silent</automated>
  </verify>
  <acceptance_criteria>
    - `abvcap-congress/middleware.ts` existe na raiz do projeto (NÃO em `app/`).
    - `middleware.ts` contém literalmente `path.startsWith('/dashboard')` e `path.startsWith('/api/auth')`.
    - `middleware.ts` contém literalmente `role !== 'admin'`.
    - `middleware.ts` contém o `matcher` config exportada.
    - `abvcap-congress/lib/auth.ts` contém literalmente `export async function requireAdmin`.
    - `abvcap-congress/lib/auth.ts` contém literalmente `redirect('/login')`.
    - `abvcap-congress/app/api/auth/login/route.ts` contém literalmente `signInWithPassword`.
    - `abvcap-congress/app/api/auth/login/route.ts` contém literalmente `'Acesso restrito a administradores.'`.
    - `abvcap-congress/app/api/auth/logout/route.ts` contém literalmente `signOut()`.
    - `package.json` contém `"zod":` em dependencies.
    - `npx tsc --noEmit` retorna 0 erros.
    - `npm run lint` passa sem erros.
  </acceptance_criteria>
  <done>
Middleware ativo protegendo /dashboard e /api/* (exceto /api/auth/*). Helpers `requireAdmin` e `getCurrentUser` disponíveis. Route handlers /api/auth/login (com Zod validation + app_metadata.role check) e /api/auth/logout funcionando. Zod instalado.
  </done>
</task>

<task type="auto">
  <name>Task 3: Página /login com form e teste de fluxo end-to-end</name>
  <files>abvcap-congress/app/login/page.tsx, abvcap-congress/app/login/login-form.tsx</files>
  <read_first>
    - abvcap-congress/components/ui/button.tsx (estilo dos botões shadcn já no projeto)
    - abvcap-congress/components/ui/input.tsx (estilo dos inputs)
    - abvcap-congress/components/ui/card.tsx (estrutura card)
    - abvcap-congress/app/layout.tsx (root layout — para não duplicar wrappers)
    - abvcap-congress/app/globals.css (variáveis de cor/tema disponíveis)
  </read_first>
  <action>
1. Criar `abvcap-congress/app/login/page.tsx` (server component):
   ```tsx
   import { LoginForm } from './login-form'

   export const metadata = { title: 'Login — Dashboard ABVCAP' }

   export default async function LoginPage({
     searchParams,
   }: {
     searchParams: Promise<{ redirect?: string; error?: string }>
   }) {
     const { redirect, error } = await searchParams
     return (
       <div className="min-h-screen flex items-center justify-center bg-background px-4">
         <div className="w-full max-w-sm space-y-6">
           <div className="text-center space-y-2">
             <p className="text-[11px] font-mono tracking-[0.30em] text-primary uppercase">
               ABVCAP
             </p>
             <h1 className="font-display text-2xl">Dashboard Eventos</h1>
             <p className="text-sm text-muted-foreground">
               Acesso restrito a administradores.
             </p>
           </div>
           <LoginForm
             redirectTo={redirect ?? '/dashboard'}
             initialError={
               error === 'forbidden'
                 ? 'Acesso restrito a administradores.'
                 : null
             }
           />
         </div>
       </div>
     )
   }
   ```

2. Criar `abvcap-congress/app/login/login-form.tsx` (client component):
   ```tsx
   'use client'

   import { useState } from 'react'
   import { useRouter } from 'next/navigation'
   import { Button } from '@/components/ui/button'
   import { Input } from '@/components/ui/input'

   export function LoginForm({
     redirectTo,
     initialError,
   }: {
     redirectTo: string
     initialError: string | null
   }) {
     const router = useRouter()
     const [email, setEmail] = useState('')
     const [password, setPassword] = useState('')
     const [error, setError] = useState<string | null>(initialError)
     const [loading, setLoading] = useState(false)

     async function onSubmit(e: React.FormEvent) {
       e.preventDefault()
       setError(null)
       setLoading(true)
       try {
         const res = await fetch('/api/auth/login', {
           method: 'POST',
           headers: { 'Content-Type': 'application/json' },
           body: JSON.stringify({ email, password }),
         })
         const data = await res.json()
         if (!res.ok) {
           setError(data.error ?? 'Falha no login.')
           return
         }
         router.push(redirectTo || '/dashboard')
         router.refresh()
       } catch {
         setError('Erro de rede. Tente novamente.')
       } finally {
         setLoading(false)
       }
     }

     return (
       <form onSubmit={onSubmit} className="space-y-4">
         <label className="block space-y-1.5">
           <span className="text-xs font-mono uppercase tracking-wider text-muted-foreground">
             Email
           </span>
           <Input
             type="email"
             autoComplete="email"
             required
             value={email}
             onChange={(e) => setEmail(e.target.value)}
           />
         </label>
         <label className="block space-y-1.5">
           <span className="text-xs font-mono uppercase tracking-wider text-muted-foreground">
             Senha
           </span>
           <Input
             type="password"
             autoComplete="current-password"
             required
             value={password}
             onChange={(e) => setPassword(e.target.value)}
           />
         </label>
         {error && (
           <p
             role="alert"
             className="text-sm text-red-600 dark:text-red-400 bg-red-500/10 border border-red-500/20 rounded p-2"
           >
             {error}
           </p>
         )}
         <Button type="submit" disabled={loading} className="w-full">
           {loading ? 'Entrando...' : 'Entrar'}
         </Button>
       </form>
     )
   }
   ```

3. Garantir que o tsconfig já resolve o alias `@/components/ui/button` (Phase 1 já configurou — não mexer).
  </action>
  <verify>
    <automated>cd abvcap-congress && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `abvcap-congress/app/login/page.tsx` existe e contém literalmente `<LoginForm`.
    - `abvcap-congress/app/login/page.tsx` lê `searchParams` como `Promise<...>` (Next.js 16 async searchParams).
    - `abvcap-congress/app/login/login-form.tsx` existe e contém `'use client'` na primeira linha.
    - `login-form.tsx` contém literalmente `fetch('/api/auth/login'`.
    - `login-form.tsx` exibe error via `role="alert"`.
    - `npx tsc --noEmit` retorna 0 erros.
    - `npm run build` completa com sucesso (verifica que o middleware compila e não há rotas conflitantes).
  </acceptance_criteria>
  <done>
Página /login funcional com form server-side validado. Erros exibidos em PT-BR. Loading state durante submit. Redireciona para `?redirect=` (ou /dashboard) após login OK.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint humano — Provisionar admin + testar fluxo de auth end-to-end</name>
  <what-built>
    Sistema de autenticação completo:
    - Middleware protegendo /dashboard e /api/* (exceto /api/auth/*)
    - Página /login com form, validação Zod e erros em PT-BR
    - Helpers requireAdmin/getCurrentUser para uso em Route Handlers
    - Cookie httpOnly+sameSite=lax via @supabase/ssr
    - Gating por app_metadata.role === "admin"
  </what-built>
  <how-to-verify>
    **Setup (uma vez):**
    1. No Supabase Dashboard (https://supabase.com/dashboard/project/kcgwyzvwxmmygfdsetgd):
       a. Project Settings → API → copiar o `anon public key`
       b. Adicionar em `abvcap-congress/.env.local`: `NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon_key>`
       c. Authentication → Users → "Add user" → criar com email/senha (anote ambos)
       d. SQL Editor → executar:
          ```sql
          UPDATE auth.users
          SET raw_app_meta_data = COALESCE(raw_app_meta_data,'{}'::jsonb) || '{"role":"admin"}'::jsonb
          WHERE email='<email_do_admin_criado>';
          ```
       e. (Opcional) Criar um SEGUNDO usuário SEM o role admin para testar o caminho de rejeição.

    **Testes funcionais (rodar `npm run dev` em `abvcap-congress/`):**

    1. Acesso protegido sem auth:
       - Abrir http://localhost:3000/dashboard
       - **Esperado:** redireciona para http://localhost:3000/login?redirect=%2Fdashboard
    2. Login com credenciais inválidas:
       - Form com email aleatório + senha errada → submit
       - **Esperado:** mensagem "Email ou senha inválidos." aparece em vermelho
    3. Login com usuário sem role admin:
       - Submeter form com o segundo usuário (sem app_metadata.role)
       - **Esperado:** mensagem "Acesso restrito a administradores." aparece
    4. Login com admin correto:
       - Submeter form com o admin
       - **Esperado:** redirect para /dashboard, dashboard renderiza
    5. Persistência de sessão:
       - Após login, dar F5 (reload) na página /dashboard
       - **Esperado:** continua autenticado, dashboard renderiza
    6. Logout (via DevTools Console):
       - Executar `await fetch('/api/auth/logout', {method:'POST'})`
       - F5 na /dashboard → **Esperado:** redireciona para /login

    **Verificações de segurança (DevTools → Application → Cookies):**
    - Cookies `sb-*` presentes
    - Cookies `sb-*` têm flag `HttpOnly` checked
    - Cookies `sb-*` têm `SameSite=Lax`
  </how-to-verify>
  <resume-signal>Digite "approved" se todos os 6 testes passarem, ou descreva o que falhou (com screenshot do erro, se possível).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/auth/login | Credenciais em plaintext sobre TLS; resposta seta cookie de sessão. |
| Browser → /dashboard, /api/* | Cookie de sessão atravessa boundary; middleware valida em todo request. |
| Middleware → Supabase Auth | Chama `auth.getUser()` para validar JWT do cookie (não confia no cookie cego). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.02-01 | Spoofing | Session fixation | mitigate | Cookies regenerados em cada signIn pelo @supabase/ssr; sessão validada via `auth.getUser()` (vai ao Supabase, não confia no JWT local). |
| T-02.02-02 | Spoofing | Brute force de senha | mitigate | Supabase Auth tem rate limit nativo (10 tentativas/hora por IP). Em produção habilitar Captcha no projeto Supabase. Mensagem de erro genérica ("Email ou senha inválidos") não vaza se email existe. |
| T-02.02-03 | Tampering | Cookie de sessão modificado | mitigate | Cookies `sb-*` contêm JWT assinado pelo Supabase; middleware chama `auth.getUser()` que verifica assinatura. JWT modificado falha validação. |
| T-02.02-04 | Information Disclosure | XSS captura sessão | mitigate | Cookie `httpOnly: true` impede `document.cookie`. CSP herdada do Next.js default. Próxima fase pode adicionar CSP strict. |
| T-02.02-05 | Information Disclosure | CSRF em /api/auth/login | mitigate | Cookies `sameSite: 'lax'` bloqueia cross-site POST. Login requer JSON body (não form-encoded) — preflight CORS necessário, navegador bloqueia por default. |
| T-02.02-06 | Elevation of Privilege | Usuário não-admin acessa /dashboard | mitigate | Middleware verifica `app_metadata.role==='admin'`; não-admin é forçado a logout + redirect. Verificação repete em `requireAdmin()` para Route Handlers como defense-in-depth. |
| T-02.02-07 | Repudiation | Sem audit de logins | accept | Supabase Auth tem audit log nativo (Dashboard → Authentication → Logs). Audit custom fora de escopo desta fase. |
| T-02.02-08 | Denial of Service | Spam em /api/auth/login | mitigate | Rate limit do Supabase Auth + Vercel Edge protection (default). Sem alarme custom nesta fase. |
| T-02.02-09 | Information Disclosure | Anon key vazada | accept | Anon key É pública por design (NEXT_PUBLIC_); RLS deve proteger dados sensíveis. Service role key permanece server-only conforme Phase 1. |
</threat_model>

<verification>
1. Sem auth, qualquer rota `/dashboard/*` redireciona para `/login?redirect=...`.
2. Login com role!=admin é rejeitado com mensagem clara em PT-BR.
3. Login admin redireciona para /dashboard e sessão persiste após F5.
4. Cookies `sb-*` no browser têm `HttpOnly`, `SameSite=Lax`, `Secure` (em produção).
5. `npx tsc --noEmit` e `npm run build` passam.
</verification>

<success_criteria>
- AUTH-01: /dashboard protegido. ✓
- AUTH-02: Apenas admin (app_metadata.role==='admin') passa. ✓
- AUTH-03: Sessão sobrevive a reload via cookie. ✓
- Página /login estilizada conforme tema existente do projeto.
- Helpers `requireAdmin/getCurrentUser` prontos para uso em Plans 03 e 04.
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-import-pipeline-auth/02-02-SUMMARY.md` incluindo:
- Arquivos criados (lista)
- Decisões de implementação (anon key vs service role, por que @supabase/ssr)
- Instruções de provisionamento de admin (para AGENTS.md)
- Resultado dos 6 testes do checkpoint humano
</output>
