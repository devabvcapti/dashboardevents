---
phase: 02-import-pipeline-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - abvcap-congress/supabase/migrations/003_phase2_participants_extensions.sql
  - abvcap-congress/supabase/migrations/004_phase2_form_responses_extensions.sql
  - abvcap-congress/supabase/migrations/005_upsert_participants_rpc.sql
  - abvcap-congress/supabase/migrations/006_upsert_form_responses_rpc.sql
  - abvcap-congress/lib/database.types.ts
autonomous: true
requirements:
  - IMPORT-07
  - IMPORT-08
  - IMPORT-09

must_haves:
  truths:
    - "A tabela participants contém colunas job_title, cpf, phone, payment_status, is_company_member, company_segment_raw, company_segment_normalized"
    - "A tabela form_responses tem interested_in_events do tipo TEXT[] (não boolean) e novas colunas content_interests TEXT[] e dietary_details TEXT"
    - "Existe uma RPC upsert_participants_batch(p_rows jsonb, p_edition_id uuid, p_import_job_id uuid) que faz upsert atômico por (email, edition_id)"
    - "Existe uma RPC upsert_form_responses_batch(p_rows jsonb, p_edition_id uuid) que resolve participant_id por (email, edition_id) e faz upsert em form_responses por participant_id"
    - "lib/database.types.ts é regenerado a partir do schema real e inclui as novas colunas E as duas novas RPCs"
  artifacts:
    - path: "abvcap-congress/supabase/migrations/003_phase2_participants_extensions.sql"
      provides: "ALTER TABLE participants com 7 novos campos"
      contains: "ALTER TABLE participants"
    - path: "abvcap-congress/supabase/migrations/004_phase2_form_responses_extensions.sql"
      provides: "Drop+recreate interested_in_events como TEXT[], add content_interests TEXT[], dietary_details TEXT"
      contains: "interested_in_events text[]"
    - path: "abvcap-congress/supabase/migrations/005_upsert_participants_rpc.sql"
      provides: "Função upsert_participants_batch para commit atômico"
      contains: "CREATE OR REPLACE FUNCTION upsert_participants_batch"
    - path: "abvcap-congress/supabase/migrations/006_upsert_form_responses_rpc.sql"
      provides: "Função upsert_form_responses_batch que persiste respostas de formulário em form_responses, resolvendo participant_id por (email, edition_id)"
      contains: "CREATE OR REPLACE FUNCTION upsert_form_responses_batch"
    - path: "abvcap-congress/lib/database.types.ts"
      provides: "Types regenerados com novas colunas e RPCs"
      contains: "company_segment_normalized"
  key_links:
    - from: "abvcap-congress/lib/database.types.ts"
      to: "schema Supabase remoto"
      via: "supabase gen types typescript"
      pattern: "company_segment_normalized"
    - from: "abvcap-congress/supabase/migrations/005_upsert_participants_rpc.sql"
      to: "participants table"
      via: "ON CONFLICT (email, edition_id) DO UPDATE"
      pattern: "ON CONFLICT"
    - from: "abvcap-congress/supabase/migrations/006_upsert_form_responses_rpc.sql"
      to: "form_responses table"
      via: "ON CONFLICT (participant_id) DO UPDATE — participant_id resolvido via JOIN com participants (email, edition_id)"
      pattern: "ON CONFLICT \\(participant_id\\)"
---

<objective>
Estender o schema do banco (criado em Phase 1) com os campos adicionais identificados na análise do arquivo Excel real ABVCAP, criar **duas** RPCs atômicas — uma para upsert em batch de participantes e outra para upsert em batch de respostas de formulário — e regenerar os tipos TypeScript a partir do schema atualizado.

Purpose: A Phase 1 criou um schema minimalista; a análise do Excel real (60 colunas) revelou campos adicionais necessários (job_title, cpf, phone, payment_status, is_company_member, company_segment_raw + normalized) e correções (interested_in_events deve ser TEXT[], não boolean). Sem esses ajustes, o parser da Phase 2 não tem onde gravar os dados parseados. Adicionalmente, sem a RPC `upsert_form_responses_batch` os campos de formulário parseados em Plan 03 (topics_of_interest, interested_in_events, preferred_channels, content_interests, dietary_restrictions, dietary_details) seriam descartados pelo commit route — a tabela form_responses permaneceria vazia após cada import.

Output: 4 migrations SQL aplicadas ao Supabase (kcgwyzvwxmmygfdsetgd), tipos TS regenerados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-import-pipeline-auth/CONTEXT.md
@abvcap-congress/supabase/migrations/001_foundation.sql
@abvcap-congress/supabase/migrations/002_rpc_stats.sql
@abvcap-congress/lib/database.types.ts

<interfaces>
<!-- Tipos atuais (Phase 1) - referência para regeneração -->

From abvcap-congress/lib/database.types.ts (estado atual):
```typescript
export type CompanySegment = Database["public"]["Enums"]["company_segment"]
// "GP" | "LP" | "FUNDO" | "CORPORATIVO" | "GOVERNO" | "ACADEMIA" | "OUTRO"

participants.Row atual: {
  id, edition_id, email, full_name, company,
  ticket_membership, ticket_value, import_job_id, created_at
}

form_responses.Row atual contém: interested_in_events: boolean | null
form_responses tem UNIQUE NOT NULL em participant_id (FK → participants.id)
```

After this plan, participants.Row will gain:
- job_title: string | null
- cpf: string | null            // 11 dígitos, zero-padded
- phone: string | null
- payment_status: string | null
- is_company_member: boolean | null
- company_segment_raw: string | null
- company_segment_normalized: CompanySegment | null

After this plan, form_responses.Row will be:
- interested_in_events: string[] | null  (was: boolean | null)
- content_interests: string[] | null     (new)
- dietary_details: string | null         (new)

New RPC signatures:
- upsert_participants_batch(
    p_rows: Json,           // jsonb array of participant objects
    p_edition_id: string,   // uuid
    p_import_job_id: string // uuid
  ): { inserted: number, updated: number, errors: Json }

- upsert_form_responses_batch(
    p_rows: Json,           // jsonb array of form-response objects, each with participant_email + form fields
    p_edition_id: string    // uuid — usado para resolver participant_id via (email, edition_id)
  ): { inserted: number, updated: number, errors: number, error_log: Json }
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 003 — Estender participants com novos campos</name>
  <files>abvcap-congress/supabase/migrations/003_phase2_participants_extensions.sql</files>
  <read_first>
    - abvcap-congress/supabase/migrations/001_foundation.sql (para conhecer o schema atual de participants e enum company_segment)
    - .planning/phases/02-import-pipeline-auth/CONTEXT.md (para confirmar exatamente quais colunas adicionar — seção "participants — campos a adicionar")
  </read_first>
  <action>
Criar o arquivo `abvcap-congress/supabase/migrations/003_phase2_participants_extensions.sql` com o seguinte conteúdo EXATO:

```sql
-- Migration 003: Extensões da tabela participants para Phase 2
-- Baseado em análise do arquivo Excel real ABVCAP (60 colunas).
-- Adiciona campos identificados na análise do CONTEXT.md.

ALTER TABLE participants
  ADD COLUMN IF NOT EXISTS job_title TEXT,
  ADD COLUMN IF NOT EXISTS cpf TEXT,                          -- 11 dígitos com zero-padding
  ADD COLUMN IF NOT EXISTS phone TEXT,
  ADD COLUMN IF NOT EXISTS payment_status TEXT,
  ADD COLUMN IF NOT EXISTS is_company_member BOOLEAN,
  ADD COLUMN IF NOT EXISTS company_segment_raw TEXT,
  ADD COLUMN IF NOT EXISTS company_segment_normalized company_segment;

-- Índice para busca por CPF (uso futuro de deduplicação fallback)
CREATE INDEX IF NOT EXISTS idx_participants_cpf
  ON participants (cpf) WHERE cpf IS NOT NULL;

-- Índice para filtros por segmento normalizado
CREATE INDEX IF NOT EXISTS idx_participants_segment_normalized
  ON participants (company_segment_normalized)
  WHERE company_segment_normalized IS NOT NULL;
```

**Aplicação da migration — caminho primário (MCP) com fallback CLI:**

Caminho **primário (preferido)**: usar a ferramenta MCP `mcp__claude_ai_Supabase__apply_migration`:
- name: `003_phase2_participants_extensions`
- query: o conteúdo SQL acima (sem o comentário do header é aceitável, mas preservar todos os ALTER e CREATE INDEX).

Caminho de **fallback** — se o MCP Supabase não estiver disponível, executor é responsável por aplicar via Supabase CLI:
```powershell
cd abvcap-congress
npx supabase db push --project-ref kcgwyzvwxmmygfdsetgd
```
(Pré-requisitos do fallback: `npx supabase login` já executado e `npx supabase link --project-ref kcgwyzvwxmmygfdsetgd` já feito. STATE.md confirma `kcgwyzvwxmmygfdsetgd` como o project ID.)

**Pré-requisito explícito desta task:** o executor DEVE ter um dos dois caminhos disponível (MCP `claude_ai_Supabase` OU Supabase CLI logada e linkada). Se nenhum estiver disponível, pausar e pedir ao usuário antes de prosseguir.
  </action>
  <verify>
    <automated>Após aplicar a migration (via MCP `mcp__claude_ai_Supabase__apply_migration` OU `npx supabase db push`), validar via UM dos dois caminhos:

CAMINHO A (MCP — preferido): chamar `mcp__claude_ai_Supabase__execute_sql` com a query: `SELECT column_name, data_type, udt_name FROM information_schema.columns WHERE table_name='participants' AND column_name IN ('job_title','cpf','phone','payment_status','is_company_member','company_segment_raw','company_segment_normalized') ORDER BY column_name;` — deve retornar 7 linhas.

CAMINHO B (CLI fallback): executar no shell:
```powershell
cd abvcap-congress
npx supabase db remote query "SELECT column_name FROM information_schema.columns WHERE table_name='participants' AND column_name IN ('job_title','cpf','phone','payment_status','is_company_member','company_segment_raw','company_segment_normalized') ORDER BY column_name;"
```
deve listar 7 colunas. Se o subcomando `db remote query` não estiver disponível na CLI instalada, usar `psql` direto com a connection string do dashboard Supabase: `psql "$env:SUPABASE_DB_URL" -c "SELECT ..."`.

Pelo menos UM caminho de verificação deve passar — ambos retornam o mesmo resultado.</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `abvcap-congress/supabase/migrations/003_phase2_participants_extensions.sql` existe.
    - O arquivo contém literalmente a string `ADD COLUMN IF NOT EXISTS company_segment_normalized company_segment` (verificar via grep).
    - Query `SELECT column_name FROM information_schema.columns WHERE table_name='participants' AND column_name='company_segment_normalized'` no Supabase retorna 1 linha.
    - Query `SELECT column_name FROM information_schema.columns WHERE table_name='participants' AND column_name='cpf'` retorna 1 linha com data_type='text'.
    - Query `SELECT column_name FROM information_schema.columns WHERE table_name='participants' AND column_name='is_company_member'` retorna 1 linha com data_type='boolean'.
    - Os índices `idx_participants_cpf` e `idx_participants_segment_normalized` aparecem em `SELECT indexname FROM pg_indexes WHERE tablename='participants'`.
  </acceptance_criteria>
  <done>
Migration 003 aplicada com sucesso. participants tem 7 novas colunas (job_title TEXT, cpf TEXT, phone TEXT, payment_status TEXT, is_company_member BOOLEAN, company_segment_raw TEXT, company_segment_normalized company_segment). Dois índices condicionais criados.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migration 004 — Corrigir/estender form_responses</name>
  <files>abvcap-congress/supabase/migrations/004_phase2_form_responses_extensions.sql</files>
  <read_first>
    - abvcap-congress/supabase/migrations/001_foundation.sql (conferir definição atual de form_responses, especificamente interested_in_events: boolean)
    - .planning/phases/02-import-pipeline-auth/CONTEXT.md (seção "form_responses — campos a corrigir/adicionar")
    - abvcap-congress/lib/database.types.ts (linhas 36-91 — confirma `interested_in_events: boolean | null` atualmente)
  </read_first>
  <action>
Criar `abvcap-congress/supabase/migrations/004_phase2_form_responses_extensions.sql` com:

```sql
-- Migration 004: Correções e extensões em form_responses para Phase 2
-- Análise do Excel real mostrou que interested_in_events é multi-select (TEXT[]),
-- não boolean. Adiciona campos faltando: content_interests, dietary_details.

-- 1. Drop coluna boolean errada e recriar como TEXT[]
ALTER TABLE form_responses DROP COLUMN IF EXISTS interested_in_events;
ALTER TABLE form_responses ADD COLUMN interested_in_events TEXT[];

-- 2. Adicionar novos campos identificados na análise
ALTER TABLE form_responses
  ADD COLUMN IF NOT EXISTS content_interests TEXT[],
  ADD COLUMN IF NOT EXISTS dietary_details TEXT;

-- 3. Índices GIN para os novos arrays (para filtros futuros)
CREATE INDEX IF NOT EXISTS idx_form_responses_interested_in_events
  ON form_responses USING GIN (interested_in_events);

CREATE INDEX IF NOT EXISTS idx_form_responses_content_interests
  ON form_responses USING GIN (content_interests);
```

**Aplicação da migration — caminho primário (MCP) com fallback CLI:**

Caminho **primário (preferido)**: `mcp__claude_ai_Supabase__apply_migration` com:
- name: `004_phase2_form_responses_extensions`
- query: o conteúdo SQL acima.

Caminho de **fallback** — se o MCP Supabase não estiver disponível: `cd abvcap-congress && npx supabase db push --project-ref kcgwyzvwxmmygfdsetgd`.

Mesmas pré-condições da Task 1.

Nota importante: como Phase 1 ainda não populou nenhum dado em form_responses, o `DROP COLUMN` é seguro. Não há perda de dados.
  </action>
  <verify>
    <automated>Após aplicar a migration (via MCP `mcp__claude_ai_Supabase__apply_migration` OU `npx supabase db push`), validar via UM dos dois caminhos:

CAMINHO A (MCP — preferido): chamar `mcp__claude_ai_Supabase__execute_sql` com: `SELECT column_name, data_type, udt_name FROM information_schema.columns WHERE table_name='form_responses' AND column_name IN ('interested_in_events','content_interests','dietary_details');` — deve retornar 3 linhas, com `interested_in_events` e `content_interests` tendo `data_type='ARRAY'` e `udt_name='_text'`, e `dietary_details` tendo `data_type='text'`.

CAMINHO B (CLI fallback): `cd abvcap-congress; npx supabase db remote query "SELECT column_name, udt_name FROM information_schema.columns WHERE table_name='form_responses' AND column_name IN ('interested_in_events','content_interests','dietary_details');"` — deve listar as mesmas 3 colunas com `_text` para os dois ARRAYs.

Pelo menos UM caminho deve passar.</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `004_phase2_form_responses_extensions.sql` existe.
    - O arquivo contém literalmente `ADD COLUMN interested_in_events TEXT[]` (não boolean).
    - O arquivo contém literalmente `ADD COLUMN IF NOT EXISTS content_interests TEXT[]`.
    - O arquivo contém literalmente `ADD COLUMN IF NOT EXISTS dietary_details TEXT`.
    - Query no Supabase retorna `udt_name='_text'` para `interested_in_events` (não `bool`).
    - Índices `idx_form_responses_interested_in_events` e `idx_form_responses_content_interests` existem em `pg_indexes`.
  </acceptance_criteria>
  <done>
form_responses.interested_in_events agora é TEXT[] (era boolean). Colunas content_interests TEXT[] e dietary_details TEXT adicionadas. Dois índices GIN criados.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migration 005 — RPC upsert_participants_batch</name>
  <files>abvcap-congress/supabase/migrations/005_upsert_participants_rpc.sql</files>
  <read_first>
    - abvcap-congress/supabase/migrations/001_foundation.sql (constraint UNIQUE(email, edition_id) já existe — confirmar)
    - abvcap-congress/supabase/migrations/002_rpc_stats.sql (estilo da RPC anterior para manter consistência)
    - .planning/phases/02-import-pipeline-auth/CONTEXT.md (decisões IMPORT-07 sobre upsert atômico em batches de 500)
  </read_first>
  <action>
Criar `abvcap-congress/supabase/migrations/005_upsert_participants_rpc.sql` com a função que faz upsert atômico de até 500 linhas em uma única transação. Cada linha do `p_rows` JSONB deve seguir o shape do schema participants estendido (Task 1).

Conteúdo EXATO:

```sql
-- Migration 005: RPC para upsert em batch de participantes (Phase 2)
-- Faz upsert atômico de até 500 linhas por chamada, vinculando ao import_job_id.
-- Retorna contagens de inseridos/atualizados/erros + log de erros por linha.

CREATE OR REPLACE FUNCTION upsert_participants_batch(
  p_rows         jsonb,
  p_edition_id   uuid,
  p_import_job_id uuid
)
RETURNS json AS $$
DECLARE
  v_row              jsonb;
  v_inserted         int := 0;
  v_updated          int := 0;
  v_errors           int := 0;
  v_error_log        jsonb := '[]'::jsonb;
  v_email            text;
  v_existed          boolean;
  v_excel_row        int;
  v_segment_normalized company_segment;
BEGIN
  IF jsonb_typeof(p_rows) <> 'array' THEN
    RAISE EXCEPTION 'p_rows must be a JSON array';
  END IF;

  FOR v_row IN SELECT * FROM jsonb_array_elements(p_rows)
  LOOP
    BEGIN
      v_email := lower(trim(v_row->>'email'));
      v_excel_row := COALESCE((v_row->>'excel_row')::int, 0);

      IF v_email IS NULL OR v_email = '' THEN
        v_errors := v_errors + 1;
        v_error_log := v_error_log || jsonb_build_object(
          'excel_row', v_excel_row,
          'error', 'Email vazio'
        );
        CONTINUE;
      END IF;

      -- Validate enum cast for company_segment_normalized
      IF v_row ? 'company_segment_normalized' AND v_row->>'company_segment_normalized' IS NOT NULL THEN
        BEGIN
          v_segment_normalized := (v_row->>'company_segment_normalized')::company_segment;
        EXCEPTION WHEN invalid_text_representation THEN
          v_segment_normalized := 'OUTRO'::company_segment;
        END;
      ELSE
        v_segment_normalized := NULL;
      END IF;

      SELECT EXISTS(
        SELECT 1 FROM participants
        WHERE email = v_email AND edition_id = p_edition_id
      ) INTO v_existed;

      INSERT INTO participants (
        edition_id, email, full_name, company,
        job_title, cpf, phone, payment_status,
        is_company_member, company_segment_raw, company_segment_normalized,
        ticket_membership, ticket_value, import_job_id
      ) VALUES (
        p_edition_id,
        v_email,
        v_row->>'full_name',
        NULLIF(v_row->>'company', ''),
        NULLIF(v_row->>'job_title', ''),
        NULLIF(v_row->>'cpf', ''),
        NULLIF(v_row->>'phone', ''),
        NULLIF(v_row->>'payment_status', ''),
        CASE
          WHEN v_row->>'is_company_member' IN ('true','false')
            THEN (v_row->>'is_company_member')::boolean
          ELSE NULL
        END,
        NULLIF(v_row->>'company_segment_raw', ''),
        v_segment_normalized,
        (v_row->>'ticket_membership')::ticket_membership,
        NULLIF(v_row->>'ticket_value','')::numeric,
        p_import_job_id
      )
      ON CONFLICT (email, edition_id) DO UPDATE SET
        full_name = EXCLUDED.full_name,
        company = EXCLUDED.company,
        job_title = EXCLUDED.job_title,
        cpf = EXCLUDED.cpf,
        phone = EXCLUDED.phone,
        payment_status = EXCLUDED.payment_status,
        is_company_member = EXCLUDED.is_company_member,
        company_segment_raw = EXCLUDED.company_segment_raw,
        company_segment_normalized = EXCLUDED.company_segment_normalized,
        ticket_membership = EXCLUDED.ticket_membership,
        ticket_value = EXCLUDED.ticket_value,
        import_job_id = EXCLUDED.import_job_id;

      IF v_existed THEN
        v_updated := v_updated + 1;
      ELSE
        v_inserted := v_inserted + 1;
      END IF;

    EXCEPTION WHEN OTHERS THEN
      v_errors := v_errors + 1;
      v_error_log := v_error_log || jsonb_build_object(
        'excel_row', v_excel_row,
        'email', v_email,
        'error', SQLERRM
      );
    END;
  END LOOP;

  RETURN json_build_object(
    'inserted',  v_inserted,
    'updated',   v_updated,
    'errors',    v_errors,
    'error_log', v_error_log
  );
END;
$$ LANGUAGE plpgsql VOLATILE;
```

**Aplicação da migration — caminho primário (MCP) com fallback CLI:**

Caminho **primário (preferido)**: `mcp__claude_ai_Supabase__apply_migration` com:
- name: `005_upsert_participants_rpc`
- query: o conteúdo SQL acima.

Caminho de **fallback** — se o MCP Supabase não estiver disponível: `cd abvcap-congress && npx supabase db push --project-ref kcgwyzvwxmmygfdsetgd`.

Mesmas pré-condições da Task 1.
  </action>
  <verify>
    <automated>Após aplicar a migration, validar via UM dos dois caminhos:

CAMINHO A (MCP — preferido): chamar `mcp__claude_ai_Supabase__execute_sql` com: `SELECT proname, pronargs FROM pg_proc WHERE proname='upsert_participants_batch';` — deve retornar 1 linha com pronargs=3. Em seguida, testar com dados sintéticos: `SELECT upsert_participants_batch('[{"email":"test@phase2.dev","full_name":"Test User","ticket_membership":"MEMBRO","ticket_value":"100","excel_row":3}]'::jsonb, (SELECT id FROM editions WHERE year=2025), gen_random_uuid());` — deve retornar JSON com inserted=1, updated=0, errors=0. Depois LIMPAR: `DELETE FROM participants WHERE email='test@phase2.dev';`.

CAMINHO B (CLI fallback): `cd abvcap-congress; npx supabase db remote query "SELECT proname, pronargs FROM pg_proc WHERE proname='upsert_participants_batch';"` — mesmo resultado esperado, depois executar a chamada de teste via `psql` ou `supabase db remote query` com o mesmo SELECT acima.

Pelo menos UM caminho deve passar.</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `005_upsert_participants_rpc.sql` existe.
    - O arquivo contém literalmente `CREATE OR REPLACE FUNCTION upsert_participants_batch`.
    - O arquivo contém literalmente `ON CONFLICT (email, edition_id) DO UPDATE SET`.
    - O arquivo contém literalmente o retorno JSON com keys `inserted`, `updated`, `errors`, `error_log`.
    - Query `SELECT proname FROM pg_proc WHERE proname='upsert_participants_batch'` no Supabase retorna 1 linha.
    - Chamada de teste com 1 linha válida retorna `{"inserted":1,"updated":0,"errors":0,"error_log":[]}`.
    - Segunda chamada com a mesma linha retorna `{"inserted":0,"updated":1,"errors":0,"error_log":[]}`.
    - Chamada com email vazio retorna errors >= 1 e error_log com mensagem "Email vazio".
  </acceptance_criteria>
  <done>
Função `upsert_participants_batch(jsonb, uuid, uuid)` deployada. Faz upsert atômico por (email, edition_id), conta inseridos vs atualizados, captura erros por linha sem abortar a transação inteira. Testes com dados sintéticos passam.
  </done>
</task>

<task type="auto">
  <name>Task 4: Migration 006 — RPC upsert_form_responses_batch</name>
  <files>abvcap-congress/supabase/migrations/006_upsert_form_responses_rpc.sql</files>
  <read_first>
    - abvcap-congress/supabase/migrations/001_foundation.sql (linha 66 — `participant_id uuid REFERENCES participants(id) UNIQUE NOT NULL` — confirma que ON CONFLICT (participant_id) é válido)
    - abvcap-congress/supabase/migrations/004_phase2_form_responses_extensions.sql (lista atualizada de colunas TEXT[])
    - .planning/phases/02-import-pipeline-auth/CONTEXT.md (campos do form_responses parseados em Plan 03: topics_of_interest, interested_in_events, preferred_channels, content_interests, dietary_restrictions, dietary_details)
    - .planning/phases/02-import-pipeline-auth/02-03-PLAN.md (linhas 145-150, 549-554 — confirma shape do `ParticipantRow` com os 6 campos de form)
  </read_first>
  <action>
**Motivação (corrige checker BLOCKER):** Plan 03 parseia 6 campos de form (topics_of_interest, interested_in_events, preferred_channels, content_interests, dietary_restrictions, dietary_details) e os armazena no preview, mas sem esta RPC o commit route (Plan 04) não tem como persistir esses campos — eles seriam descartados na serialização e form_responses ficaria vazio após cada import.

Criar `abvcap-congress/supabase/migrations/006_upsert_form_responses_rpc.sql` com a função que faz upsert atômico em form_responses, resolvendo `participant_id` via JOIN com `participants` usando (email, edition_id).

Conteúdo EXATO:

```sql
-- Migration 006: RPC para upsert em batch de respostas de formulário (Phase 2)
-- Resolve participant_id via (email, edition_id) e faz upsert em form_responses
-- por participant_id (que tem constraint UNIQUE em participants(id)).
-- DEVE ser chamada DEPOIS de upsert_participants_batch no mesmo commit,
-- pois depende dos participants já existirem.

CREATE OR REPLACE FUNCTION upsert_form_responses_batch(
  p_rows       jsonb,
  p_edition_id uuid
)
RETURNS json AS $$
DECLARE
  v_row             jsonb;
  v_inserted        int := 0;
  v_updated         int := 0;
  v_errors          int := 0;
  v_error_log       jsonb := '[]'::jsonb;
  v_email           text;
  v_excel_row       int;
  v_participant_id  uuid;
  v_existed         boolean;
  v_topics          text[];
  v_events          text[];
  v_channels        text[];
  v_contents        text[];
  v_dietary_rest    text;
  v_dietary_details text;
BEGIN
  IF jsonb_typeof(p_rows) <> 'array' THEN
    RAISE EXCEPTION 'p_rows must be a JSON array';
  END IF;

  FOR v_row IN SELECT * FROM jsonb_array_elements(p_rows)
  LOOP
    BEGIN
      v_email := lower(trim(v_row->>'participant_email'));
      v_excel_row := COALESCE((v_row->>'excel_row')::int, 0);

      IF v_email IS NULL OR v_email = '' THEN
        v_errors := v_errors + 1;
        v_error_log := v_error_log || jsonb_build_object(
          'excel_row', v_excel_row,
          'error', 'participant_email vazio'
        );
        CONTINUE;
      END IF;

      -- Resolver participant_id via (email, edition_id)
      SELECT id INTO v_participant_id
      FROM participants
      WHERE email = v_email AND edition_id = p_edition_id;

      IF v_participant_id IS NULL THEN
        v_errors := v_errors + 1;
        v_error_log := v_error_log || jsonb_build_object(
          'excel_row', v_excel_row,
          'email', v_email,
          'error', 'Participant não encontrado para (email, edition_id) — upsert_participants_batch foi chamado antes?'
        );
        CONTINUE;
      END IF;

      -- Cast arrays JSON → text[]; jsonb_array_elements_text trata cada elemento como text
      v_topics := CASE
        WHEN jsonb_typeof(v_row->'topics_of_interest') = 'array'
          THEN ARRAY(SELECT jsonb_array_elements_text(v_row->'topics_of_interest'))
        ELSE NULL
      END;
      v_events := CASE
        WHEN jsonb_typeof(v_row->'interested_in_events') = 'array'
          THEN ARRAY(SELECT jsonb_array_elements_text(v_row->'interested_in_events'))
        ELSE NULL
      END;
      v_channels := CASE
        WHEN jsonb_typeof(v_row->'preferred_channels') = 'array'
          THEN ARRAY(SELECT jsonb_array_elements_text(v_row->'preferred_channels'))
        ELSE NULL
      END;
      v_contents := CASE
        WHEN jsonb_typeof(v_row->'content_interests') = 'array'
          THEN ARRAY(SELECT jsonb_array_elements_text(v_row->'content_interests'))
        ELSE NULL
      END;
      v_dietary_rest    := NULLIF(v_row->>'dietary_restrictions', '');
      v_dietary_details := NULLIF(v_row->>'dietary_details', '');

      SELECT EXISTS(SELECT 1 FROM form_responses WHERE participant_id = v_participant_id)
      INTO v_existed;

      INSERT INTO form_responses (
        participant_id,
        topics_of_interest,
        interested_in_events,
        preferred_channels,
        content_interests,
        dietary_restrictions,
        dietary_details
      ) VALUES (
        v_participant_id,
        v_topics,
        v_events,
        v_channels,
        v_contents,
        v_dietary_rest,
        v_dietary_details
      )
      ON CONFLICT (participant_id) DO UPDATE SET
        topics_of_interest   = EXCLUDED.topics_of_interest,
        interested_in_events = EXCLUDED.interested_in_events,
        preferred_channels   = EXCLUDED.preferred_channels,
        content_interests    = EXCLUDED.content_interests,
        dietary_restrictions = EXCLUDED.dietary_restrictions,
        dietary_details      = EXCLUDED.dietary_details;

      IF v_existed THEN
        v_updated := v_updated + 1;
      ELSE
        v_inserted := v_inserted + 1;
      END IF;

    EXCEPTION WHEN OTHERS THEN
      v_errors := v_errors + 1;
      v_error_log := v_error_log || jsonb_build_object(
        'excel_row', v_excel_row,
        'email', v_email,
        'error', SQLERRM
      );
    END;
  END LOOP;

  RETURN json_build_object(
    'inserted',  v_inserted,
    'updated',   v_updated,
    'errors',    v_errors,
    'error_log', v_error_log
  );
END;
$$ LANGUAGE plpgsql VOLATILE;
```

**Aplicação da migration — caminho primário (MCP) com fallback CLI:**

Caminho **primário (preferido)**: `mcp__claude_ai_Supabase__apply_migration` com:
- name: `006_upsert_form_responses_rpc`
- query: o conteúdo SQL acima.

Caminho de **fallback** — se o MCP Supabase não estiver disponível: `cd abvcap-congress && npx supabase db push --project-ref kcgwyzvwxmmygfdsetgd`.

**Nota de design — coluna `dietary_restrictions`:** o schema original (001_foundation.sql) define `dietary_restrictions` como TEXT, e o parser do Plan 03 produz `'Sim' | 'Não' | null`. A RPC armazena o valor textual diretamente. Quando o parser produzir `null`, a RPC grava `NULL` graças ao `NULLIF(..., '')` — comportamento idempotente em reimport.
  </action>
  <verify>
    <automated>Após aplicar a migration, validar via UM dos dois caminhos:

CAMINHO A (MCP — preferido): `mcp__claude_ai_Supabase__execute_sql` com: `SELECT proname, pronargs FROM pg_proc WHERE proname='upsert_form_responses_batch';` — deve retornar 1 linha com pronargs=2.

Depois, smoke test (assume que upsert_participants_batch foi testado antes na Task 3 e o participant test@phase2.dev pode ser recriado temporariamente):
```sql
-- Setup: criar um participant de teste primeiro
SELECT upsert_participants_batch(
  '[{"email":"formtest@phase2.dev","full_name":"Form Test","ticket_membership":"MEMBRO","ticket_value":"0","excel_row":3}]'::jsonb,
  (SELECT id FROM editions WHERE year=2025),
  gen_random_uuid()
);

-- Testar a RPC de form_responses
SELECT upsert_form_responses_batch(
  '[{
    "participant_email":"formtest@phase2.dev",
    "excel_row":3,
    "topics_of_interest":["Private Equity","Venture Capital"],
    "interested_in_events":["LP Day"],
    "preferred_channels":["Email"],
    "content_interests":["Webinars"],
    "dietary_restrictions":"Sim",
    "dietary_details":"Vegetariano"
  }]'::jsonb,
  (SELECT id FROM editions WHERE year=2025)
);
-- Esperado: {"inserted":1,"updated":0,"errors":0,"error_log":[]}

-- Segunda chamada deve UPDATE
SELECT upsert_form_responses_batch(
  '[{"participant_email":"formtest@phase2.dev","excel_row":3,"topics_of_interest":["Outro"]}]'::jsonb,
  (SELECT id FROM editions WHERE year=2025)
);
-- Esperado: {"inserted":0,"updated":1,"errors":0,"error_log":[]}

-- Confirmar persistência
SELECT topics_of_interest, interested_in_events, dietary_restrictions FROM form_responses
WHERE participant_id = (SELECT id FROM participants WHERE email='formtest@phase2.dev');
-- topics_of_interest deve ser {"Outro"} (sobrescrito pela 2ª call)

-- LIMPAR
DELETE FROM form_responses WHERE participant_id = (SELECT id FROM participants WHERE email='formtest@phase2.dev');
DELETE FROM participants WHERE email='formtest@phase2.dev';
```

CAMINHO B (CLI fallback): mesmos passos via `npx supabase db remote query "..."` ou `psql`.

Pelo menos UM caminho deve passar.</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `006_upsert_form_responses_rpc.sql` existe.
    - O arquivo contém literalmente `CREATE OR REPLACE FUNCTION upsert_form_responses_batch`.
    - O arquivo contém literalmente `ON CONFLICT (participant_id) DO UPDATE SET`.
    - O arquivo contém literalmente `jsonb_array_elements_text(v_row->'topics_of_interest')`.
    - O arquivo contém literalmente as 6 colunas no INSERT: topics_of_interest, interested_in_events, preferred_channels, content_interests, dietary_restrictions, dietary_details.
    - Query `SELECT proname FROM pg_proc WHERE proname='upsert_form_responses_batch'` no Supabase retorna 1 linha com pronargs=2.
    - Smoke test 1 (insert) retorna `{"inserted":1,"updated":0,"errors":0,"error_log":[]}`.
    - Smoke test 2 (mesma chamada com topics diferente) retorna `{"inserted":0,"updated":1,"errors":0,"error_log":[]}`.
    - SELECT em form_responses confirma topics_of_interest = `{"Outro"}` após o UPDATE.
    - Chamada com email inexistente retorna errors >= 1 e error_log com mensagem "Participant não encontrado...".
  </acceptance_criteria>
  <done>
Função `upsert_form_responses_batch(jsonb, uuid)` deployada. Resolve participant_id via (email, edition_id), faz upsert atômico por participant_id, cast correto de jsonb arrays para TEXT[] em todas as 4 colunas multi-select. Smoke tests passam. Plan 04 Task 2 agora tem como persistir os campos de formulário.
  </done>
</task>

<task type="auto">
  <name>Task 5: [BLOCKING] Regenerar database.types.ts com schema atualizado</name>
  <files>abvcap-congress/lib/database.types.ts</files>
  <read_first>
    - abvcap-congress/lib/database.types.ts (estado atual — para comparar após regeneração)
    - .planning/STATE.md (project ID: kcgwyzvwxmmygfdsetgd)
  </read_first>
  <action>
[BLOCKING] As migrations 003, 004, 005 e 006 já foram aplicadas via Supabase MCP (ou CLI fallback) nas tasks anteriores — NÃO há push pendente. Esta task SOMENTE regenera os tipos TypeScript a partir do estado atual do banco.

Executar a partir do diretório `abvcap-congress/`:

```powershell
npx supabase gen types typescript --project-id kcgwyzvwxmmygfdsetgd --schema public > lib/database.types.ts
```

Depois do comando, preservar manualmente o bloco de "Convenience aliases" no final do arquivo (linhas 211-229 do arquivo original) anexando-o ao final do arquivo gerado, mais a adição de novos aliases:

```typescript
// ─── Convenience aliases ───────────────────────────────────────────────────

export type TicketMembership = Database["public"]["Enums"]["ticket_membership"]
export type CompanySegment = Database["public"]["Enums"]["company_segment"]
export type ImportStatus = Database["public"]["Enums"]["import_status"]

export type Participant = Database["public"]["Tables"]["participants"]["Row"]
export type Edition = Database["public"]["Tables"]["editions"]["Row"]
export type ImportJob = Database["public"]["Tables"]["import_jobs"]["Row"]
export type FormResponse = Database["public"]["Tables"]["form_responses"]["Row"]

export interface OverviewStats {
  total: number
  membro: number
  nao_membro: number
  total_revenue: number
  avg_ticket: number
}
```

Se o comando `npx supabase gen types` falhar (CLI ausente, login pendente etc.), usar o MCP fallback: `mcp__claude_ai_Supabase__generate_typescript_types` e gravar o resultado em `abvcap-congress/lib/database.types.ts`, e em seguida adicionar manualmente o bloco "Convenience aliases" acima.

Após gerar, rodar `npx tsc --noEmit` em `abvcap-congress/` e CORRIGIR qualquer erro de tipo decorrente (esperado: zero erros — esta task é só regeneração + tipos).
  </action>
  <verify>
    <automated>cd abvcap-congress && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo `abvcap-congress/lib/database.types.ts` contém literalmente a string `company_segment_normalized:` (nova coluna em participants).
    - O arquivo contém literalmente `is_company_member:` (nova coluna em participants).
    - O arquivo contém literalmente `content_interests:` (nova coluna em form_responses).
    - O arquivo contém literalmente `dietary_details:` (nova coluna em form_responses).
    - O arquivo contém o tipo de `interested_in_events` como `string[] | null` (NÃO `boolean | null`). Grep por `interested_in_events: string\[\] | null` deve retornar match.
    - O arquivo contém `upsert_participants_batch:` na seção `Functions` do tipo Database.
    - O arquivo contém `upsert_form_responses_batch:` na seção `Functions` do tipo Database.
    - O arquivo contém o bloco `// ─── Convenience aliases ─` preservado.
    - `npx tsc --noEmit` em `abvcap-congress/` retorna 0 erros.
  </acceptance_criteria>
  <done>
`lib/database.types.ts` regenerado refletindo as migrations 003/004/005/006. participants tem 7 novas colunas tipadas, form_responses.interested_in_events agora é `string[] | null`, RPCs `upsert_participants_batch` e `upsert_form_responses_batch` aparecem em Functions. `npx tsc --noEmit` zero erros.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App server → Postgres | Input vindo do parser (Phase 2 Plan 03) cruza para escrita via RPC. JSONB é parsed e cast para enums/arrays. |
| Migrations CI/CD → Supabase | Aplicação de DDL via MCP `apply_migration` ou CLI `db push` — privilégio elevado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.01-01 | Tampering | upsert_participants_batch RPC | mitigate | Cast explícito para `company_segment` com fallback para `OUTRO` em caso de `invalid_text_representation`; `ticket_membership` cast direto falha cedo se valor inválido (capturado pelo BEGIN/EXCEPTION e contabilizado em errors, não corrompe banco). |
| T-02.01-02 | Denial of Service | RPC com payload muito grande | mitigate | Documentar limite de 500 linhas por chamada (Plan 04 fará chunking); Postgres recusará jsonb > 1GB nativamente. Sem retry agressivo no cliente. |
| T-02.01-03 | Elevation of Privilege | RPC pode ser chamada por anon role | accept | Acesso à RPC é via service_role do Next.js (Phase 1 isolou a chave server-side); RLS não está habilitado nesta fase. Risco aceito até v2 quando RLS for habilitado em todas tabelas. |
| T-02.01-04 | Information Disclosure | error_log JSON pode conter PII (email) | mitigate | error_log armazena apenas email + excel_row + mensagem SQL; nenhum dado de CPF, phone ou ticket_value é serializado em erro. Logs de import_jobs ficam atrás de auth admin (Plan 02). |
| T-02.01-05 | Tampering | SQL injection via jsonb body | accept | Postgres jsonb não interpreta valores como SQL; todas inserções usam binding paramétrico via `EXCLUDED.*`. Risco extremamente baixo. |
| T-02.01-06 | Repudiation | Quem aplicou a migration | mitigate | Migrations versionadas em `supabase/migrations/` com prefixo numérico; commit Git registra autor. |
| T-02.01-07 | Tampering | upsert_form_responses_batch chamada antes de upsert_participants_batch deixa órfãos | mitigate | RPC retorna erro por linha com mensagem clara "Participant não encontrado"; Plan 04 commit route DEVE chamar participants ANTES de form_responses no mesmo job, validado em checkpoint final. |
| T-02.01-08 | Information Disclosure | error_log de form_responses pode vazar email | mitigate | Mesma política do T-02.01-04 — somente email + excel_row + SQLERRM, sem outros campos sensíveis. |
</threat_model>

<verification>
1. Supabase MCP `list_tables` mostra participants com 7 novas colunas e form_responses com 3 novas (interested_in_events TEXT[], content_interests TEXT[], dietary_details TEXT).
2. `SELECT proname FROM pg_proc WHERE proname IN ('upsert_participants_batch','upsert_form_responses_batch')` retorna 2 linhas.
3. Chamada de smoke test à RPC de participants com 1 linha válida retorna `inserted=1`. Segunda chamada com mesma linha retorna `updated=1`.
4. Chamada de smoke test à RPC de form_responses (após participant criado) retorna `inserted=1`; segunda chamada com mesmo email mas dados diferentes retorna `updated=1` e o SELECT confirma sobrescrita.
5. `npx tsc --noEmit` em `abvcap-congress/` retorna zero erros.
6. `lib/database.types.ts` contém todas as colunas novas, as duas RPCs em Functions, e o bloco de Convenience aliases.
</verification>

<success_criteria>
- Schema estendido conforme decisões de CONTEXT.md.
- Duas RPCs atômicas disponíveis para Plan 04 consumir: participants + form_responses.
- Tipos TS sincronizados com banco real (incluindo as duas novas Functions).
- Zero quebras no código existente da Phase 1 (overview ainda funciona — não modificamos colunas existentes, apenas adicionamos).
- form_responses garantidamente populável após import (corrige BLOCKER do checker).
</success_criteria>

<output>
Após completar, criar `.planning/phases/02-import-pipeline-auth/02-01-SUMMARY.md` resumindo:
- Migrations aplicadas (003, 004, 005, 006)
- Novos campos em participants e form_responses
- Assinaturas das RPCs upsert_participants_batch e upsert_form_responses_batch
- Regeneração de tipos confirmada (tsc zero erros)
- Tabela de mudanças vs schema Phase 1
</output>
</content>
</invoke>