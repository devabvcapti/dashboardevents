---
phase: 03-dashboard-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - abvcap-congress/supabase/migrations/007_remove_editions_year_unique.sql
  - abvcap-congress/supabase/migrations/008_rpc_stats_by_edition_id.sql
  - abvcap-congress/supabase/migrations/009_rpc_member_revenue_analysis.sql
  - abvcap-congress/supabase/migrations/010_participant_filter_indexes.sql
  - abvcap-congress/lib/database.types.ts
  - abvcap-congress/lib/data.ts
  - abvcap-congress/lib/edition-cookie.ts
  - abvcap-congress/components/edition-selector.tsx
  - abvcap-congress/components/sidebar.tsx
  - abvcap-congress/app/dashboard/layout.tsx
  - abvcap-congress/app/api/edition/select/route.ts
  - abvcap-congress/app/dashboard/eventos/page.tsx
  - abvcap-congress/app/dashboard/eventos/eventos-client.tsx
  - abvcap-congress/app/api/edition/create/route.ts
  - abvcap-congress/app/dashboard/import/page.tsx
  - abvcap-congress/app/dashboard/import/import-client.tsx
  - abvcap-congress/app/api/import/commit/route.ts
  - abvcap-congress/package.json
autonomous: false
requirements:
  - OV-01
  - OV-02
user_setup: []

must_haves:
  truths:
    - "A constraint UNIQUE em editions.year não existe mais — múltiplos eventos com o mesmo ano podem ser criados"
    - "RPC get_overview_stats aceita p_edition_id uuid e retorna unique_companies + states_represented"
    - "RPCs get_member_analysis e get_revenue_analysis estão deployadas e retornam JSON estruturado"
    - "Um admin acessando o dashboard sem cookie é automaticamente vinculado à edição com maior ano"
    - "Trocar a edição no sidebar persiste em cookie httpOnly e re-fetcha os Server Components via router.refresh()"
    - "A página /dashboard/eventos lista edições existentes e permite criar nova edição (nome + ano)"
    - "Se não houver edições, /dashboard/import bloqueia upload e linka para /dashboard/eventos"
    - "/api/import/commit aceita editionId (uuid) diretamente — sem lookup por editionYear"
    - "lib/data.ts não tem mais editionYear hardcoded — todas as funções aceitam editionId: string"
    - "exceljs foi movido de devDependencies para dependencies em package.json"
  artifacts:
    - path: "abvcap-congress/supabase/migrations/007_remove_editions_year_unique.sql"
      provides: "ALTER TABLE editions DROP CONSTRAINT IF EXISTS editions_year_key"
      contains: "DROP CONSTRAINT IF EXISTS editions_year_key"
    - path: "abvcap-congress/supabase/migrations/008_rpc_stats_by_edition_id.sql"
      provides: "RPC get_overview_stats(p_edition_id uuid) com unique_companies e states_represented"
      contains: "p_edition_id uuid"
    - path: "abvcap-congress/supabase/migrations/009_rpc_member_revenue_analysis.sql"
      provides: "RPCs get_member_analysis e get_revenue_analysis"
      contains: "get_member_analysis"
    - path: "abvcap-congress/supabase/migrations/010_participant_filter_indexes.sql"
      provides: "Índices de suporte para filtros da lista"
      contains: "idx_participants_edition_membership"
    - path: "abvcap-congress/lib/edition-cookie.ts"
      provides: "getActiveEditionId() server-side com fallback para edição mais recente"
      contains: "getActiveEditionId"
    - path: "abvcap-congress/lib/data.ts"
      provides: "Todas as funções aceitam editionId: string; getEditions(); getParticipantsPaginated(); getParticipantsForExport(); getMemberAnalysis(); getRevenueAnalysis()"
      contains: "getParticipantsPaginated"
    - path: "abvcap-congress/components/edition-selector.tsx"
      provides: "Dropdown 'use client' que chama /api/edition/select + router.refresh()"
      contains: "router.refresh()"
    - path: "abvcap-congress/components/sidebar.tsx"
      provides: "Sidebar que recebe editions e activeEditionId como props e renderiza EditionSelector + novo link Eventos"
      contains: "EditionSelector"
    - path: "abvcap-congress/app/dashboard/layout.tsx"
      provides: "Server Component que fetcha editions + activeEditionId e passa para Sidebar"
      contains: "getEditions"
    - path: "abvcap-congress/app/api/edition/select/route.ts"
      provides: "POST handler que valida editionId via Zod + verifica existência no banco antes de setar cookie"
      contains: "active_edition_id"
    - path: "abvcap-congress/app/dashboard/eventos/page.tsx"
      provides: "Página de gestão de edições (lista + form de criação)"
      contains: "getEditions"
    - path: "abvcap-congress/app/api/edition/create/route.ts"
      provides: "POST handler que cria nova edição (Zod: name + year)"
      contains: "insert"
    - path: "abvcap-congress/app/api/import/commit/route.ts"
      provides: "Aceita editionId uuid diretamente no body (substitui editionYear)"
      contains: "editionId: z.string().uuid()"
  key_links:
    - from: "abvcap-congress/components/sidebar.tsx"
      to: "abvcap-congress/components/edition-selector.tsx"
      via: "render direto após o bloco do logo, antes do menu Nav"
      pattern: "EditionSelector"
    - from: "abvcap-congress/components/edition-selector.tsx"
      to: "/api/edition/select"
      via: "fetch POST + router.refresh()"
      pattern: "fetch.*api/edition/select"
    - from: "abvcap-congress/app/dashboard/layout.tsx"
      to: "abvcap-congress/lib/data.ts (getEditions) + lib/edition-cookie.ts (getActiveEditionId)"
      via: "Promise.all em Server Component"
      pattern: "getEditions"
    - from: "abvcap-congress/app/api/import/commit/route.ts"
      to: "tabela editions via Supabase service role"
      via: ".eq('id', body.editionId).single()"
      pattern: "editionId"
---

<objective>
Construir toda a infraestrutura multi-evento que sustenta as demais páginas da Phase 3. Esta é a Wave 1 da fase: nada mais pode ser construído sem que (a) a constraint UNIQUE em editions.year esteja removida, (b) get_overview_stats aceite uuid, (c) os helpers de edition-cookie e getEditions existam, (d) o sidebar tenha o EditionSelector, e (e) o commit route aceite editionId direto.

Esta plano também adiciona migrations 009 (RPCs de member/revenue) e 010 (índices) — Plans 02/04/05 dependem dessas RPCs e índices, mas só consomem as funções já tipadas/exportadas em lib/data.ts, não modificam migrations.

Purpose: Eliminar todo o lock em editionYear=2025 do código atual e habilitar gestão de múltiplos eventos com seletor global. Sem isso, OV-01..OV-04, LIST-01..05, MBR-01..03 e REV-01..03 não podem ser implementados corretamente.

Output: 4 migrations novas + push aplicado, lib/data.ts refatorado com TODAS as funções editionId-only e os novos helpers paginated/export/member/revenue, helper edition-cookie, EditionSelector, sidebar com nova prop, dashboard layout server-side, página /dashboard/eventos, route handlers /api/edition/select e /api/edition/create, commit route migrado, exceljs movido para dependencies.

Maps decisions: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13. Maps requirements: OV-01 (unique_companies/states_represented já vêm na RPC nova), OV-02 (idem).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dashboard-core/03-CONTEXT.md
@.planning/phases/03-dashboard-core/03-RESEARCH.md
@abvcap-congress/lib/data.ts
@abvcap-congress/lib/database.types.ts
@abvcap-congress/lib/supabase.ts
@abvcap-congress/lib/supabase-server.ts
@abvcap-congress/lib/auth.ts
@abvcap-congress/components/sidebar.tsx
@abvcap-congress/components/ui/select.tsx
@abvcap-congress/app/dashboard/layout.tsx
@abvcap-congress/app/dashboard/page.tsx
@abvcap-congress/app/api/import/commit/route.ts
@abvcap-congress/app/dashboard/import/page.tsx
@abvcap-congress/app/dashboard/import/import-client.tsx
@abvcap-congress/supabase/migrations/001_foundation.sql
@abvcap-congress/supabase/migrations/002_rpc_stats.sql
@abvcap-congress/package.json

<interfaces>
<!-- Tipos e contratos que esta plano cria/modifica. Os Plans 02-05 dependem disso. -->

Em lib/database.types.ts (regenerar OU editar à mão para refletir migrations 008/009):

```typescript
// OverviewStats DEVE incluir os 2 novos campos:
export interface OverviewStats {
  total: number
  membro: number
  nao_membro: number
  total_revenue: number
  avg_ticket: number
  unique_companies: number   // NOVO (OV-02)
  states_represented: number // NOVO (OV-02)
}

// Em Functions:
//   get_overview_stats: { Args: { p_edition_id: string }; Returns: Json }
//   get_member_analysis: { Args: { p_edition_id: string }; Returns: Json }
//   get_revenue_analysis: { Args: { p_edition_id: string }; Returns: Json }

// Novos tipos auxiliares (exportados de lib/data.ts):
export interface MemberAnalysisRow {
  segment: string           // 'GP' | 'LP' | 'FUNDO' | 'CORPORATIVO' | 'GOVERNO' | 'ACADEMIA' | 'OUTRO' | 'SEM_SEGMENTO'
  membro_count: number
  nao_membro_count: number
  total: number
  membership_pct: number    // 0..100
}

export interface RevenueByMembership {
  ticket_membership: 'MEMBRO' | 'NAO_MEMBRO'
  count: number
  total_revenue: number
  avg_ticket: number
}

export interface RevenueHistogramBucket {
  faixa: string             // 'Gratuito' | 'R$1–500' | 'R$501–1000' | etc
  count: number
  min_val: number | null
  max_val: number | null
}

export interface RevenueAnalysis {
  by_membership: RevenueByMembership[]
  histogram: RevenueHistogramBucket[]
}

export interface PaginatedParticipants {
  data: Participant[]
  count: number
}

export interface ParticipantWithState extends Participant {
  origin_state: string | null
}
```

Em lib/data.ts — assinaturas FINAIS (todas em editionId: string):

```typescript
export async function getOverviewStats(editionId: string): Promise<OverviewStats>
export async function getEditions(): Promise<Edition[]>
export async function getCompanySegmentSummary(editionId: string): Promise<CompanySegmentSummary[]>
export async function getRegistrationsByDay(editionId: string): Promise<RegistrationsByDay[]>
export async function getTicketMembershipSummary(editionId: string): Promise<TicketMembershipSummary[]>
export async function getFreeTicketStats(editionId: string): Promise<FreeTicketStats>

export async function getParticipantsPaginated(filters: {
  editionId: string
  search?: string
  membership?: TicketMembership
  segment?: CompanySegment
  state?: string
  minValue?: number
  maxValue?: number
  sort?: string
  dir?: 'asc' | 'desc'
  limit: number
  offset: number
}): Promise<PaginatedParticipants>

export async function getParticipantsForExport(filters: {
  editionId: string
  search?: string
  membership?: TicketMembership
  segment?: CompanySegment
  state?: string
  minValue?: number
  maxValue?: number
}): Promise<ParticipantWithState[]>

export async function getMemberAnalysis(editionId: string): Promise<MemberAnalysisRow[]>
export async function getRevenueAnalysis(editionId: string): Promise<RevenueAnalysis>
```

Em lib/edition-cookie.ts:

```typescript
export async function getActiveEditionId(): Promise<string>
// throws Error('Nenhuma edição cadastrada') quando getEditions() retorna []
```

Em components/sidebar.tsx — nova assinatura:

```typescript
export function Sidebar({
  editions,
  activeEditionId,
}: {
  editions: { id: string; name: string; year: number }[]
  activeEditionId: string
}): JSX.Element
```

Em app/api/edition/select/route.ts:
- POST body: `{ editionId: string (uuid) }` — Zod validation
- valida que editionId existe em editions antes de setar cookie
- seta cookie 'active_edition_id', httpOnly, sameSite lax, secure em produção, maxAge 1 ano

Em app/api/edition/create/route.ts:
- POST body: `{ name: string (1-200 chars), year: number (2000-2100) }`
- requireAdmin()
- insere em editions, retorna { id, name, year, created_at }

Em app/api/import/commit/route.ts (BREAKING CHANGE):
- Body Zod schema atual: `{ serverToken, editionYear?: number }`
- Body novo: `{ serverToken: string (32-64 chars), editionId: z.string().uuid() }`
- Remove o lookup `.eq('year', body.editionYear)`
- Usa `.eq('id', body.editionId).single()` para resolver edition
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migrations 007/008/009/010 e aplicar via supabase db push</name>
  <files>abvcap-congress/supabase/migrations/007_remove_editions_year_unique.sql, abvcap-congress/supabase/migrations/008_rpc_stats_by_edition_id.sql, abvcap-congress/supabase/migrations/009_rpc_member_revenue_analysis.sql, abvcap-congress/supabase/migrations/010_participant_filter_indexes.sql, abvcap-congress/lib/database.types.ts, abvcap-congress/package.json</files>
  <read_first>abvcap-congress/supabase/migrations/001_foundation.sql, abvcap-congress/supabase/migrations/002_rpc_stats.sql, abvcap-congress/supabase/migrations/005_upsert_participants_rpc.sql, abvcap-congress/lib/database.types.ts, abvcap-congress/package.json, .planning/phases/03-dashboard-core/03-RESEARCH.md</read_first>
  <action>
Criar 4 migrations SQL na ordem 007 → 010 e regenerar/editar `lib/database.types.ts`. Conteúdo literal de cada arquivo:

**007_remove_editions_year_unique.sql:**
```sql
-- Migration 007: Remove UNIQUE constraint em editions.year (D-06)
-- Permite múltiplos eventos no mesmo ano via /dashboard/eventos.
ALTER TABLE editions DROP CONSTRAINT IF EXISTS editions_year_key;
```

**008_rpc_stats_by_edition_id.sql:**
```sql
-- Migration 008: get_overview_stats aceita p_edition_id uuid (D-11)
-- + adiciona unique_companies e states_represented (OV-02)
DROP FUNCTION IF EXISTS get_overview_stats(int);
DROP FUNCTION IF EXISTS get_overview_stats(p_edition_year int);

CREATE OR REPLACE FUNCTION get_overview_stats(p_edition_id uuid)
RETURNS json AS $$
DECLARE v_result json;
BEGIN
  SELECT json_build_object(
    'total',                COUNT(*),
    'membro',               COUNT(*) FILTER (WHERE p.ticket_membership = 'MEMBRO'),
    'nao_membro',           COUNT(*) FILTER (WHERE p.ticket_membership = 'NAO_MEMBRO'),
    'total_revenue',        COALESCE(SUM(p.ticket_value), 0),
    'avg_ticket',           COALESCE(AVG(p.ticket_value) FILTER (WHERE p.ticket_value > 0), 0),
    'unique_companies',     COUNT(DISTINCT p.company) FILTER (WHERE p.company IS NOT NULL),
    'states_represented',   COUNT(DISTINCT fr.origin_state) FILTER (WHERE fr.origin_state IS NOT NULL)
  ) INTO v_result
  FROM participants p
  LEFT JOIN form_responses fr ON fr.participant_id = p.id
  WHERE p.edition_id = p_edition_id;
  RETURN v_result;
END;
$$ LANGUAGE plpgsql STABLE;
```

**009_rpc_member_revenue_analysis.sql:**
```sql
-- Migration 009: RPCs de análise de membros e receita (MBR-01..03, REV-01..03)

CREATE OR REPLACE FUNCTION get_member_analysis(p_edition_id uuid)
RETURNS json AS $$
BEGIN
  RETURN COALESCE((
    SELECT json_agg(row_to_json(t))
    FROM (
      SELECT
        COALESCE(fr.company_segment::text, 'SEM_SEGMENTO') AS segment,
        COUNT(*) FILTER (WHERE p.ticket_membership = 'MEMBRO')     AS membro_count,
        COUNT(*) FILTER (WHERE p.ticket_membership = 'NAO_MEMBRO') AS nao_membro_count,
        COUNT(*)                                                    AS total,
        ROUND(
          COUNT(*) FILTER (WHERE p.ticket_membership = 'MEMBRO')::numeric
          / NULLIF(COUNT(*), 0) * 100, 1
        ) AS membership_pct
      FROM participants p
      LEFT JOIN form_responses fr ON fr.participant_id = p.id
      WHERE p.edition_id = p_edition_id
      GROUP BY COALESCE(fr.company_segment::text, 'SEM_SEGMENTO')
      ORDER BY total DESC
    ) t
  ), '[]'::json);
END;
$$ LANGUAGE plpgsql STABLE;

CREATE OR REPLACE FUNCTION get_revenue_analysis(p_edition_id uuid)
RETURNS json AS $$
BEGIN
  RETURN json_build_object(
    'by_membership', COALESCE((
      SELECT json_agg(row_to_json(t))
      FROM (
        SELECT
          ticket_membership::text AS ticket_membership,
          COUNT(*) AS count,
          COALESCE(SUM(ticket_value), 0) AS total_revenue,
          COALESCE(AVG(ticket_value) FILTER (WHERE ticket_value > 0), 0) AS avg_ticket
        FROM participants
        WHERE edition_id = p_edition_id
        GROUP BY ticket_membership
      ) t
    ), '[]'::json),
    'histogram', COALESCE((
      SELECT json_agg(row_to_json(t))
      FROM (
        SELECT
          CASE
            WHEN ticket_value = 0     THEN 'Gratuito'
            WHEN ticket_value <= 500  THEN 'R$1–500'
            WHEN ticket_value <= 1000 THEN 'R$501–1000'
            WHEN ticket_value <= 2000 THEN 'R$1001–2000'
            WHEN ticket_value <= 3000 THEN 'R$2001–3000'
            ELSE 'Acima de R$3000'
          END AS faixa,
          COUNT(*) AS count,
          MIN(ticket_value) AS min_val,
          MAX(ticket_value) AS max_val,
          MIN(
            CASE
              WHEN ticket_value = 0     THEN 0
              WHEN ticket_value <= 500  THEN 1
              WHEN ticket_value <= 1000 THEN 2
              WHEN ticket_value <= 2000 THEN 3
              WHEN ticket_value <= 3000 THEN 4
              ELSE 5
            END
          ) AS sort_order
        FROM participants
        WHERE edition_id = p_edition_id
          AND ticket_value IS NOT NULL
        GROUP BY 1
        ORDER BY sort_order
      ) t
    ), '[]'::json)
  );
END;
$$ LANGUAGE plpgsql STABLE;
```

**010_participant_filter_indexes.sql:**
```sql
-- Migration 010: Índices de suporte para filtros da lista (LIST-03)
CREATE INDEX IF NOT EXISTS idx_form_responses_origin_state
  ON form_responses (origin_state) WHERE origin_state IS NOT NULL;

CREATE INDEX IF NOT EXISTS idx_participants_edition_membership
  ON participants (edition_id, ticket_membership);

CREATE INDEX IF NOT EXISTS idx_participants_ticket_value
  ON participants (edition_id, ticket_value) WHERE ticket_value IS NOT NULL;

CREATE INDEX IF NOT EXISTS idx_participants_edition_company
  ON participants (edition_id, company);
```

Em `lib/database.types.ts`:
1. Atualizar `OverviewStats` interface adicionando `unique_companies: number` e `states_represented: number` (manter os 5 campos existentes).
2. No bloco `Functions:`, substituir a assinatura atual de `get_overview_stats: { Args: { p_edition_year?: number }; Returns: Json }` por:
   ```typescript
   get_overview_stats: { Args: { p_edition_id: string }; Returns: Json }
   get_member_analysis: { Args: { p_edition_id: string }; Returns: Json }
   get_revenue_analysis: { Args: { p_edition_id: string }; Returns: Json }
   ```
3. Adicionar no final do arquivo (depois dos aliases existentes):
   ```typescript
   export interface MemberAnalysisRow {
     segment: string
     membro_count: number
     nao_membro_count: number
     total: number
     membership_pct: number
   }
   export interface RevenueByMembership {
     ticket_membership: TicketMembership
     count: number
     total_revenue: number
     avg_ticket: number
   }
   export interface RevenueHistogramBucket {
     faixa: string
     count: number
     min_val: number | null
     max_val: number | null
   }
   export interface RevenueAnalysis {
     by_membership: RevenueByMembership[]
     histogram: RevenueHistogramBucket[]
   }
   export interface PaginatedParticipants {
     data: Participant[]
     count: number
   }
   export interface ParticipantWithState extends Participant {
     origin_state: string | null
   }
   ```

Em `package.json`:
- Remover `"exceljs": "^4.4.0"` de `devDependencies`.
- Adicionar `"exceljs": "^4.4.0"` em `dependencies` (alfabetizado entre `date-fns` e `lucide-react`).
- NÃO mudar a versão. Rodar `cd abvcap-congress && npm install` em seguida para regenerar lockfile.
  </action>
  <verify>
    <automated>cd abvcap-congress && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File abvcap-congress/supabase/migrations/007_remove_editions_year_unique.sql exists
    - File 007 contains exact string `DROP CONSTRAINT IF EXISTS editions_year_key`
    - File abvcap-congress/supabase/migrations/008_rpc_stats_by_edition_id.sql exists
    - File 008 contains exact string `get_overview_stats(p_edition_id uuid)`
    - File 008 contains exact string `unique_companies` and `states_represented`
    - File 008 contains `DROP FUNCTION IF EXISTS get_overview_stats(int)`
    - File abvcap-congress/supabase/migrations/009_rpc_member_revenue_analysis.sql exists
    - File 009 contains exact string `get_member_analysis(p_edition_id uuid)`
    - File 009 contains exact string `get_revenue_analysis(p_edition_id uuid)`
    - File 009 contains exact string `histogram` and `by_membership`
    - File abvcap-congress/supabase/migrations/010_participant_filter_indexes.sql exists
    - File 010 contains exact string `idx_participants_edition_membership`
    - File 010 contains exact string `idx_form_responses_origin_state`
    - File abvcap-congress/lib/database.types.ts contains `unique_companies: number`
    - File database.types.ts contains `states_represented: number`
    - File database.types.ts contains `p_edition_id: string` (not `p_edition_year`)
    - File database.types.ts contains `MemberAnalysisRow` interface
    - File database.types.ts contains `RevenueAnalysis` interface
    - File database.types.ts contains `PaginatedParticipants` interface
    - File abvcap-congress/package.json `dependencies` object contains `"exceljs"`
    - File package.json `devDependencies` object does NOT contain `"exceljs"`
    - `cd abvcap-congress && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>4 migrations criadas, types regenerados/editados, exceljs movido para dependencies, tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: Aplicar migrations no Supabase via db push</name>
  <files>abvcap-congress/supabase/migrations/</files>
  <read_first>abvcap-congress/supabase/migrations/007_remove_editions_year_unique.sql, abvcap-congress/supabase/migrations/008_rpc_stats_by_edition_id.sql, abvcap-congress/supabase/migrations/009_rpc_member_revenue_analysis.sql, abvcap-congress/supabase/migrations/010_participant_filter_indexes.sql</read_first>
  <action>
Aplicar as 4 migrations (007, 008, 009, 010) no projeto Supabase remoto.

Estratégia preferencial: usar a tool MCP `apply_migration` por arquivo, com `name` = nome do arquivo sem extensão e `query` = conteúdo completo.

Ordem obrigatória:
1. `apply_migration` name=`007_remove_editions_year_unique` query=conteúdo do 007
2. `apply_migration` name=`008_rpc_stats_by_edition_id` query=conteúdo do 008
3. `apply_migration` name=`009_rpc_member_revenue_analysis` query=conteúdo do 009
4. `apply_migration` name=`010_participant_filter_indexes` query=conteúdo do 010

Fallback (caso MCP indisponível): rodar `cd abvcap-congress && supabase db push` no PowerShell. Set `SUPABASE_ACCESS_TOKEN` env var antes via `$env:SUPABASE_ACCESS_TOKEN = "..."` se interactive prompt falhar.

Após aplicar, verificar com query:
```sql
SELECT routine_name FROM information_schema.routines
WHERE routine_schema = 'public'
  AND routine_name IN ('get_overview_stats','get_member_analysis','get_revenue_analysis');
```

Esperado: 3 rows.

E verificar constraint removida:
```sql
SELECT constraint_name FROM information_schema.table_constraints
WHERE table_name = 'editions' AND constraint_type = 'UNIQUE';
```

Esperado: 0 rows (ou somente PK, sem UNIQUE em year).

Este task é AUTONOMOUS=false porque depende de credenciais Supabase; se MCP estiver disponível, executar autonomamente. Caso contrário, levantar pedido de execução manual ao usuário.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');['007_remove_editions_year_unique','008_rpc_stats_by_edition_id','009_rpc_member_revenue_analysis','010_participant_filter_indexes'].forEach(f=>{ if(!fs.existsSync('abvcap-congress/supabase/migrations/'+f+'.sql')) {console.error('MISSING '+f); process.exit(1)} }); console.log('migrations present')"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 migration files exist on disk (verified by node script above)
    - Supabase remote returns 3 rows from `SELECT routine_name FROM information_schema.routines WHERE routine_schema='public' AND routine_name IN ('get_overview_stats','get_member_analysis','get_revenue_analysis')`
    - Supabase remote returns 0 rows from `SELECT constraint_name FROM information_schema.table_constraints WHERE table_name='editions' AND constraint_type='UNIQUE'`
    - Indexes exist: `SELECT indexname FROM pg_indexes WHERE tablename='participants' AND indexname IN ('idx_participants_edition_membership','idx_participants_ticket_value','idx_participants_edition_company')` returns 3 rows
    - Index exists: `SELECT indexname FROM pg_indexes WHERE tablename='form_responses' AND indexname='idx_form_responses_origin_state'` returns 1 row
  </acceptance_criteria>
  <done>Migrations 007-010 aplicadas no Supabase remoto; RPCs e índices visíveis em information_schema.</done>
</task>

<task type="auto">
  <name>Task 3: Refatorar lib/data.ts para editionId + adicionar todas as funções consumidas pelos Plans 02/03/04/05</name>
  <files>abvcap-congress/lib/data.ts</files>
  <read_first>abvcap-congress/lib/data.ts, abvcap-congress/lib/database.types.ts, abvcap-congress/lib/supabase.ts</read_first>
  <action>
Reescrever `lib/data.ts` inteiramente. Imports:
```typescript
import { getSupabase } from './supabase'
import type {
  Participant,
  OverviewStats,
  TicketMembership,
  CompanySegment,
  Edition,
  MemberAnalysisRow,
  RevenueAnalysis,
  PaginatedParticipants,
  ParticipantWithState,
} from './database.types'

export type {
  Participant, OverviewStats, TicketMembership, CompanySegment, Edition,
  MemberAnalysisRow, RevenueAnalysis, PaginatedParticipants, ParticipantWithState,
}
```

Funções refatoradas (TODAS aceitam `editionId: string`, ZERO referência a `editionYear`):

```typescript
export async function getOverviewStats(editionId: string): Promise<OverviewStats> {
  const { data, error } = await getSupabase()
    .rpc('get_overview_stats', { p_edition_id: editionId })
  if (error) throw error
  return data as unknown as OverviewStats
}

export async function getEditions(): Promise<Edition[]> {
  const { data, error } = await getSupabase()
    .from('editions')
    .select('*')
    .order('year', { ascending: false })
    .order('created_at', { ascending: false })
  if (error) throw error
  return (data ?? []) as Edition[]
}

export interface CompanySegmentSummary { type: string; count: number }
export async function getCompanySegmentSummary(editionId: string): Promise<CompanySegmentSummary[]> {
  const { data, error } = await getSupabase()
    .from('participants')
    .select('company_segment_raw')
    .eq('edition_id', editionId)
    .not('company_segment_raw', 'is', null)
  if (error) throw error
  const counts: Record<string, number> = {}
  for (const row of data ?? []) {
    const seg = (row.company_segment_raw as string).trim()
    if (seg) counts[seg] = (counts[seg] ?? 0) + 1
  }
  return Object.entries(counts).map(([type, count]) => ({ type, count })).sort((a, b) => b.count - a.count)
}

export interface RegistrationsByDay { date: string; count: number }
export async function getRegistrationsByDay(editionId: string): Promise<RegistrationsByDay[]> {
  const { data, error } = await getSupabase()
    .from('participants')
    .select('created_at')
    .eq('edition_id', editionId)
    .not('created_at', 'is', null)
    .order('created_at', { ascending: true })
  if (error) throw error
  const counts: Record<string, number> = {}
  for (const row of data ?? []) {
    const date = (row.created_at as string).slice(0, 10)
    counts[date] = (counts[date] ?? 0) + 1
  }
  return Object.entries(counts).map(([date, count]) => ({ date, count }))
}

export interface TicketMembershipSummary { ticket_membership: TicketMembership; count: number }
export async function getTicketMembershipSummary(editionId: string): Promise<TicketMembershipSummary[]> {
  const membershipTypes: TicketMembership[] = ['MEMBRO', 'NAO_MEMBRO']
  const results: TicketMembershipSummary[] = []
  for (const tm of membershipTypes) {
    const { count, error } = await getSupabase()
      .from('participants')
      .select('*', { count: 'exact', head: true })
      .eq('edition_id', editionId)
      .eq('ticket_membership', tm)
    if (error) throw error
    results.push({ ticket_membership: tm, count: count ?? 0 })
  }
  return results.sort((a, b) => b.count - a.count)
}

export interface FreeTicketStats { free: number; paid: number; total: number }
export async function getFreeTicketStats(editionId: string): Promise<FreeTicketStats> {
  const [{ count: free, error: e1 }, { count: total, error: e2 }] = await Promise.all([
    getSupabase().from('participants').select('*', { count: 'exact', head: true }).eq('edition_id', editionId).eq('ticket_value', 0),
    getSupabase().from('participants').select('*', { count: 'exact', head: true }).eq('edition_id', editionId),
  ])
  if (e1) throw e1
  if (e2) throw e2
  const f = free ?? 0
  const t = total ?? 0
  return { free: f, paid: t - f, total: t }
}

// ─── NOVOS — consumidos pelos Plans 03/04/05 ─────────────────────────────────

const ALLOWED_SORT_COLUMNS = ['created_at', 'full_name', 'company', 'ticket_value', 'ticket_membership'] as const
type SortColumn = typeof ALLOWED_SORT_COLUMNS[number]

export async function getParticipantsPaginated(filters: {
  editionId: string
  search?: string
  membership?: TicketMembership
  segment?: CompanySegment
  state?: string
  minValue?: number
  maxValue?: number
  sort?: string
  dir?: 'asc' | 'desc'
  limit: number
  offset: number
}): Promise<PaginatedParticipants> {
  const sort: SortColumn = (ALLOWED_SORT_COLUMNS as readonly string[]).includes(filters.sort ?? '')
    ? (filters.sort as SortColumn)
    : 'created_at'
  const dir = filters.dir === 'asc' ? 'asc' : 'desc'

  // Quando há filtro de state, precisamos JOIN com form_responses (inner)
  const needsFormJoin = !!filters.state
  const selectExpr = needsFormJoin
    ? '*, form_responses!inner(origin_state)'
    : '*'

  let query = getSupabase()
    .from('participants')
    .select(selectExpr, { count: 'exact' })
    .eq('edition_id', filters.editionId)
    .order(sort, { ascending: dir === 'asc' })
    .range(filters.offset, filters.offset + filters.limit - 1)

  if (filters.membership) query = query.eq('ticket_membership', filters.membership)
  if (filters.segment) query = query.eq('company_segment_normalized', filters.segment)
  if (filters.minValue !== undefined) query = query.gte('ticket_value', filters.minValue)
  if (filters.maxValue !== undefined) query = query.lte('ticket_value', filters.maxValue)
  if (filters.state) query = query.eq('form_responses.origin_state', filters.state)
  if (filters.search) {
    const s = filters.search.replace(/[%,]/g, '')
    query = query.or(`full_name.ilike.%${s}%,email.ilike.%${s}%,company.ilike.%${s}%`)
  }

  const { data, count, error } = await query
  if (error) throw error
  return { data: (data ?? []) as unknown as Participant[], count: count ?? 0 }
}

export async function getParticipantsForExport(filters: {
  editionId: string
  search?: string
  membership?: TicketMembership
  segment?: CompanySegment
  state?: string
  minValue?: number
  maxValue?: number
}): Promise<ParticipantWithState[]> {
  // Sempre faz LEFT JOIN para trazer origin_state — necessário para o export
  let query = getSupabase()
    .from('participants')
    .select('*, form_responses(origin_state)')
    .eq('edition_id', filters.editionId)
    .order('created_at', { ascending: false })
    .limit(10000)

  if (filters.membership) query = query.eq('ticket_membership', filters.membership)
  if (filters.segment) query = query.eq('company_segment_normalized', filters.segment)
  if (filters.minValue !== undefined) query = query.gte('ticket_value', filters.minValue)
  if (filters.maxValue !== undefined) query = query.lte('ticket_value', filters.maxValue)
  if (filters.search) {
    const s = filters.search.replace(/[%,]/g, '')
    query = query.or(`full_name.ilike.%${s}%,email.ilike.%${s}%,company.ilike.%${s}%`)
  }

  const { data, error } = await query
  if (error) throw error

  // Para filtro de state, aplica em memória após o fetch (export trabalha com até 10k rows)
  const rows = (data ?? []) as unknown as Array<Participant & { form_responses: { origin_state: string | null } | { origin_state: string | null }[] | null }>
  const flattened: ParticipantWithState[] = rows.map(r => {
    const fr = Array.isArray(r.form_responses) ? r.form_responses[0] : r.form_responses
    return { ...r, origin_state: fr?.origin_state ?? null } as ParticipantWithState
  })
  if (filters.state) return flattened.filter(r => r.origin_state === filters.state)
  return flattened
}

export async function getMemberAnalysis(editionId: string): Promise<MemberAnalysisRow[]> {
  const { data, error } = await getSupabase()
    .rpc('get_member_analysis', { p_edition_id: editionId })
  if (error) throw error
  return (data as unknown as MemberAnalysisRow[]) ?? []
}

export async function getRevenueAnalysis(editionId: string): Promise<RevenueAnalysis> {
  const { data, error } = await getSupabase()
    .rpc('get_revenue_analysis', { p_edition_id: editionId })
  if (error) throw error
  return data as unknown as RevenueAnalysis
}
```

CRÍTICO: Remover TODA referência a `editionYear`, `2025`, e qualquer parâmetro com default numérico de ano. Nenhum lookup `.eq('year', ...)` deve permanecer.
  </action>
  <verify>
    <automated>cd abvcap-congress && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File abvcap-congress/lib/data.ts contains exact signature `export async function getOverviewStats(editionId: string): Promise<OverviewStats>`
    - File contains exact signature `export async function getEditions(): Promise<Edition[]>`
    - File contains exact signature `export async function getParticipantsPaginated(filters: {`
    - File contains exact signature `export async function getParticipantsForExport(filters: {`
    - File contains exact signature `export async function getMemberAnalysis(editionId: string): Promise<MemberAnalysisRow[]>`
    - File contains exact signature `export async function getRevenueAnalysis(editionId: string): Promise<RevenueAnalysis>`
    - File contains `.rpc('get_overview_stats', { p_edition_id: editionId })`
    - File does NOT contain `editionYear` (verify with grep)
    - File does NOT contain `2025` (verify with grep)
    - File does NOT contain `.eq('year'` (verify with grep)
    - File contains `ALLOWED_SORT_COLUMNS` array for sort whitelist
    - `cd abvcap-congress && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>lib/data.ts inteiramente em editionId-only com todas as 11 funções (5 refatoradas + 6 novas) exportadas e tsc limpo.</done>
</task>

<task type="auto">
  <name>Task 4: Criar lib/edition-cookie.ts + /api/edition/select + /api/edition/create + componente EditionSelector</name>
  <files>abvcap-congress/lib/edition-cookie.ts, abvcap-congress/components/edition-selector.tsx, abvcap-congress/app/api/edition/select/route.ts, abvcap-congress/app/api/edition/create/route.ts</files>
  <read_first>abvcap-congress/lib/auth.ts, abvcap-congress/lib/supabase.ts, abvcap-congress/lib/supabase-server.ts, abvcap-congress/lib/data.ts, abvcap-congress/components/ui/select.tsx, abvcap-congress/app/api/auth/login/route.ts</read_first>
  <action>
**lib/edition-cookie.ts** (NEW):
```typescript
import { cookies } from 'next/headers'
import { getEditions } from './data'

export const ACTIVE_EDITION_COOKIE = 'active_edition_id'

/**
 * Retorna o editionId ativo (D-01/D-02):
 *  - lê cookie httpOnly 'active_edition_id'
 *  - se ausente, faz fallback para a edição com maior year (getEditions() já ordena DESC)
 *  - lança Error('Nenhuma edição cadastrada') se getEditions() retorna []
 */
export async function getActiveEditionId(): Promise<string> {
  const cookieStore = await cookies()
  const stored = cookieStore.get(ACTIVE_EDITION_COOKIE)?.value
  if (stored) return stored

  const editions = await getEditions()
  if (editions.length === 0) {
    throw new Error('Nenhuma edição cadastrada')
  }
  return editions[0].id
}
```

**components/edition-selector.tsx** (NEW, 'use client'):
```typescript
'use client'

import { useRouter } from 'next/navigation'
import { useState, useTransition } from 'react'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'

export function EditionSelector({
  editions,
  activeEditionId,
}: {
  editions: { id: string; name: string; year: number }[]
  activeEditionId: string
}) {
  const router = useRouter()
  const [, startTransition] = useTransition()
  const [value, setValue] = useState(activeEditionId)

  async function handleChange(editionId: string) {
    setValue(editionId)
    try {
      const res = await fetch('/api/edition/select', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ editionId }),
      })
      if (!res.ok) {
        // revert visual
        setValue(activeEditionId)
        return
      }
      startTransition(() => router.refresh())
    } catch {
      setValue(activeEditionId)
    }
  }

  if (editions.length === 0) {
    return (
      <p className="text-[10px] font-mono text-sidebar-foreground/40 px-3 py-2">
        Nenhuma edição
      </p>
    )
  }

  return (
    <Select value={value} onValueChange={handleChange}>
      <SelectTrigger className="w-full text-[12px] bg-sidebar-accent/40 border-sidebar-border text-sidebar-foreground/80">
        <SelectValue />
      </SelectTrigger>
      <SelectContent>
        {editions.map(e => (
          <SelectItem key={e.id} value={e.id}>{e.name}</SelectItem>
        ))}
      </SelectContent>
    </Select>
  )
}
```

**app/api/edition/select/route.ts** (NEW):
```typescript
import { cookies } from 'next/headers'
import { NextResponse } from 'next/server'
import { z } from 'zod'
import { requireAdmin } from '@/lib/auth'
import { getSupabase } from '@/lib/supabase'
import { ACTIVE_EDITION_COOKIE } from '@/lib/edition-cookie'

export const runtime = 'nodejs'
export const dynamic = 'force-dynamic'

const Body = z.object({ editionId: z.string().uuid() })

export async function POST(req: Request) {
  try { await requireAdmin() } catch { return NextResponse.json({ error: 'Forbidden' }, { status: 403 }) }

  let raw: unknown
  try { raw = await req.json() } catch { return NextResponse.json({ error: 'JSON inválido' }, { status: 400 }) }
  const parsed = Body.safeParse(raw)
  if (!parsed.success) return NextResponse.json({ error: 'Payload inválido', details: parsed.error.issues }, { status: 400 })

  // Anti cookie-poisoning: verifica que editionId existe
  const { data, error } = await getSupabase()
    .from('editions')
    .select('id')
    .eq('id', parsed.data.editionId)
    .maybeSingle()
  if (error) return NextResponse.json({ error: 'Erro ao verificar edição' }, { status: 500 })
  if (!data) return NextResponse.json({ error: 'Edição não encontrada' }, { status: 404 })

  const cookieStore = await cookies()
  cookieStore.set(ACTIVE_EDITION_COOKIE, parsed.data.editionId, {
    httpOnly: true,
    sameSite: 'lax',
    secure: process.env.NODE_ENV === 'production',
    path: '/',
    maxAge: 60 * 60 * 24 * 365,
  })
  return NextResponse.json({ ok: true })
}
```

**app/api/edition/create/route.ts** (NEW):
```typescript
import { NextResponse } from 'next/server'
import { z } from 'zod'
import { requireAdmin } from '@/lib/auth'
import { getSupabase } from '@/lib/supabase'

export const runtime = 'nodejs'
export const dynamic = 'force-dynamic'

const Body = z.object({
  name: z.string().min(1).max(200),
  year: z.number().int().min(2000).max(2100),
})

export async function POST(req: Request) {
  try { await requireAdmin() } catch { return NextResponse.json({ error: 'Forbidden' }, { status: 403 }) }

  let raw: unknown
  try { raw = await req.json() } catch { return NextResponse.json({ error: 'JSON inválido' }, { status: 400 }) }
  const parsed = Body.safeParse(raw)
  if (!parsed.success) return NextResponse.json({ error: 'Payload inválido', details: parsed.error.issues }, { status: 400 })

  const { data, error } = await getSupabase()
    .from('editions')
    .insert({ name: parsed.data.name, year: parsed.data.year })
    .select('id, name, year, created_at')
    .single()
  if (error) return NextResponse.json({ error: 'Falha ao criar edição', details: error.message }, { status: 500 })
  return NextResponse.json(data, { status: 201 })
}
```
  </action>
  <verify>
    <automated>cd abvcap-congress && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File abvcap-congress/lib/edition-cookie.ts exists
    - edition-cookie.ts contains exact string `export const ACTIVE_EDITION_COOKIE = 'active_edition_id'`
    - edition-cookie.ts contains exact string `export async function getActiveEditionId(): Promise<string>`
    - edition-cookie.ts contains exact string `await cookies()`
    - edition-cookie.ts contains exact string `Nenhuma edição cadastrada`
    - File abvcap-congress/components/edition-selector.tsx exists
    - edition-selector.tsx contains `'use client'` as the first non-comment line
    - edition-selector.tsx contains exact string `fetch('/api/edition/select'`
    - edition-selector.tsx contains exact string `router.refresh()`
    - File abvcap-congress/app/api/edition/select/route.ts exists
    - select/route.ts contains `editionId: z.string().uuid()`
    - select/route.ts contains `cookieStore.set(ACTIVE_EDITION_COOKIE`
    - select/route.ts contains `requireAdmin()`
    - select/route.ts contains existence check on editions table
    - File abvcap-congress/app/api/edition/create/route.ts exists
    - create/route.ts contains `name: z.string().min(1).max(200)`
    - create/route.ts contains `year: z.number().int().min(2000).max(2100)`
    - create/route.ts contains `requireAdmin()`
    - `cd abvcap-congress && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Helper de cookie, 2 route handlers e EditionSelector criados com Zod validation e admin gating.</done>
</task>

<task type="auto">
  <name>Task 5: Refatorar Sidebar para receber props + Layout Server Component fetcher + criar página /dashboard/eventos</name>
  <files>abvcap-congress/components/sidebar.tsx, abvcap-congress/app/dashboard/layout.tsx, abvcap-congress/app/dashboard/eventos/page.tsx, abvcap-congress/app/dashboard/eventos/eventos-client.tsx</files>
  <read_first>abvcap-congress/components/sidebar.tsx, abvcap-congress/app/dashboard/layout.tsx, abvcap-congress/lib/data.ts, abvcap-congress/lib/edition-cookie.ts, abvcap-congress/components/edition-selector.tsx, abvcap-congress/components/stat-card.tsx, abvcap-congress/components/ui/button.tsx, abvcap-congress/components/ui/input.tsx</read_first>
  <action>
**components/sidebar.tsx** — Refatorar para aceitar 2 props novas e renderizar EditionSelector. Mudanças exatas:

1. Atualizar a assinatura:
```typescript
export function Sidebar({
  editions,
  activeEditionId,
}: {
  editions: { id: string; name: string; year: number }[]
  activeEditionId: string
}) {
```

2. Adicionar `import { EditionSelector } from '@/components/edition-selector'` no topo.

3. Adicionar `Calendar` no import de lucide-react: `import { LayoutDashboard, Users, BarChart3, TicketIcon, LogOut, Upload, Calendar, Wallet, UserCheck } from 'lucide-react'`.

4. Adicionar 3 itens ao array `nav` (mantendo os existentes):
```typescript
const nav = [
  { href: '/dashboard', label: 'Visão Geral', icon: LayoutDashboard },
  { href: '/dashboard/inscricoes', label: 'Inscrições', icon: Users },
  { href: '/dashboard/ingressos', label: 'Ingressos', icon: TicketIcon },
  { href: '/dashboard/publico', label: 'Análise de Público', icon: BarChart3 },
  { href: '/dashboard/membros', label: 'Análise de Membros', icon: UserCheck },
  { href: '/dashboard/receita', label: 'Análise de Receita', icon: Wallet },
  { href: '/dashboard/eventos', label: 'Eventos', icon: Calendar },
  { href: '/dashboard/import', label: 'Importar', icon: Upload },
]
```

5. INSERIR o EditionSelector ABAIXO do bloco do logo e ACIMA do label "Menu" (D-03). Adicionar logo após o `</div>` do bloco brand e ANTES do `<div className="px-6 mb-2">` do label "Menu":
```typescript
{/* Edition selector (D-01/D-03) */}
<div className="px-6 pb-5 -mt-2">
  <p className="text-[9px] font-mono tracking-[0.25em] text-sidebar-foreground/30 uppercase mb-2">
    Evento Ativo
  </p>
  <EditionSelector editions={editions} activeEditionId={activeEditionId} />
</div>
```

**app/dashboard/layout.tsx** — Reescrever inteiramente como Server Component:
```typescript
import { Sidebar } from '@/components/sidebar'
import { getEditions } from '@/lib/data'
import { getActiveEditionId } from '@/lib/edition-cookie'

export const dynamic = 'force-dynamic'

export default async function DashboardLayout({ children }: { children: React.ReactNode }) {
  let editions: Awaited<ReturnType<typeof getEditions>> = []
  let activeEditionId = ''
  try {
    editions = await getEditions()
    if (editions.length > 0) {
      try { activeEditionId = await getActiveEditionId() } catch { activeEditionId = editions[0].id }
    }
  } catch {
    // banco vazio ou erro de conexão — sidebar renderiza estado vazio
  }

  return (
    <div className="flex min-h-screen">
      <Sidebar editions={editions} activeEditionId={activeEditionId} />
      <main className="flex-1 overflow-auto bg-background">
        {children}
      </main>
    </div>
  )
}
```

**app/dashboard/eventos/page.tsx** (NEW Server Component, D-04):
```typescript
import { requireAdmin } from '@/lib/auth'
import { getEditions } from '@/lib/data'
import { EventosClient } from './eventos-client'

export const dynamic = 'force-dynamic'
export const metadata = { title: 'Eventos — Dashboard ABVCAP' }

export default async function EventosPage() {
  await requireAdmin()
  let editions: Awaited<ReturnType<typeof getEditions>> = []
  try { editions = await getEditions() } catch { editions = [] }

  return (
    <div className="p-8 space-y-8">
      <div className="border-b border-border pb-6">
        <p className="text-[10px] font-mono tracking-[0.22em] text-muted-foreground uppercase mb-1">
          Gestão
        </p>
        <h1 className="font-display text-3xl text-foreground leading-none">Eventos</h1>
        <p className="text-sm text-muted-foreground mt-2">
          Crie e gerencie edições do Congresso ABVCAP. Cada edição agrupa participantes, inscrições e respostas de formulário.
        </p>
      </div>
      <EventosClient editions={editions} />
    </div>
  )
}
```

**app/dashboard/eventos/eventos-client.tsx** (NEW 'use client'):
```typescript
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import type { Edition } from '@/lib/database.types'

export function EventosClient({ editions }: { editions: Edition[] }) {
  const router = useRouter()
  const [showForm, setShowForm] = useState(editions.length === 0)
  const [name, setName] = useState('')
  const [year, setYear] = useState<string>(new Date().getFullYear().toString())
  const [submitting, setSubmitting] = useState(false)
  const [error, setError] = useState<string | null>(null)

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault()
    setSubmitting(true)
    setError(null)
    try {
      const res = await fetch('/api/edition/create', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ name: name.trim(), year: Number(year) }),
      })
      const json = await res.json()
      if (!res.ok) {
        setError((json as { error?: string }).error ?? 'Falha ao criar evento.')
        return
      }
      setName(''); setYear(new Date().getFullYear().toString())
      setShowForm(false)
      router.refresh()
    } catch {
      setError('Erro de rede.')
    } finally {
      setSubmitting(false)
    }
  }

  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between">
        <h2 className="text-sm font-medium text-foreground/80">Edições cadastradas</h2>
        {!showForm && (
          <Button variant="outline" size="sm" onClick={() => setShowForm(true)}>
            + Novo evento
          </Button>
        )}
      </div>

      {showForm && (
        <form onSubmit={handleSubmit} className="border border-border rounded-lg p-5 bg-card space-y-4">
          <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
            <div className="space-y-2">
              <label className="text-xs font-mono uppercase tracking-wider text-muted-foreground">Nome</label>
              <Input value={name} onChange={e => setName(e.target.value)} placeholder="Congresso ABVCAP 2026" required minLength={1} maxLength={200} />
            </div>
            <div className="space-y-2">
              <label className="text-xs font-mono uppercase tracking-wider text-muted-foreground">Ano</label>
              <Input type="number" value={year} onChange={e => setYear(e.target.value)} min={2000} max={2100} required />
            </div>
          </div>
          {error && <p role="alert" className="text-sm text-red-600">{error}</p>}
          <div className="flex gap-2">
            <Button type="submit" disabled={submitting}>{submitting ? 'Criando…' : 'Criar evento'}</Button>
            {editions.length > 0 && (
              <Button type="button" variant="ghost" onClick={() => setShowForm(false)} disabled={submitting}>Cancelar</Button>
            )}
          </div>
        </form>
      )}

      {editions.length === 0 ? (
        <div className="border border-dashed border-border rounded-lg p-12 text-center">
          <p className="text-sm text-muted-foreground">Nenhuma edição cadastrada ainda. Use o formulário acima para criar a primeira.</p>
        </div>
      ) : (
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-3">
          {editions.map(e => (
            <div key={e.id} className="border border-border rounded-lg p-5 bg-card hover:border-primary/30 transition-all">
              <p className="text-[10px] font-mono tracking-wider text-muted-foreground uppercase mb-2">Edição {e.year}</p>
              <p className="font-display text-lg text-foreground leading-tight">{e.name}</p>
              {e.created_at && (
                <p className="text-[11px] font-mono text-muted-foreground/60 mt-3">
                  criada {new Date(e.created_at).toLocaleDateString('pt-BR')}
                </p>
              )}
            </div>
          ))}
        </div>
      )}
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd abvcap-congress && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File abvcap-congress/components/sidebar.tsx contains exact string `editions: { id: string; name: string; year: number }[]`
    - sidebar.tsx contains exact string `activeEditionId: string`
    - sidebar.tsx contains exact string `<EditionSelector editions={editions} activeEditionId={activeEditionId} />`
    - sidebar.tsx contains nav entries `'/dashboard/eventos'`, `'/dashboard/membros'`, `'/dashboard/receita'`
    - sidebar.tsx contains import `import { EditionSelector } from '@/components/edition-selector'`
    - File abvcap-congress/app/dashboard/layout.tsx contains exact string `await getEditions()`
    - layout.tsx contains exact string `await getActiveEditionId()`
    - layout.tsx contains exact string `<Sidebar editions={editions} activeEditionId={activeEditionId} />`
    - layout.tsx does NOT contain `'use client'` (must be Server Component)
    - File abvcap-congress/app/dashboard/eventos/page.tsx exists and contains `requireAdmin()`
    - eventos/page.tsx contains `await getEditions()`
    - File abvcap-congress/app/dashboard/eventos/eventos-client.tsx exists with `'use client'` first line
    - eventos-client.tsx contains `fetch('/api/edition/create'`
    - eventos-client.tsx contains `router.refresh()`
    - `cd abvcap-congress && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Sidebar refatorado com EditionSelector e novos links, layout server-side fetcha dados, página /dashboard/eventos completa com criação inline.</done>
</task>

<task type="auto">
  <name>Task 6: Migrar import flow para editionId — page.tsx server-side + import-client picker + commit route Zod schema</name>
  <files>abvcap-congress/app/dashboard/import/page.tsx, abvcap-congress/app/dashboard/import/import-client.tsx, abvcap-congress/app/api/import/commit/route.ts</files>
  <read_first>abvcap-congress/app/dashboard/import/page.tsx, abvcap-congress/app/dashboard/import/import-client.tsx, abvcap-congress/app/api/import/commit/route.ts, abvcap-congress/lib/data.ts, abvcap-congress/lib/edition-cookie.ts, abvcap-congress/components/ui/select.tsx</read_first>
  <action>
**app/dashboard/import/page.tsx** — Reescrever para passar editions + activeEditionId como props (D-07, D-08):
```typescript
import { requireAdmin } from '@/lib/auth'
import { getEditions } from '@/lib/data'
import { getActiveEditionId } from '@/lib/edition-cookie'
import Link from 'next/link'
import { ImportClient } from './import-client'

export const dynamic = 'force-dynamic'
export const metadata = { title: 'Importar Excel — Dashboard ABVCAP' }

export default async function ImportPage() {
  await requireAdmin()

  let editions: Awaited<ReturnType<typeof getEditions>> = []
  let activeEditionId = ''
  try {
    editions = await getEditions()
    if (editions.length > 0) {
      try { activeEditionId = await getActiveEditionId() } catch { activeEditionId = editions[0].id }
    }
  } catch { editions = [] }

  return (
    <div className="p-8 space-y-6">
      <div className="border-b border-border pb-6">
        <p className="text-[10px] font-mono tracking-[0.22em] text-muted-foreground uppercase mb-1">
          Pipeline
        </p>
        <h1 className="font-display text-3xl text-foreground leading-none">
          Importar Excel
        </h1>
        <p className="text-sm text-muted-foreground mt-2">
          Faça upload de uma planilha exportada da plataforma ABVCAP. O sistema irá detectar o cabeçalho,
          pedir confirmação do mapeamento de colunas e mostrar uma prévia antes de gravar no banco.
        </p>
      </div>

      {editions.length === 0 ? (
        <div className="border border-dashed border-border rounded-lg p-12 text-center space-y-3">
          <p className="text-sm font-medium text-foreground">Nenhum evento cadastrado.</p>
          <p className="text-sm text-muted-foreground">
            Crie um evento antes de importar participantes.
          </p>
          <Link
            href="/dashboard/eventos"
            className="inline-block text-sm text-primary hover:underline mt-2"
          >
            Ir para Eventos →
          </Link>
        </div>
      ) : (
        <ImportClient editions={editions} initialEditionId={activeEditionId} />
      )}
    </div>
  )
}
```

**app/dashboard/import/import-client.tsx** — Modificar para aceitar props + mostrar select de evento + enviar editionId no commit. Mudanças exatas:

1. Atualizar a assinatura:
```typescript
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import type { Edition } from '@/lib/database.types'

export function ImportClient({
  editions,
  initialEditionId,
}: {
  editions: Edition[]
  initialEditionId: string
}) {
```

2. Adicionar state local logo após os outros useState:
```typescript
const [selectedEditionId, setSelectedEditionId] = useState(initialEditionId || editions[0]?.id || '')
```

3. Renderizar o picker NO TOPO do componente (antes do bloco `{(stage === 'idle' || stage === 'error') && ...}`):
```typescript
<div className="flex items-center gap-3 border border-border rounded-lg bg-card px-4 py-3">
  <div className="flex-1">
    <p className="text-[10px] font-mono tracking-wider text-muted-foreground uppercase mb-1">
      Evento de destino
    </p>
    <Select
      value={selectedEditionId}
      onValueChange={setSelectedEditionId}
      disabled={stage === 'committing' || stage === 'uploading'}
    >
      <SelectTrigger className="w-full max-w-sm"><SelectValue /></SelectTrigger>
      <SelectContent>
        {editions.map(e => (
          <SelectItem key={e.id} value={e.id}>{e.name}</SelectItem>
        ))}
      </SelectContent>
    </Select>
  </div>
  <p className="text-[10px] font-mono text-muted-foreground/60 max-w-xs">
    Os dados serão associados a este evento. Verifique antes de confirmar.
  </p>
</div>
```

4. Atualizar a função `commit` para enviar `editionId` (substituir `editionYear`):
```typescript
async function commit(serverToken: string) {
  setStage('committing')
  setError(null)
  try {
    const res = await fetch('/api/import/commit', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ serverToken, editionId: selectedEditionId }),
    })
    const json = await res.json()
    if (!res.ok) {
      setError((json as { error?: string }).error ?? 'Falha ao importar.')
      setStage('error')
      return
    }
    setResult(json as CommitResult)
    setStage('done')
  } catch {
    setError('Erro de rede durante importação.')
    setStage('error')
  }
}
```

**app/api/import/commit/route.ts** — Substituir o Zod schema e o lookup:

1. Substituir:
```typescript
const BodySchema = z.object({
  serverToken: z.string().min(32).max(64),
  editionYear: z.number().int().optional().default(2025),
})
```
por:
```typescript
const BodySchema = z.object({
  serverToken: z.string().min(32).max(64),
  editionId: z.string().uuid(),
})
```

2. Substituir o bloco "4. Resolve edition by year":
```typescript
const { data: edition, error: editionErr } = await supabase
  .from('editions')
  .select('id, year, name')
  .eq('year', body.editionYear)
  .single()

if (editionErr || !edition) {
  return NextResponse.json(
    { error: `Edição ${body.editionYear} não encontrada.` },
    { status: 404 }
  )
}
```
por:
```typescript
// 4. Resolve edition by id
const { data: edition, error: editionErr } = await supabase
  .from('editions')
  .select('id, year, name')
  .eq('id', body.editionId)
  .single()

if (editionErr || !edition) {
  return NextResponse.json(
    { error: 'Edição não encontrada.' },
    { status: 404 }
  )
}
```

3. Manter todo o resto do arquivo (chunked upsert, audit log, COMPLETED/FAILED). Não tocar nas chamadas de RPC.
  </action>
  <verify>
    <automated>cd abvcap-congress && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File abvcap-congress/app/dashboard/import/page.tsx contains `await getEditions()`
    - import/page.tsx contains `<ImportClient editions={editions} initialEditionId={activeEditionId} />`
    - import/page.tsx contains `editions.length === 0` block with link to `/dashboard/eventos`
    - File abvcap-congress/app/dashboard/import/import-client.tsx contains exact string `editions: Edition[]`
    - import-client.tsx contains exact string `initialEditionId: string`
    - import-client.tsx contains exact string `selectedEditionId` (state variable)
    - import-client.tsx contains exact string `Evento de destino`
    - import-client.tsx commit fetch body contains `editionId: selectedEditionId`
    - import-client.tsx does NOT contain `editionYear` (verify with grep)
    - File abvcap-congress/app/api/import/commit/route.ts contains exact string `editionId: z.string().uuid()`
    - commit/route.ts contains exact string `.eq('id', body.editionId)`
    - commit/route.ts does NOT contain `editionYear` (verify with grep)
    - commit/route.ts does NOT contain `.eq('year'` (verify with grep)
    - `cd abvcap-congress && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Página de import bloqueia upload sem evento, mostra evento ativo com select, commit route aceita editionId direto.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Verificação humana do fluxo multi-evento</name>
  <files>abvcap-congress/</files>
  <read_first>abvcap-congress/components/sidebar.tsx, abvcap-congress/app/dashboard/layout.tsx, abvcap-congress/app/dashboard/eventos/page.tsx</read_first>
  <action>
**CHECKPOINT — não modificar código.** Apresentar ao operador humano o resumo do que foi construído nas Tasks 1-6 e aguardar verificação manual.

**O que foi construído:**
- 4 migrations (007 remove UNIQUE, 008 RPC editionId, 009 RPCs membros/receita, 010 índices) aplicadas no Supabase
- lib/data.ts inteiramente em editionId — todas as funções existentes refatoradas + 6 novas (getEditions, getParticipantsPaginated, getParticipantsForExport, getMemberAnalysis, getRevenueAnalysis)
- lib/edition-cookie.ts com getActiveEditionId() (fallback para edição mais recente)
- /api/edition/select + /api/edition/create
- EditionSelector no sidebar (abaixo do logo)
- /dashboard/eventos com criação inline
- Layout server-side passando props para Sidebar
- Import bloqueia upload sem evento + mostra picker no topo
- commit route aceita editionId uuid
- exceljs movido para dependencies

**Roteiro de verificação manual:**
1. Build limpa: rodar `cd abvcap-congress && npm run build` — deve passar sem erros.
2. Subir dev: `cd abvcap-congress && npm run dev` e acessar https://localhost:3000.
3. Login como admin → dashboard carrega normalmente.
4. Verificar sidebar:
   - EditionSelector aparece abaixo do logo, mostrando "Congresso ABVCAP 2025" (a edição existente).
   - Itens novos visíveis no menu: "Análise de Membros", "Análise de Receita", "Eventos".
5. Acessar /dashboard/eventos → lista mostra Congresso ABVCAP 2025 + botão "Novo evento".
6. Criar "Congresso ABVCAP 2026" ano 2026 → aparece na lista; EditionSelector no sidebar agora tem 2 opções.
7. Trocar para 2026 no EditionSelector → dashboard re-fetcha (KPIs vão para zero — esperado, sem dados).
8. Voltar para 2025 → KPIs voltam aos valores anteriores.
9. Tentar criar novamente "Congresso ABVCAP 2026" ano 2026 → deve aceitar (UNIQUE removida).
10. Acessar /dashboard/import com 2026 ativo → picker mostra "Congresso ABVCAP 2026" como destino; trocar para 2025 antes de importar.
11. Verificar no Supabase Dashboard que existem migrations 007-010 aplicadas (`select * from supabase_migrations.schema_migrations`).

Aguardar sinal humano antes de declarar o plano completo.
  </action>
  <acceptance_criteria>
    - Human operator typed "aprovado" or equivalent confirmation
    - All 11 verification steps confirmed working in production-like environment
    - npm run build passes
    - Sidebar shows EditionSelector + 3 new nav items
    - Creating two editions with the same year succeeds (UNIQUE removed)
    - Switching edition triggers router.refresh and dashboard data updates
  </acceptance_criteria>
  <verify>
    <automated>cd abvcap-congress && npm run build</automated>
  </verify>
  <done>Operador humano sinalizou "aprovado" após confirmar os 11 passos do roteiro; build passa.</done>
  <resume-signal>Digitar "aprovado" ou descrever problemas observados.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/edition/select | Admin trocando edição ativa; payload `editionId` precisa validação (uuid + existência) |
| Browser → /api/edition/create | Admin criando edição; payload `name + year` precisa validação Zod |
| Browser → /api/import/commit | Admin commitando import; agora carrega `editionId` em vez de `editionYear` — mesmo nível de risco |
| Cookie `active_edition_id` → Server Components | Cookie httpOnly, sameSite=lax, secure em prod; valor é uuid validado quando setado |
| Supabase RPCs (get_member_analysis, get_revenue_analysis) | Service role only — todas as queries passam por `getSupabase()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | /api/edition/select | mitigate | Zod `editionId.uuid()` + verificar existência no banco antes de setar cookie (anti cookie-poisoning) |
| T-03-02 | Spoofing | EditionSelector cookie | mitigate | Cookie é httpOnly + sameSite lax + secure em produção; só set via route handler que valida via `requireAdmin()` |
| T-03-03 | Information Disclosure | /api/edition/create | mitigate | `requireAdmin()` antes do insert; erros de DB retornam mensagem genérica (`Falha ao criar edição`) sem expor schema |
| T-03-04 | Tampering | /api/import/commit body | mitigate | Zod `editionId.uuid()` substitui `editionYear` int; uuid é mais difícil de forjar (impossível adivinhar id de outra edição) |
| T-03-05 | Denial of Service | get_overview_stats com edition inexistente | accept | Função STABLE retorna zeros quando WHERE não casa; sem amplificação |
| T-03-06 | Elevation of Privilege | Route handlers /api/edition/* | mitigate | Todos chamam `requireAdmin()` no início — defense-in-depth com proxy.ts |
| T-03-07 | Repudiation | Criação de edição não registrada | accept | v1 não tem audit log para editions; created_at + admin user implícito no contexto da sessão é suficiente |
</threat_model>

<verification>
- `cd abvcap-congress && npx tsc --noEmit` exits 0 (zero TS errors)
- `cd abvcap-congress && npm run build` completes successfully
- Supabase remoto tem: 3 RPCs (get_overview_stats, get_member_analysis, get_revenue_analysis), 4 índices novos, sem UNIQUE em editions.year
- `grep -r "editionYear" abvcap-congress/lib abvcap-congress/app` retorna ZERO matches
- `grep -r "= 2025" abvcap-congress/lib abvcap-congress/app` retorna ZERO matches
- EditionSelector renderiza corretamente no sidebar e troca de edição faz router.refresh()
</verification>

<success_criteria>
- Todas as 4 migrations criadas, aplicadas no Supabase remoto e visíveis em information_schema
- lib/data.ts contém todas as 11 funções (5 refatoradas + 6 novas) com assinatura `editionId: string`
- Cookie `active_edition_id` é setado/lido corretamente; fallback para edição mais recente funciona
- /dashboard/eventos permite criar nova edição; constraint UNIQUE em year foi removida
- /dashboard/import bloqueia upload se editions.length === 0; senão mostra picker e envia editionId no commit
- Sidebar mostra EditionSelector + 3 novos itens de navegação (Membros, Receita, Eventos)
- exceljs em dependencies, não em devDependencies
- npm run build passa limpo
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-core/03-01-SUMMARY.md` summarizing:
- Migrations aplicadas (números e o que cada uma faz)
- Assinaturas finais de lib/data.ts (11 funções)
- Como o EditionSelector + cookie funciona end-to-end
- Padrão para Plans 02/03/04/05 consumirem getActiveEditionId() + lib/data.ts
- Quaisquer ajustes feitos durante execução (ex: nome real da constraint UNIQUE se diferente de editions_year_key)
</output>
