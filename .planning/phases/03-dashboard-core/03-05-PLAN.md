---
phase: 03-dashboard-core
plan: 05
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - abvcap-congress/app/dashboard/receita/page.tsx
  - abvcap-congress/app/dashboard/receita/receita-charts.tsx
autonomous: true
requirements:
  - REV-01
  - REV-02
  - REV-03

must_haves:
  truths:
    - "A página /dashboard/receita existe e está acessível pelo sidebar (link 'Análise de Receita')"
    - "A página exibe KPI cards de Receita Total e Ticket Médio Geral (REV-01)"
    - "A página exibe Receita e Ticket Médio por tipo de ingresso (MEMBRO vs NAO_MEMBRO) lado a lado (REV-02)"
    - "A página exibe histograma de distribuição de valores de ingresso (REV-03) com 6 faixas (Gratuito, R$1–500, R$501–1000, R$1001–2000, R$2001–3000, Acima de R$3000)"
    - "Dados vêm de get_revenue_analysis(editionId) via RPC criada em Plan 01"
    - "Sem evento ativo, página mostra CTA para /dashboard/eventos"
    - "Build TypeScript passa sem erros"
  artifacts:
    - path: "abvcap-congress/app/dashboard/receita/page.tsx"
      provides: "Server Component que lê editionId ativo + chama getRevenueAnalysis(editionId)"
      contains: "getRevenueAnalysis"
    - path: "abvcap-congress/app/dashboard/receita/receita-charts.tsx"
      provides: "Client Component com cards por membership + histograma de valores"
      contains: "RevenueAnalysis"
  key_links:
    - from: "abvcap-congress/app/dashboard/receita/page.tsx"
      to: "abvcap-congress/lib/data.ts (getRevenueAnalysis)"
      via: "await com editionId resolvido"
      pattern: "getRevenueAnalysis\\(editionId\\)"
    - from: "abvcap-congress/components/sidebar.tsx (modificado em Plan 01)"
      to: "abvcap-congress/app/dashboard/receita/page.tsx"
      via: "link com href '/dashboard/receita'"
      pattern: "/dashboard/receita"
---

<objective>
Construir a página de Análise de Receita. Renderiza 3 visões: (1) KPI cards de receita total e ticket médio geral (REV-01), (2) Comparativo de receita e ticket médio por tipo de ingresso MEMBRO vs NAO_MEMBRO (REV-02), e (3) Histograma de distribuição de valores de ingresso em 6 faixas (REV-03).

Purpose: Implementar REV-01/REV-02/REV-03. Perguntas como "qual a faixa de valor que concentra mais inscritos" e "membros pagam mais ou menos em média" são respondidas aqui.

Output: Server Component `/dashboard/receita/page.tsx` que chama `getRevenueAnalysis(editionId)` (criada em Plan 01) + Client Component `receita-charts.tsx`.

Maps decisions: D-10, D-13 (editionId via cookie). Maps requirements: REV-01, REV-02, REV-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-core/03-CONTEXT.md
@.planning/phases/03-dashboard-core/03-RESEARCH.md
@.planning/phases/03-dashboard-core/03-01-PLAN.md
@abvcap-congress/lib/data.ts
@abvcap-congress/lib/edition-cookie.ts
@abvcap-congress/lib/database.types.ts
@abvcap-congress/lib/auth.ts
@abvcap-congress/app/dashboard/overview-charts.tsx
@abvcap-congress/components/stat-card.tsx

<interfaces>
<!-- Disponíveis após Plan 01: -->

```typescript
// lib/database.types.ts
export interface RevenueByMembership {
  ticket_membership: TicketMembership   // 'MEMBRO' | 'NAO_MEMBRO'
  count: number
  total_revenue: number
  avg_ticket: number
}

export interface RevenueHistogramBucket {
  faixa: string                          // 'Gratuito' | 'R$1–500' | 'R$501–1000' | 'R$1001–2000' | 'R$2001–3000' | 'Acima de R$3000'
  count: number
  min_val: number | null
  max_val: number | null
}

export interface RevenueAnalysis {
  by_membership: RevenueByMembership[]
  histogram: RevenueHistogramBucket[]   // já ordenado por sort_order ASC (Gratuito → Acima de R$3000)
}

// lib/data.ts
export async function getRevenueAnalysis(editionId: string): Promise<RevenueAnalysis>

// lib/edition-cookie.ts
export async function getActiveEditionId(): Promise<string>
```

SQL backend (Plan 01 — migration 009):
- `by_membership`: GROUP BY ticket_membership, SUM(ticket_value), AVG(ticket_value FILTER WHERE > 0)
- `histogram`: CASE WHEN ticket_value=0 'Gratuito' ELSE faixas, COUNT(*), MIN/MAX, ORDER BY sort_order
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar Server Component /dashboard/receita/page.tsx</name>
  <files>abvcap-congress/app/dashboard/receita/page.tsx</files>
  <read_first>abvcap-congress/lib/data.ts, abvcap-congress/lib/edition-cookie.ts, abvcap-congress/lib/database.types.ts, abvcap-congress/lib/auth.ts, abvcap-congress/app/dashboard/page.tsx, abvcap-congress/components/stat-card.tsx</read_first>
  <action>
Criar `app/dashboard/receita/page.tsx`:

```typescript
import { requireAdmin } from '@/lib/auth'
import { getRevenueAnalysis } from '@/lib/data'
import { getActiveEditionId } from '@/lib/edition-cookie'
import Link from 'next/link'
import { StatCard } from '@/components/stat-card'
import { ReceitaCharts } from './receita-charts'

export const dynamic = 'force-dynamic'
export const metadata = { title: 'Análise de Receita — Dashboard ABVCAP' }

const formatBRL = (v: number) =>
  `R$ ${Number(v).toLocaleString('pt-BR', { minimumFractionDigits: 0, maximumFractionDigits: 0 })}`

const formatBRLDecimal = (v: number) =>
  `R$ ${Number(v).toLocaleString('pt-BR', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`

export default async function ReceitaPage() {
  await requireAdmin()

  let editionId: string | null = null
  try { editionId = await getActiveEditionId() } catch { editionId = null }

  if (!editionId) {
    return (
      <div className="p-8 space-y-6">
        <div className="border-b border-border pb-6">
          <h1 className="font-display text-3xl text-foreground leading-none">Análise de Receita</h1>
        </div>
        <div className="border border-dashed border-border rounded-lg p-12 text-center space-y-3">
          <p className="text-sm font-medium text-foreground">Nenhum evento cadastrado.</p>
          <Link href="/dashboard/eventos" className="inline-block text-sm text-primary hover:underline">
            Ir para Eventos →
          </Link>
        </div>
      </div>
    )
  }

  let analysis: Awaited<ReturnType<typeof getRevenueAnalysis>> = { by_membership: [], histogram: [] }
  let loadError = false
  try { analysis = await getRevenueAnalysis(editionId) } catch { loadError = true }

  // REV-01: totais globais agregados de by_membership
  const totalRevenue = (analysis.by_membership ?? []).reduce((acc, r) => acc + Number(r.total_revenue || 0), 0)
  const totalCount = (analysis.by_membership ?? []).reduce((acc, r) => acc + Number(r.count || 0), 0)
  // Ticket médio global = média ponderada (sum of total_revenue / sum of count com ticket_value > 0)
  // Aproximação: média ponderada usando avg * count quando avg > 0, else ignora.
  let weightedSum = 0
  let weightedCount = 0
  for (const r of analysis.by_membership ?? []) {
    const c = Number(r.count || 0)
    const a = Number(r.avg_ticket || 0)
    if (a > 0 && c > 0) { weightedSum += a * c; weightedCount += c }
  }
  const avgTicketGlobal = weightedCount > 0 ? weightedSum / weightedCount : 0

  return (
    <div className="p-8 space-y-8">
      <div className="flex items-end justify-between border-b border-border pb-6">
        <div>
          <p className="text-[10px] font-mono tracking-[0.22em] text-muted-foreground uppercase mb-1">
            Análise
          </p>
          <h1 className="font-display text-3xl text-foreground leading-none">Análise de Receita</h1>
          <p className="text-sm text-muted-foreground mt-2">
            Receita total, ticket médio por categoria e distribuição de valores de ingresso.
          </p>
        </div>
        <p className="text-[11px] font-mono text-muted-foreground/60 pb-0.5">
          {totalCount.toLocaleString('pt-BR')} inscrito{totalCount !== 1 ? 's' : ''}
        </p>
      </div>

      {loadError && (
        <div className="border border-red-500/20 bg-red-500/5 rounded-lg p-6 text-sm text-red-600">
          Falha ao carregar análise. Tente recarregar a página.
        </div>
      )}

      {!loadError && (
        <>
          {/* REV-01 — Receita total + Ticket médio global */}
          <div className="grid grid-cols-1 md:grid-cols-2 gap-3">
            <StatCard
              title="Receita Total"
              value={formatBRL(totalRevenue)}
              accent="teal"
            />
            <StatCard
              title="Ticket Médio Geral"
              value={formatBRLDecimal(avgTicketGlobal)}
              subtitle="Apenas ingressos pagos (> R$0)"
              accent="green"
            />
          </div>

          <ReceitaCharts analysis={analysis} />
        </>
      )}
    </div>
  )
}
```

CRÍTICO:
- Server Component (sem `'use client'`).
- Resolve editionId via getActiveEditionId; sem evento → CTA.
- KPIs REV-01 calculados no Server: `totalRevenue` (soma de total_revenue por membership) e `avgTicketGlobal` (média ponderada).
- `avgTicketGlobal` usa fórmula ponderada: sum(avg_ticket * count) / sum(count) — porque cada `avg_ticket` da RPC já excluiu zeros (`FILTER WHERE ticket_value > 0`), então ponderar pela count é correto. Subtitle deixa claro que ignora gratuitos.
- Passa `analysis` (objeto completo) para `<ReceitaCharts>`.
  </action>
  <verify>
    <automated>cd abvcap-congress && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File abvcap-congress/app/dashboard/receita/page.tsx exists
    - File contains exact string `requireAdmin()`
    - File contains exact string `getActiveEditionId()`
    - File contains exact string `getRevenueAnalysis(editionId)`
    - File contains exact string `<ReceitaCharts analysis={analysis} />`
    - File contains CTA block when `!editionId` linking to `/dashboard/eventos`
    - File contains StatCard for `Receita Total` and `Ticket Médio Geral`
    - File contains weighted average calculation (`weightedSum`, `weightedCount`)
    - File contains `export const dynamic = 'force-dynamic'`
    - File does NOT contain `'use client'`
    - `cd abvcap-congress && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Server Component da receita: KPIs REV-01 calculados (receita total + média ponderada de ticket pago), delega charts ao Client Component.</done>
</task>

<task type="auto">
  <name>Task 2: Criar Client Component receita-charts.tsx — comparativo por membership (REV-02) + histograma (REV-03)</name>
  <files>abvcap-congress/app/dashboard/receita/receita-charts.tsx</files>
  <read_first>abvcap-congress/lib/database.types.ts, abvcap-congress/app/dashboard/overview-charts.tsx</read_first>
  <action>
Criar `app/dashboard/receita/receita-charts.tsx`:

```typescript
'use client'

import {
  BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer,
  CartesianGrid, LabelList, Cell,
} from 'recharts'
import { useTheme } from 'next-themes'
import type { RevenueAnalysis } from '@/lib/database.types'

const NAVY_LIGHT = '#112468'
const NAVY_DARK = '#6b9be8'

const AXIS_STYLE = {
  fontSize: 11,
  fontFamily: 'var(--font-ibm-mono)',
  fill: 'oklch(0.52 0.04 254)',
}

const TOOLTIP_STYLE = {
  backgroundColor: '#ffffff',
  border: '1px solid oklch(0.89 0.010 240)',
  borderRadius: '6px',
  color: '#112468',
  fontSize: '12px',
  fontFamily: 'var(--font-ibm-mono)',
  boxShadow: '0 4px 16px rgba(17, 36, 104, 0.08)',
}

const GRID_COLOR = 'oklch(0.89 0.010 240)'

const formatBRL = (v: number) =>
  `R$ ${Number(v).toLocaleString('pt-BR', { minimumFractionDigits: 0, maximumFractionDigits: 0 })}`

const formatBRLDecimal = (v: number) =>
  `R$ ${Number(v).toLocaleString('pt-BR', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`

interface Props {
  analysis: RevenueAnalysis
}

const MEMBERSHIP_LABEL: Record<string, string> = {
  MEMBRO: 'Membro',
  NAO_MEMBRO: 'Não Membro',
}

export function ReceitaCharts({ analysis }: Props) {
  const { resolvedTheme } = useTheme()
  const memberColor = '#00a99d'
  const naoMemberColor = resolvedTheme === 'dark' ? NAVY_DARK : NAVY_LIGHT

  const byMembership = (analysis.by_membership ?? []).map(r => ({
    membership: r.ticket_membership,
    label: MEMBERSHIP_LABEL[r.ticket_membership] ?? r.ticket_membership,
    count: Number(r.count || 0),
    total_revenue: Number(r.total_revenue || 0),
    avg_ticket: Number(r.avg_ticket || 0),
    color: r.ticket_membership === 'MEMBRO' ? memberColor : naoMemberColor,
  }))

  const histogram = (analysis.histogram ?? []).map(b => ({
    faixa: b.faixa,
    count: Number(b.count || 0),
  }))

  return (
    <div className="space-y-6">
      {/* REV-02 — Comparativo Receita + Ticket Médio por Membership */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-4">
        <div className="bg-card border border-border rounded-lg p-5 shadow-sm">
          <ChartLabel>Receita por Tipo de Ingresso</ChartLabel>
          {byMembership.length === 0 ? <EmptyChart /> : (
            <ResponsiveContainer width="100%" height={240}>
              <BarChart data={byMembership} margin={{ top: 20, right: 8, left: 0, bottom: 0 }}>
                <CartesianGrid vertical={false} stroke={GRID_COLOR} strokeOpacity={0.6} />
                <XAxis dataKey="label" tick={AXIS_STYLE} axisLine={false} tickLine={false} />
                <YAxis
                  tick={AXIS_STYLE}
                  axisLine={false}
                  tickLine={false}
                  tickFormatter={(v: number) => `R$${(v / 1000).toLocaleString('pt-BR', { maximumFractionDigits: 0 })}k`}
                />
                <Tooltip
                  contentStyle={TOOLTIP_STYLE}
                  cursor={{ fill: 'oklch(0.21 0.11 265 / 0.04)' }}
                  formatter={(v: number) => [formatBRL(v), 'Receita']}
                />
                <Bar dataKey="total_revenue" name="Receita" radius={[3, 3, 0, 0]} maxBarSize={80}>
                  {byMembership.map((r, i) => <Cell key={i} fill={r.color} />)}
                  <LabelList
                    dataKey="total_revenue"
                    position="top"
                    formatter={(v: number) => formatBRL(v)}
                    style={{ fontSize: 11, fontFamily: 'var(--font-ibm-mono)', fill: 'oklch(0.30 0.10 265)' }}
                  />
                </Bar>
              </BarChart>
            </ResponsiveContainer>
          )}
        </div>

        <div className="bg-card border border-border rounded-lg p-5 shadow-sm">
          <ChartLabel>Ticket Médio por Tipo de Ingresso</ChartLabel>
          {byMembership.length === 0 ? <EmptyChart /> : (
            <ResponsiveContainer width="100%" height={240}>
              <BarChart data={byMembership} margin={{ top: 20, right: 8, left: 0, bottom: 0 }}>
                <CartesianGrid vertical={false} stroke={GRID_COLOR} strokeOpacity={0.6} />
                <XAxis dataKey="label" tick={AXIS_STYLE} axisLine={false} tickLine={false} />
                <YAxis
                  tick={AXIS_STYLE}
                  axisLine={false}
                  tickLine={false}
                  tickFormatter={(v: number) => `R$${(v).toLocaleString('pt-BR', { maximumFractionDigits: 0 })}`}
                />
                <Tooltip
                  contentStyle={TOOLTIP_STYLE}
                  cursor={{ fill: 'oklch(0.21 0.11 265 / 0.04)' }}
                  formatter={(v: number) => [formatBRLDecimal(v), 'Ticket médio']}
                />
                <Bar dataKey="avg_ticket" name="Ticket médio" radius={[3, 3, 0, 0]} maxBarSize={80}>
                  {byMembership.map((r, i) => <Cell key={i} fill={r.color} />)}
                  <LabelList
                    dataKey="avg_ticket"
                    position="top"
                    formatter={(v: number) => formatBRLDecimal(v)}
                    style={{ fontSize: 11, fontFamily: 'var(--font-ibm-mono)', fill: 'oklch(0.30 0.10 265)' }}
                  />
                </Bar>
              </BarChart>
            </ResponsiveContainer>
          )}
        </div>
      </div>

      {/* REV-02 — Tabela complementar */}
      <div className="bg-card border border-border rounded-lg overflow-hidden">
        <div className="px-5 py-3 border-b border-border">
          <p className="text-[10px] font-mono tracking-[0.20em] text-muted-foreground uppercase">
            Resumo por Tipo de Ingresso
          </p>
        </div>
        <table className="w-full">
          <thead className="bg-muted/40">
            <tr>
              <th className="text-left text-xs font-semibold p-3">Tipo</th>
              <th className="text-right text-xs font-semibold p-3">Inscritos</th>
              <th className="text-right text-xs font-semibold p-3">Receita</th>
              <th className="text-right text-xs font-semibold p-3">Ticket Médio</th>
            </tr>
          </thead>
          <tbody>
            {byMembership.map(r => (
              <tr key={r.membership} className="border-t border-border">
                <td className="p-3 text-sm font-medium">{r.label}</td>
                <td className="p-3 text-sm text-right font-mono tabular-nums">{r.count.toLocaleString('pt-BR')}</td>
                <td className="p-3 text-sm text-right font-mono tabular-nums">{formatBRL(r.total_revenue)}</td>
                <td className="p-3 text-sm text-right font-mono tabular-nums text-primary">{formatBRLDecimal(r.avg_ticket)}</td>
              </tr>
            ))}
            {byMembership.length === 0 && (
              <tr><td colSpan={4} className="p-6 text-center text-sm text-muted-foreground">Sem dados</td></tr>
            )}
          </tbody>
        </table>
      </div>

      {/* REV-03 — Histograma de distribuição */}
      <div className="bg-card border border-border rounded-lg p-5 shadow-sm">
        <ChartLabel>Distribuição de Valores de Ingresso</ChartLabel>
        {histogram.length === 0 ? <EmptyChart /> : (
          <ResponsiveContainer width="100%" height={280}>
            <BarChart data={histogram} margin={{ top: 20, right: 16, left: 0, bottom: 0 }}>
              <CartesianGrid vertical={false} stroke={GRID_COLOR} strokeOpacity={0.6} />
              <XAxis
                dataKey="faixa"
                tick={{ ...AXIS_STYLE, fontSize: 10 }}
                axisLine={false}
                tickLine={false}
                interval={0}
              />
              <YAxis tick={AXIS_STYLE} axisLine={false} tickLine={false} allowDecimals={false} />
              <Tooltip
                contentStyle={TOOLTIP_STYLE}
                cursor={{ fill: 'oklch(0.21 0.11 265 / 0.04)' }}
                formatter={(v: number) => [`${v} inscritos`, 'Quantidade']}
              />
              <Bar dataKey="count" name="Inscritos" fill={memberColor} radius={[3, 3, 0, 0]} maxBarSize={64}>
                <LabelList
                  dataKey="count"
                  position="top"
                  style={{ fontSize: 11, fontFamily: 'var(--font-ibm-mono)', fill: 'oklch(0.30 0.10 265)' }}
                />
              </Bar>
            </BarChart>
          </ResponsiveContainer>
        )}
        <p className="text-[10px] font-mono text-muted-foreground/60 mt-3">
          Faixas: Gratuito · R$1–500 · R$501–1000 · R$1001–2000 · R$2001–3000 · Acima de R$3000
        </p>
      </div>
    </div>
  )
}

function ChartLabel({ children }: { children: React.ReactNode }) {
  return (
    <p className="text-[10px] font-mono tracking-[0.20em] text-muted-foreground uppercase mb-4">
      {children}
    </p>
  )
}

function EmptyChart({ height = 220 }: { height?: number }) {
  return (
    <div className="flex flex-col items-center justify-center gap-2" style={{ height }}>
      <div className="w-6 h-6 rounded-full border-2 border-dashed border-border" />
      <p className="text-[11px] font-mono text-muted-foreground/40">sem dados ainda</p>
    </div>
  )
}
```

CRÍTICO:
- REV-02: 2 cards lado a lado (Receita por Tipo, Ticket Médio por Tipo) + tabela complementar com 4 colunas (Tipo, Inscritos, Receita, Ticket Médio).
- Bars do bloco REV-02 usam cores diferentes: teal para Membro, navy adaptativo para Não Membro.
- Y-axis da receita formata em `R$Xk` (milhares) para legibilidade.
- Y-axis do ticket médio formata em `R$X` (unidade).
- LabelList em topo das barras mostra valor BRL exato.
- REV-03: BarChart vertical com 6 faixas no eixo X (ordem vem do servidor via sort_order); `interval={0}` força exibir todas as labels.
- Footer do histograma lista explicitamente todas as 6 faixas para clareza.
  </action>
  <verify>
    <automated>cd abvcap-congress && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File abvcap-congress/app/dashboard/receita/receita-charts.tsx contains `'use client'` as first non-comment line
    - File contains exact string `Receita por Tipo de Ingresso`
    - File contains exact string `Ticket Médio por Tipo de Ingresso`
    - File contains exact string `Distribuição de Valores de Ingresso`
    - File contains exact string `Resumo por Tipo de Ingresso` (tabela)
    - File contains all 6 histogram faixa labels in the footer: `Gratuito`, `R$1–500`, `R$501–1000`, `R$1001–2000`, `R$2001–3000`, `Acima de R$3000`
    - File contains exact string `dataKey="total_revenue"`
    - File contains exact string `dataKey="avg_ticket"`
    - File contains exact string `dataKey="count"`
    - File contains exact string `interval={0}`
    - File imports `RevenueAnalysis` from `@/lib/database.types`
    - File contains exact string `MEMBRO: 'Membro'` and `NAO_MEMBRO: 'Não Membro'`
    - `cd abvcap-congress && npm run build` completes successfully
  </acceptance_criteria>
  <done>Charts da receita: REV-02 com 2 bars + tabela, REV-03 com histograma de 6 faixas; build limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → RPC get_revenue_analysis | Service role; editionId vem de cookie httpOnly validado |
| Browser → /dashboard/receita | Sessão Supabase Auth; requireAdmin() no Server Component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-20 | Information Disclosure | /dashboard/receita sem auth | mitigate | requireAdmin() no início do Server Component |
| T-03-21 | Information Disclosure | RPC retorna dados de outra edição | mitigate | editionId vem de cookie httpOnly validado; RPC filtra por p_edition_id |
| T-03-22 | DoS | RPC com agregação custosa | accept | get_revenue_analysis usa GROUP BY com WHERE indexado (idx_participants_edition_membership + idx_participants_ticket_value) |
| T-03-23 | Information Disclosure | Receita exposta em endpoint público | mitigate | Página /dashboard/receita é admin-only via proxy.ts + requireAdmin() no Server Component |
</threat_model>

<verification>
- `cd abvcap-congress && npm run build` completes successfully
- Manual: clicar "Análise de Receita" no sidebar abre `/dashboard/receita` sem erro
- Manual: KPIs no topo mostram Receita Total e Ticket Médio Geral em BRL formatado
- Manual: Cards de REV-02 mostram receita e ticket médio por MEMBRO vs NAO_MEMBRO com cores diferenciadas
- Manual: Tabela complementar mostra valores numéricos exatos para ambos os grupos
- Manual: Histograma mostra 6 barras com counts; faixa "Gratuito" aparece se houver ingressos zerados; labels não se cortam
- Manual: Trocar edição no sidebar atualiza todos os dados desta página
</verification>

<success_criteria>
- REV-01 satisfeito: receita total e ticket médio geral visíveis (KPI cards no topo)
- REV-02 satisfeito: receita e ticket médio por tipo de ingresso (MEMBRO vs NAO_MEMBRO) em 2 charts + tabela
- REV-03 satisfeito: histograma de distribuição com 6 faixas vinda da RPC
- Build passa sem TypeScript errors
- Página acessível pelo sidebar
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-core/03-05-SUMMARY.md` documentando:
- 3 visões implementadas (KPIs REV-01, comparativo REV-02 com 2 bars + tabela, histograma REV-03)
- Fórmula do ticket médio global (média ponderada ignorando gratuitos)
- Decisão sobre faixas do histograma e como vêm sort-ordered do servidor
</output>
