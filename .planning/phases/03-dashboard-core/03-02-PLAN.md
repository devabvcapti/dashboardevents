---
phase: 03-dashboard-core
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - abvcap-congress/app/dashboard/page.tsx
  - abvcap-congress/app/dashboard/overview-kpis.tsx
  - abvcap-congress/app/dashboard/overview-charts.tsx
  - abvcap-congress/lib/mock-data.ts
autonomous: true
requirements:
  - OV-01
  - OV-02
  - OV-03
  - OV-04

must_haves:
  truths:
    - "A página /dashboard exibe 7 KPI cards: Total Inscritos, Membros (count + %), Não-Membros (count + %), Receita Total, Ticket Médio, Empresas Únicas, Estados Representados"
    - "O donut chart Membros vs Não-Membros tem label central mostrando o total geral de inscritos"
    - "O gráfico de barras horizontais por tipo de empresa está ordenado decrescente por count e mostra count + percentual"
    - "Os dados vêm da edição ativa (cookie active_edition_id, com fallback para edição mais recente)"
    - "Build TypeScript passa sem erros"
  artifacts:
    - path: "abvcap-congress/app/dashboard/page.tsx"
      provides: "Server Component que lê editionId ativo, busca overview/segments/byTicket e passa para componentes"
      contains: "getActiveEditionId"
    - path: "abvcap-congress/app/dashboard/overview-kpis.tsx"
      provides: "7 KPI cards corretos (OV-01 + OV-02)"
      contains: "Empresas Únicas"
    - path: "abvcap-congress/app/dashboard/overview-charts.tsx"
      provides: "Donut central + barras horizontais (OV-03, OV-04)"
      contains: "innerRadius"
  key_links:
    - from: "abvcap-congress/app/dashboard/page.tsx"
      to: "abvcap-congress/lib/data.ts (getOverviewStats, getCompanySegmentSummary, getTicketMembershipSummary)"
      via: "Promise.all com editionId resolvido"
      pattern: "getOverviewStats\\(editionId\\)"
    - from: "abvcap-congress/app/dashboard/overview-charts.tsx"
      to: "Recharts PieChart com label central via div absoluta"
      via: "wrapper relative + div absolute inset-0"
      pattern: "innerRadius"
---

<objective>
Substituir os KPI cards e charts da visão geral pelos corretos conforme OV-01..OV-04. Adicionar 3 cards novos (Ticket Médio, Empresas Únicas, Estados Representados — os 2 últimos vêm dos campos novos do get_overview_stats criados em Plan 01). Adicionar donut chart Membros vs Não-Membros com label central de total. Trocar o gráfico de pizza atual (por tipo de empresa) por gráfico de BARRAS HORIZONTAIS sorted desc com count + %.

Purpose: Implementar o requisito de Visão Geral conforme ROADMAP — sem isso, OV-01/OV-02/OV-03/OV-04 não são satisfeitos.

Output: page.tsx refatorada para usar editionId resolvido por getActiveEditionId, componente novo overview-kpis.tsx com 7 cards, overview-charts.tsx atualizado com donut central e barras horizontais.

Maps decisions: D-10 (editionId), D-13 (getActiveEditionId). Maps requirements: OV-01, OV-02, OV-03, OV-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-core/03-CONTEXT.md
@.planning/phases/03-dashboard-core/03-RESEARCH.md
@.planning/phases/03-dashboard-core/03-01-PLAN.md
@abvcap-congress/app/dashboard/page.tsx
@abvcap-congress/app/dashboard/overview-charts.tsx
@abvcap-congress/components/stat-card.tsx
@abvcap-congress/lib/mock-data.ts
@abvcap-congress/lib/data.ts
@abvcap-congress/lib/edition-cookie.ts
@abvcap-congress/lib/database.types.ts

<interfaces>
<!-- Interfaces de Plan 01 (já existentes quando este plano executar): -->

```typescript
// lib/database.types.ts
export interface OverviewStats {
  total: number
  membro: number
  nao_membro: number
  total_revenue: number
  avg_ticket: number
  unique_companies: number    // criado em Plan 01
  states_represented: number  // criado em Plan 01
}

// lib/data.ts
export async function getOverviewStats(editionId: string): Promise<OverviewStats>
export async function getCompanySegmentSummary(editionId: string): Promise<CompanySegmentSummary[]>
export async function getTicketMembershipSummary(editionId: string): Promise<TicketMembershipSummary[]>
export async function getRegistrationsByDay(editionId: string): Promise<RegistrationsByDay[]>
export async function getFreeTicketStats(editionId: string): Promise<FreeTicketStats>

// lib/edition-cookie.ts
export async function getActiveEditionId(): Promise<string>
// Throws Error('Nenhuma edição cadastrada') se editions vazias
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar componente overview-kpis.tsx com 7 KPI cards (OV-01 + OV-02)</name>
  <files>abvcap-congress/app/dashboard/overview-kpis.tsx, abvcap-congress/lib/mock-data.ts</files>
  <read_first>abvcap-congress/components/stat-card.tsx, abvcap-congress/lib/mock-data.ts, abvcap-congress/app/dashboard/page.tsx, abvcap-congress/lib/database.types.ts</read_first>
  <action>
**abvcap-congress/lib/mock-data.ts** — Atualizar `MOCK_STATS` para incluir os 2 novos campos. Encontrar o bloco de `MOCK_STATS` e adicionar `unique_companies` e `states_represented` (manter valores plausíveis):
```typescript
export const MOCK_STATS: OverviewStats = {
  total: 1247,
  membro: 412,
  nao_membro: 835,
  total_revenue: 1856400,
  avg_ticket: 1820.5,
  unique_companies: 287,
  states_represented: 18,
}
```
(Se MOCK_STATS ainda usa um shape antigo, alinhar com OverviewStats e adicionar os 2 campos.)

**abvcap-congress/app/dashboard/overview-kpis.tsx** (NEW Client/Server-agnostic, sem 'use client' — apenas markup):
```typescript
import { StatCard } from '@/components/stat-card'
import type { OverviewStats } from '@/lib/database.types'

interface Props {
  stats: OverviewStats
}

export function OverviewKpis({ stats }: Props) {
  const memberPct = stats.total > 0
    ? Math.round((stats.membro / stats.total) * 100)
    : 0
  const naoMembroPct = stats.total > 0
    ? Math.round((stats.nao_membro / stats.total) * 100)
    : 0

  const formatBRL = (v: number) =>
    `R$ ${Number(v).toLocaleString('pt-BR', { minimumFractionDigits: 0, maximumFractionDigits: 0 })}`
  const formatBRLDecimal = (v: number) =>
    `R$ ${Number(v).toLocaleString('pt-BR', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`

  return (
    <div className="grid grid-cols-2 lg:grid-cols-4 gap-3">
      {/* Row 1 — OV-01: total, membros %, não-membros %, receita */}
      <StatCard
        title="Total Inscritos"
        value={stats.total}
        accent="blue"
      />
      <StatCard
        title="Membros"
        value={stats.membro}
        subtitle={`${memberPct}% do total`}
        accent="green"
      />
      <StatCard
        title="Não Membros"
        value={stats.nao_membro}
        subtitle={`${naoMembroPct}% do total`}
        accent="amber"
      />
      <StatCard
        title="Receita Total"
        value={formatBRL(stats.total_revenue)}
        accent="teal"
      />

      {/* Row 2 — OV-01 (ticket médio) + OV-02 (empresas + estados) */}
      <StatCard
        title="Ticket Médio"
        value={formatBRLDecimal(stats.avg_ticket)}
        accent="teal"
      />
      <StatCard
        title="Empresas Únicas"
        value={stats.unique_companies}
        accent="blue"
      />
      <StatCard
        title="Estados Representados"
        value={stats.states_represented}
        accent="default"
      />
      {/* Placeholder vazio para manter grade 4-col equilibrada em desktop */}
      <div className="hidden lg:block" />
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd abvcap-congress && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File abvcap-congress/app/dashboard/overview-kpis.tsx exists
    - overview-kpis.tsx contains exact strings: `Total Inscritos`, `Membros`, `Não Membros`, `Receita Total`, `Ticket Médio`, `Empresas Únicas`, `Estados Representados`
    - overview-kpis.tsx contains `stats.unique_companies` and `stats.states_represented`
    - overview-kpis.tsx contains `memberPct` and `naoMembroPct` calculations
    - overview-kpis.tsx imports `StatCard from '@/components/stat-card'`
    - overview-kpis.tsx imports `OverviewStats` from `@/lib/database.types`
    - File abvcap-congress/lib/mock-data.ts contains `unique_companies:` and `states_represented:` keys in MOCK_STATS
    - `cd abvcap-congress && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Componente overview-kpis.tsx criado com os 7 cards exigidos por OV-01 + OV-02; mock-data alinhado ao novo shape.</done>
</task>

<task type="auto">
  <name>Task 2: Refatorar overview-charts.tsx — donut Membros vs Não-Membros (OV-03) + barras horizontais por tipo de empresa (OV-04)</name>
  <files>abvcap-congress/app/dashboard/overview-charts.tsx</files>
  <read_first>abvcap-congress/app/dashboard/overview-charts.tsx</read_first>
  <action>
Reescrever `overview-charts.tsx` inteiramente. Conteúdo final:

```typescript
'use client'

import {
  BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer,
  PieChart, Pie, Cell, LineChart, Line, CartesianGrid, LabelList,
} from 'recharts'
import { useTheme } from 'next-themes'

const NAVY_LIGHT = '#112468'
const NAVY_DARK  = '#6b9be8'

function useChartColors() {
  const { resolvedTheme } = useTheme()
  const navy = resolvedTheme === 'dark' ? NAVY_DARK : NAVY_LIGHT
  return [
    '#00a99d',
    navy,
    'oklch(0.62 0.14 162)',
    'oklch(0.72 0.14 68)',
    'oklch(0.64 0.18 28)',
    'oklch(0.60 0.15 295)',
    'oklch(0.68 0.12 130)',
  ]
}

const AXIS_STYLE = {
  fontSize: 11,
  fontFamily: 'var(--font-ibm-mono)',
  fill: 'oklch(0.52 0.04 254)',
}

const TOOLTIP_STYLE = {
  backgroundColor: '#ffffff',
  border: '1px solid oklch(0.89 0.010 240)',
  borderRadius: '6px',
  color: '#112468',
  fontSize: '12px',
  fontFamily: 'var(--font-ibm-mono)',
  boxShadow: '0 4px 16px rgba(17, 36, 104, 0.08)',
}

const GRID_COLOR = 'oklch(0.89 0.010 240)'

interface Props {
  byTicketType: { type: string; count: number }[]
  byCompanyType: { type: string; count: number }[]
  registrationsByDay: { date: string; count: number }[]
  freeTickets: { free: number; paid: number; total: number }
  totalInscritos: number
}

export function OverviewCharts({
  byTicketType,
  byCompanyType,
  registrationsByDay,
  freeTickets,
  totalInscritos,
}: Props) {
  const CHART_COLORS = useChartColors()

  // OV-04: ordenar desc + computar % sobre total
  const sumCompany = byCompanyType.reduce((acc, r) => acc + r.count, 0)
  const companyData = [...byCompanyType]
    .sort((a, b) => b.count - a.count)
    .map(r => ({
      ...r,
      pct: sumCompany > 0 ? Math.round((r.count / sumCompany) * 1000) / 10 : 0,
      label: `${r.count} (${sumCompany > 0 ? Math.round((r.count / sumCompany) * 100) : 0}%)`,
    }))

  const freePct = freeTickets.total > 0 ? Math.round((freeTickets.free / freeTickets.total) * 100) : 0
  const freeChartData = freeTickets.total > 0
    ? [
        { type: 'Grátis (R$0)', count: freeTickets.free },
        { type: 'Pagos', count: freeTickets.paid },
      ]
    : []

  return (
    <div className="grid grid-cols-1 lg:grid-cols-2 gap-4">
      {/* OV-03 — Donut Membros vs Não-Membros com label central */}
      <div className="bg-card border border-border rounded-lg p-5 shadow-sm">
        <ChartLabel>Membros vs Não-Membros</ChartLabel>
        {byTicketType.length === 0 ? <EmptyChart /> : (
          <div className="relative">
            <ResponsiveContainer width="100%" height={260}>
              <PieChart>
                <Pie
                  data={byTicketType}
                  dataKey="count"
                  nameKey="type"
                  cx="50%" cy="50%"
                  innerRadius={70}
                  outerRadius={105}
                  paddingAngle={2}
                  stroke="transparent"
                >
                  {byTicketType.map((_, i) => (
                    <Cell key={i} fill={CHART_COLORS[i % CHART_COLORS.length]} />
                  ))}
                </Pie>
                <Tooltip
                  contentStyle={TOOLTIP_STYLE}
                  formatter={(v: number, name: string) => [`${v} inscritos`, name]}
                />
              </PieChart>
            </ResponsiveContainer>
            <div className="absolute inset-0 flex flex-col items-center justify-center pointer-events-none">
              <p className="font-display tabular-nums text-3xl text-foreground leading-none">{totalInscritos}</p>
              <p className="text-[9px] font-mono text-muted-foreground uppercase tracking-wider mt-1">total</p>
            </div>
          </div>
        )}
      </div>

      {/* OV-04 — Barras HORIZONTAIS por tipo de empresa (sorted desc) */}
      <div className="bg-card border border-border rounded-lg p-5 shadow-sm">
        <ChartLabel>Perfil por Tipo de Empresa</ChartLabel>
        {companyData.length === 0 ? <EmptyChart /> : (
          <ResponsiveContainer width="100%" height={Math.max(220, companyData.length * 36)}>
            <BarChart data={companyData} layout="vertical" margin={{ top: 8, right: 80, left: 8, bottom: 0 }}>
              <CartesianGrid horizontal={false} stroke={GRID_COLOR} strokeOpacity={0.5} />
              <XAxis type="number" tick={AXIS_STYLE} axisLine={false} tickLine={false} allowDecimals={false} />
              <YAxis dataKey="type" type="category" tick={AXIS_STYLE} axisLine={false} tickLine={false} width={120} />
              <Tooltip
                contentStyle={TOOLTIP_STYLE}
                cursor={{ fill: 'oklch(0.21 0.11 265 / 0.04)' }}
                formatter={(v: number, _name: string, ctx: { payload?: { pct?: number } }) => [
                  `${v} (${ctx?.payload?.pct ?? 0}%)`,
                  'Inscritos',
                ]}
              />
              <Bar dataKey="count" name="Inscritos" radius={[0, 3, 3, 0]} maxBarSize={22}>
                {companyData.map((_, i) => (
                  <Cell key={i} fill={CHART_COLORS[i % CHART_COLORS.length]} />
                ))}
                <LabelList
                  dataKey="label"
                  position="right"
                  style={{ fontSize: 11, fontFamily: 'var(--font-ibm-mono)', fill: 'oklch(0.52 0.04 254)' }}
                />
              </Bar>
            </BarChart>
          </ResponsiveContainer>
        )}
      </div>

      {/* Free vs Pagos (mantido — informativo) */}
      <div className="bg-card border border-border rounded-lg p-5 shadow-sm lg:col-span-2">
        <ChartLabel>Ingressos Grátis (R$0) vs Pagos</ChartLabel>
        {freeChartData.length === 0 ? <EmptyChart height={120} /> : (
          <div className="flex items-center gap-8">
            <div className="shrink-0 text-center">
              <p className="font-display tabular-nums text-5xl text-foreground leading-none">{freeTickets.free}</p>
              <p className="mt-1 text-[10px] font-mono text-muted-foreground uppercase tracking-wider">grátis</p>
              <p className="mt-0.5 text-[11px] font-mono text-muted-foreground/60">{freePct}% do total</p>
            </div>
            <div className="flex-1">
              <ResponsiveContainer width="100%" height={120}>
                <BarChart data={freeChartData} margin={{ top: 8, right: 0, left: -28, bottom: 0 }}>
                  <CartesianGrid vertical={false} stroke={GRID_COLOR} strokeOpacity={0.6} />
                  <XAxis dataKey="type" tick={AXIS_STYLE} axisLine={false} tickLine={false} />
                  <YAxis tick={AXIS_STYLE} axisLine={false} tickLine={false} allowDecimals={false} />
                  <Tooltip contentStyle={TOOLTIP_STYLE} cursor={{ fill: 'oklch(0.21 0.11 265 / 0.04)' }} />
                  <Bar dataKey="count" name="Inscritos" radius={[3, 3, 0, 0]} maxBarSize={48}>
                    <Cell fill={CHART_COLORS[0]} />
                    <Cell fill={CHART_COLORS[1]} />
                  </Bar>
                </BarChart>
              </ResponsiveContainer>
            </div>
          </div>
        )}
      </div>

      {/* Linha — inscrições ao longo do tempo (mantido) */}
      <div className="bg-card border border-border rounded-lg p-5 shadow-sm lg:col-span-2">
        <ChartLabel>Inscrições ao Longo do Tempo</ChartLabel>
        {registrationsByDay.length === 0 ? <EmptyChart height={180} /> : (
          <ResponsiveContainer width="100%" height={180}>
            <LineChart data={registrationsByDay} margin={{ top: 8, right: 8, left: -28, bottom: 0 }}>
              <CartesianGrid vertical={false} stroke={GRID_COLOR} strokeOpacity={0.6} />
              <XAxis dataKey="date" tick={{ ...AXIS_STYLE, fontSize: 10 }} axisLine={false} tickLine={false} />
              <YAxis tick={AXIS_STYLE} axisLine={false} tickLine={false} allowDecimals={false} />
              <Tooltip contentStyle={TOOLTIP_STYLE} />
              <Line
                type="monotone"
                dataKey="count"
                name="Inscrições"
                stroke="#00a99d"
                strokeWidth={2.5}
                dot={{ r: 3, fill: '#00a99d', strokeWidth: 0 }}
                activeDot={{ r: 5, fill: '#00a99d', stroke: '#ffffff', strokeWidth: 2 }}
              />
            </LineChart>
          </ResponsiveContainer>
        )}
      </div>
    </div>
  )
}

function ChartLabel({ children }: { children: React.ReactNode }) {
  return (
    <p className="text-[10px] font-mono tracking-[0.20em] text-muted-foreground uppercase mb-4">
      {children}
    </p>
  )
}

function EmptyChart({ height = 220 }: { height?: number }) {
  return (
    <div className="flex flex-col items-center justify-center gap-2" style={{ height }}>
      <div className="w-6 h-6 rounded-full border-2 border-dashed border-border" />
      <p className="text-[11px] font-mono text-muted-foreground/40">
        sem dados ainda
      </p>
    </div>
  )
}
```

CRÍTICO:
- O Pie da seção "Membros vs Não-Membros" usa `innerRadius={70}` (donut) e o label central é renderizado via `<div className="absolute inset-0 ...">` em wrapper `relative` — OV-03.
- O BarChart da seção "Perfil por Tipo de Empresa" usa `layout="vertical"` (= horizontal bars no Recharts) com `YAxis dataKey="type" type="category"` — OV-04.
- `companyData` é PRE-ORDENADO desc por count no servidor/cliente antes de passar ao Recharts.
- `LabelList` mostra `${count} (${pct}%)` à direita de cada barra.
  </action>
  <verify>
    <automated>cd abvcap-congress && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File abvcap-congress/app/dashboard/overview-charts.tsx contains exact string `innerRadius={70}`
    - overview-charts.tsx contains exact string `Membros vs Não-Membros`
    - overview-charts.tsx contains the absolute-positioned central label div: `absolute inset-0 flex flex-col items-center justify-center pointer-events-none`
    - overview-charts.tsx contains exact string `totalInscritos`
    - overview-charts.tsx contains exact string `layout="vertical"` (horizontal bar chart)
    - overview-charts.tsx contains exact string `YAxis dataKey="type" type="category"`
    - overview-charts.tsx contains exact string `LabelList`
    - overview-charts.tsx contains exact string `.sort((a, b) => b.count - a.count)`
    - overview-charts.tsx Props interface contains `totalInscritos: number`
    - `cd abvcap-congress && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>overview-charts.tsx atualizado com donut central (OV-03) e barras horizontais ordenadas (OV-04); compila.</done>
</task>

<task type="auto">
  <name>Task 3: Refatorar app/dashboard/page.tsx para usar editionId ativo e novos componentes</name>
  <files>abvcap-congress/app/dashboard/page.tsx</files>
  <read_first>abvcap-congress/app/dashboard/page.tsx, abvcap-congress/app/dashboard/overview-kpis.tsx, abvcap-congress/app/dashboard/overview-charts.tsx, abvcap-congress/lib/edition-cookie.ts, abvcap-congress/lib/data.ts</read_first>
  <action>
Reescrever `app/dashboard/page.tsx` inteiramente:

```typescript
import { requireAdmin } from '@/lib/auth'
import {
  getOverviewStats, getCompanySegmentSummary, getRegistrationsByDay,
  getTicketMembershipSummary, getFreeTicketStats,
} from '@/lib/data'
import { getActiveEditionId } from '@/lib/edition-cookie'
import Link from 'next/link'
import { OverviewKpis } from './overview-kpis'
import { OverviewCharts } from './overview-charts'
import { MOCK_STATS } from '@/lib/mock-data'

export const dynamic = 'force-dynamic'

export default async function DashboardPage() {
  await requireAdmin()

  // Resolver editionId — sem editions, renderiza CTA
  let editionId: string | null = null
  try {
    editionId = await getActiveEditionId()
  } catch {
    editionId = null
  }

  if (!editionId) {
    return (
      <div className="p-8 space-y-6">
        <div className="border-b border-border pb-6">
          <h1 className="font-display text-3xl text-foreground leading-none">Visão Geral</h1>
        </div>
        <div className="border border-dashed border-border rounded-lg p-12 text-center space-y-3">
          <p className="text-sm font-medium text-foreground">Nenhum evento cadastrado.</p>
          <p className="text-sm text-muted-foreground">
            Crie uma edição para começar a visualizar dados.
          </p>
          <Link href="/dashboard/eventos" className="inline-block text-sm text-primary hover:underline">
            Ir para Eventos →
          </Link>
        </div>
      </div>
    )
  }

  let stats: Awaited<ReturnType<typeof getOverviewStats>> | null = null
  let byTicketType: { type: string; count: number }[] = []
  let byCompanyType: { type: string; count: number }[] = []
  let registrationsByDay: { date: string; count: number }[] = []
  let freeTickets = { free: 0, paid: 0, total: 0 }
  let isMock = false

  try {
    const [s, ticket, segment, regByDay, free] = await Promise.all([
      getOverviewStats(editionId),
      getTicketMembershipSummary(editionId),
      getCompanySegmentSummary(editionId),
      getRegistrationsByDay(editionId),
      getFreeTicketStats(editionId),
    ])
    stats = s
    byTicketType = ticket.map(r => ({
      type: r.ticket_membership === 'MEMBRO' ? 'Membros' : 'Não Membros',
      count: r.count,
    }))
    byCompanyType = segment
    registrationsByDay = regByDay
    freeTickets = free
  } catch {
    stats = null
    isMock = true
  }

  const display = stats ?? MOCK_STATS

  return (
    <div className="p-8 space-y-8">
      <div className="flex items-end justify-between border-b border-border pb-6">
        <div>
          <p className="text-[10px] font-mono tracking-[0.22em] text-muted-foreground uppercase mb-1">
            Painel de Controle
          </p>
          <h1 className="font-display text-3xl text-foreground leading-none">
            Visão Geral
          </h1>
        </div>
        <div className="flex items-center gap-3 pb-0.5">
          {isMock && (
            <span className="text-[9px] font-mono tracking-widest text-amber-500/70 uppercase border border-amber-500/20 bg-amber-500/5 px-2 py-0.5 rounded">
              dados demo
            </span>
          )}
          <p className="text-[11px] font-mono text-muted-foreground/50">
            Eventos ABVCAP
          </p>
        </div>
      </div>

      <OverviewKpis stats={display} />

      <OverviewCharts
        byTicketType={byTicketType}
        byCompanyType={byCompanyType}
        registrationsByDay={registrationsByDay}
        freeTickets={freeTickets}
        totalInscritos={display.total}
      />
    </div>
  )
}
```

CRÍTICO:
- Toda a página agora resolve `editionId` via `getActiveEditionId()` — sem editions, renderiza CTA para `/dashboard/eventos`.
- `getOverviewStats`, `getTicketMembershipSummary`, etc. são chamadas com `editionId` (string), não com year hardcoded.
- `<OverviewKpis stats={display} />` substitui os 4 cards inline.
- `<OverviewCharts ... totalInscritos={display.total} />` recebe a nova prop para o label central do donut.
  </action>
  <verify>
    <automated>cd abvcap-congress && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File abvcap-congress/app/dashboard/page.tsx contains exact string `getActiveEditionId()`
    - page.tsx contains exact string `getOverviewStats(editionId)`
    - page.tsx contains exact string `<OverviewKpis stats={display} />`
    - page.tsx contains exact string `totalInscritos={display.total}`
    - page.tsx contains the CTA block when `!editionId` (`Nenhum evento cadastrado` text + link to `/dashboard/eventos`)
    - page.tsx contains import from `./overview-kpis`
    - page.tsx does NOT contain `getOverviewStats()` (no-arg call — verify with grep that all calls pass editionId)
    - page.tsx does NOT contain `editionYear`
    - page.tsx does NOT contain `2025` literal
    - `cd abvcap-congress && npm run build` completes successfully
  </acceptance_criteria>
  <done>Página de visão geral lê editionId ativo, renderiza CTA se sem evento, e usa OverviewKpis + OverviewCharts atualizados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Supabase RPC `get_overview_stats` | Service role; entrada validada (editionId uuid via getActiveEditionId que vem de cookie httpOnly setado em route validado) |
| Browser → /dashboard | Sessão Supabase Auth; requireAdmin() gate antes do render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | /dashboard | mitigate | requireAdmin() no início do Server Component impede acesso não-admin |
| T-03-09 | Information Disclosure | OverviewKpis renderiza dados de outra edição | mitigate | editionId vem de cookie httpOnly validado em /api/edition/select; sem rota client-side de troca |
| T-03-10 | DoS | get_overview_stats com edition_id falso | accept | RPC é STABLE com LEFT JOIN — retorna zeros sem amplificação |
</threat_model>

<verification>
- `cd abvcap-congress && npm run build` completes successfully
- Manual: acessar `/dashboard` mostra 7 KPI cards (Total, Membros %, Não-Membros %, Receita, Ticket Médio, Empresas Únicas, Estados Representados)
- Manual: donut chart "Membros vs Não-Membros" tem label central com o número total
- Manual: gráfico "Perfil por Tipo de Empresa" é horizontal, ordenado desc por count, com labels `count (pct%)` à direita
- Manual: sem evento cadastrado, página mostra CTA para criar evento em vez de erro 500
</verification>

<success_criteria>
- Os 7 KPI cards aparecem com valores corretos do RPC para a edição ativa
- Donut central + barras horizontais renderizam dados reais (não mock) quando há dados
- Trocar a edição no sidebar atualiza os KPIs e charts via router.refresh()
- Build passa sem TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-core/03-02-SUMMARY.md` documentando:
- 7 cards renderizados (mapping OV-01/OV-02)
- Como o donut central funciona (wrapper relative + div absolute inset-0)
- Como as barras horizontais ordenam (sort + LabelList)
- Como page.tsx integra getActiveEditionId
</output>
