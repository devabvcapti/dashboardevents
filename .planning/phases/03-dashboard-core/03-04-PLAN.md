---
phase: 03-dashboard-core
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - abvcap-congress/app/dashboard/membros/page.tsx
  - abvcap-congress/app/dashboard/membros/membros-charts.tsx
autonomous: true
requirements:
  - MBR-01
  - MBR-02
  - MBR-03

must_haves:
  truths:
    - "A página /dashboard/membros existe e está acessível pelo sidebar (link 'Análise de Membros')"
    - "A página exibe breakdown membros vs não-membros por tipo de empresa (segmento) — barras agrupadas por segmento"
    - "A página exibe percentual de adesão (membership_pct) por segmento — ordenado decrescente"
    - "A página exibe breakdown por segmento dentro de cada grupo (membros / não-membros) — pode ser stacked bar ou tabela"
    - "Dados vêm de get_member_analysis(editionId) via RPC criada em Plan 01"
    - "Sem evento ativo, página mostra CTA para /dashboard/eventos (mesmo padrão da Visão Geral)"
    - "Build TypeScript passa sem erros"
  artifacts:
    - path: "abvcap-congress/app/dashboard/membros/page.tsx"
      provides: "Server Component que lê editionId ativo + chama getMemberAnalysis(editionId)"
      contains: "getMemberAnalysis"
    - path: "abvcap-congress/app/dashboard/membros/membros-charts.tsx"
      provides: "Client Component com charts: stacked bars por segmento + adesão %"
      contains: "MemberAnalysisRow"
  key_links:
    - from: "abvcap-congress/app/dashboard/membros/page.tsx"
      to: "abvcap-congress/lib/data.ts (getMemberAnalysis)"
      via: "await com editionId resolvido"
      pattern: "getMemberAnalysis\\(editionId\\)"
    - from: "abvcap-congress/components/sidebar.tsx (modificado em Plan 01)"
      to: "abvcap-congress/app/dashboard/membros/page.tsx"
      via: "link com href '/dashboard/membros'"
      pattern: "/dashboard/membros"
---

<objective>
Construir a página de Análise de Membros. Renderiza 3 visões correlacionadas: (1) Breakdown total por segmento de empresa com colunas Membro/Não-Membro/Total (MBR-01/MBR-02), (2) Gráfico de barras empilhadas Membros vs Não-Membros por segmento (MBR-01), e (3) Ranking de adesão % por segmento (MBR-03) — mostrando quais tipos de empresa têm maior taxa de associação.

Purpose: Implementar MBR-01/MBR-02/MBR-03 — perguntas como "quais segmentos têm maior aderência ABVCAP" são respondidas aqui.

Output: Server Component `/dashboard/membros/page.tsx` que chama `getMemberAnalysis(editionId)` (criada em Plan 01) + Client Component `membros-charts.tsx` com 3 visões.

Maps decisions: D-10, D-13 (editionId via cookie). Maps requirements: MBR-01, MBR-02, MBR-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-core/03-CONTEXT.md
@.planning/phases/03-dashboard-core/03-RESEARCH.md
@.planning/phases/03-dashboard-core/03-01-PLAN.md
@abvcap-congress/lib/data.ts
@abvcap-congress/lib/edition-cookie.ts
@abvcap-congress/lib/database.types.ts
@abvcap-congress/lib/auth.ts
@abvcap-congress/app/dashboard/overview-charts.tsx
@abvcap-congress/components/stat-card.tsx
@abvcap-congress/app/dashboard/publico/publico-charts.tsx

<interfaces>
<!-- Disponíveis após Plan 01: -->

```typescript
// lib/database.types.ts
export interface MemberAnalysisRow {
  segment: string             // 'GP' | 'LP' | 'FUNDO' | 'CORPORATIVO' | 'GOVERNO' | 'ACADEMIA' | 'OUTRO' | 'SEM_SEGMENTO'
  membro_count: number
  nao_membro_count: number
  total: number
  membership_pct: number      // 0..100, 1 casa decimal
}

// lib/data.ts
export async function getMemberAnalysis(editionId: string): Promise<MemberAnalysisRow[]>
// Retorna array ordenado por total DESC. Pode ser [] se não houver dados.

// lib/edition-cookie.ts
export async function getActiveEditionId(): Promise<string>
// Throws 'Nenhuma edição cadastrada' se editions vazio
```

SQL backend (já criado em Plan 01 — migration 009):
```sql
CREATE OR REPLACE FUNCTION get_member_analysis(p_edition_id uuid) RETURNS json
-- agrupa por COALESCE(fr.company_segment::text, 'SEM_SEGMENTO')
-- retorna json_agg(row_to_json(t)) ORDER BY total DESC
```

SEGMENT_LABELS (manter consistência com Plan 03 e RESEARCH.md):
```typescript
const SEGMENT_LABELS: Record<string, string> = {
  GP: 'Gestora de PE/VC',
  LP: 'Investidor (LP)',
  FUNDO: 'Fundo de Pensão',
  CORPORATIVO: 'Corporativo',
  GOVERNO: 'Governo',
  ACADEMIA: 'Academia',
  OUTRO: 'Outro',
  SEM_SEGMENTO: 'Sem segmento',
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar Server Component /dashboard/membros/page.tsx</name>
  <files>abvcap-congress/app/dashboard/membros/page.tsx</files>
  <read_first>abvcap-congress/lib/data.ts, abvcap-congress/lib/edition-cookie.ts, abvcap-congress/lib/database.types.ts, abvcap-congress/lib/auth.ts, abvcap-congress/app/dashboard/page.tsx</read_first>
  <action>
Criar `app/dashboard/membros/page.tsx`:

```typescript
import { requireAdmin } from '@/lib/auth'
import { getMemberAnalysis } from '@/lib/data'
import { getActiveEditionId } from '@/lib/edition-cookie'
import Link from 'next/link'
import { MembrosCharts } from './membros-charts'

export const dynamic = 'force-dynamic'
export const metadata = { title: 'Análise de Membros — Dashboard ABVCAP' }

export default async function MembrosPage() {
  await requireAdmin()

  let editionId: string | null = null
  try { editionId = await getActiveEditionId() } catch { editionId = null }

  if (!editionId) {
    return (
      <div className="p-8 space-y-6">
        <div className="border-b border-border pb-6">
          <h1 className="font-display text-3xl text-foreground leading-none">Análise de Membros</h1>
        </div>
        <div className="border border-dashed border-border rounded-lg p-12 text-center space-y-3">
          <p className="text-sm font-medium text-foreground">Nenhum evento cadastrado.</p>
          <Link href="/dashboard/eventos" className="inline-block text-sm text-primary hover:underline">
            Ir para Eventos →
          </Link>
        </div>
      </div>
    )
  }

  let rows: Awaited<ReturnType<typeof getMemberAnalysis>> = []
  let loadError = false
  try { rows = await getMemberAnalysis(editionId) } catch { loadError = true }

  // Agregados para KPI strip
  const totalMembros = rows.reduce((acc, r) => acc + r.membro_count, 0)
  const totalNaoMembros = rows.reduce((acc, r) => acc + r.nao_membro_count, 0)
  const totalGeral = totalMembros + totalNaoMembros
  const adesaoGlobal = totalGeral > 0 ? Math.round((totalMembros / totalGeral) * 1000) / 10 : 0

  return (
    <div className="p-8 space-y-8">
      <div className="flex items-end justify-between border-b border-border pb-6">
        <div>
          <p className="text-[10px] font-mono tracking-[0.22em] text-muted-foreground uppercase mb-1">
            Análise
          </p>
          <h1 className="font-display text-3xl text-foreground leading-none">Análise de Membros</h1>
          <p className="text-sm text-muted-foreground mt-2">
            Comparativo entre membros e não-membros por tipo de empresa, com taxa de adesão por segmento.
          </p>
        </div>
        <p className="text-[11px] font-mono text-muted-foreground/60 pb-0.5">
          {totalGeral.toLocaleString('pt-BR')} inscrito{totalGeral !== 1 ? 's' : ''}
        </p>
      </div>

      {loadError && (
        <div className="border border-red-500/20 bg-red-500/5 rounded-lg p-6 text-sm text-red-600">
          Falha ao carregar análise. Tente recarregar a página.
        </div>
      )}

      {!loadError && rows.length === 0 && (
        <div className="border border-dashed border-border rounded-lg p-12 text-center">
          <p className="text-sm text-muted-foreground">Sem dados de participantes nesta edição.</p>
        </div>
      )}

      {!loadError && rows.length > 0 && (
        <>
          {/* KPIs agregados */}
          <div className="grid grid-cols-1 md:grid-cols-3 gap-3">
            <KpiCard title="Membros" value={totalMembros} />
            <KpiCard title="Não Membros" value={totalNaoMembros} />
            <KpiCard
              title="Adesão Global"
              value={`${adesaoGlobal.toLocaleString('pt-BR', { minimumFractionDigits: 1, maximumFractionDigits: 1 })}%`}
            />
          </div>
          <MembrosCharts rows={rows} />
        </>
      )}
    </div>
  )
}

function KpiCard({ title, value }: { title: string; value: number | string }) {
  return (
    <div className="border border-border rounded-lg bg-card p-5 relative overflow-hidden">
      <div className="absolute top-0 left-0 right-0 h-[2px] bg-primary" />
      <p className="text-[10px] font-mono tracking-[0.20em] text-muted-foreground uppercase mb-3">{title}</p>
      <p className="font-display tabular-nums text-4xl text-foreground leading-none">
        {typeof value === 'number' ? value.toLocaleString('pt-BR') : value}
      </p>
    </div>
  )
}
```

CRÍTICO:
- Server Component (sem `'use client'`).
- Resolve editionId via getActiveEditionId; sem evento → CTA.
- KPIs agregados calculados no Server (totalMembros, totalNaoMembros, adesaoGlobal).
- Passa rows brutos para `<MembrosCharts>`.
  </action>
  <verify>
    <automated>cd abvcap-congress && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File abvcap-congress/app/dashboard/membros/page.tsx exists
    - File contains exact string `requireAdmin()`
    - File contains exact string `getActiveEditionId()`
    - File contains exact string `getMemberAnalysis(editionId)`
    - File contains exact string `<MembrosCharts rows={rows} />`
    - File contains CTA block when `!editionId` linking to `/dashboard/eventos`
    - File contains KPI cards for `Membros`, `Não Membros`, `Adesão Global`
    - File contains `export const dynamic = 'force-dynamic'`
    - File does NOT contain `'use client'`
    - `cd abvcap-congress && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Server Component cria página de membros, calcula KPIs agregados, delega charts ao Client Component.</done>
</task>

<task type="auto">
  <name>Task 2: Criar Client Component membros-charts.tsx — barras empilhadas + ranking adesão + tabela detalhada</name>
  <files>abvcap-congress/app/dashboard/membros/membros-charts.tsx</files>
  <read_first>abvcap-congress/lib/database.types.ts, abvcap-congress/app/dashboard/overview-charts.tsx, abvcap-congress/components/ui/table.tsx</read_first>
  <action>
Criar `app/dashboard/membros/membros-charts.tsx`:

```typescript
'use client'

import {
  BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer,
  CartesianGrid, LabelList, Cell,
} from 'recharts'
import { useTheme } from 'next-themes'
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'
import type { MemberAnalysisRow } from '@/lib/database.types'

const NAVY_LIGHT = '#112468'
const NAVY_DARK = '#6b9be8'

const SEGMENT_LABELS: Record<string, string> = {
  GP: 'Gestora de PE/VC',
  LP: 'Investidor (LP)',
  FUNDO: 'Fundo de Pensão',
  CORPORATIVO: 'Corporativo',
  GOVERNO: 'Governo',
  ACADEMIA: 'Academia',
  OUTRO: 'Outro',
  SEM_SEGMENTO: 'Sem segmento',
}

const AXIS_STYLE = {
  fontSize: 11,
  fontFamily: 'var(--font-ibm-mono)',
  fill: 'oklch(0.52 0.04 254)',
}

const TOOLTIP_STYLE = {
  backgroundColor: '#ffffff',
  border: '1px solid oklch(0.89 0.010 240)',
  borderRadius: '6px',
  color: '#112468',
  fontSize: '12px',
  fontFamily: 'var(--font-ibm-mono)',
  boxShadow: '0 4px 16px rgba(17, 36, 104, 0.08)',
}

const GRID_COLOR = 'oklch(0.89 0.010 240)'

interface Props {
  rows: MemberAnalysisRow[]
}

export function MembrosCharts({ rows }: Props) {
  const { resolvedTheme } = useTheme()
  const navy = resolvedTheme === 'dark' ? NAVY_DARK : NAVY_LIGHT
  const memberColor = '#00a99d'
  const naoMemberColor = navy

  // Para barras empilhadas (MBR-01/MBR-02): mesmo dado, label legível
  const stackedData = rows.map(r => ({
    segment: r.segment,
    segmentLabel: SEGMENT_LABELS[r.segment] ?? r.segment,
    Membro: r.membro_count,
    'Não Membro': r.nao_membro_count,
    total: r.total,
    pct: r.membership_pct,
  }))

  // Para adesão (MBR-03): ordena por membership_pct DESC, mas só segmentos com pelo menos 3 inscritos (evita ruído)
  const adesaoData = [...rows]
    .filter(r => r.total >= 3)
    .sort((a, b) => b.membership_pct - a.membership_pct)
    .map(r => ({
      segment: r.segment,
      segmentLabel: SEGMENT_LABELS[r.segment] ?? r.segment,
      pct: r.membership_pct,
      total: r.total,
      label: `${r.membership_pct.toLocaleString('pt-BR', { minimumFractionDigits: 1, maximumFractionDigits: 1 })}%`,
    }))

  return (
    <div className="space-y-6">
      {/* MBR-01/MBR-02 — Stacked bars Membros + Não-Membros por segmento */}
      <div className="bg-card border border-border rounded-lg p-5 shadow-sm">
        <ChartLabel>Membros vs Não-Membros por Tipo de Empresa</ChartLabel>
        <ResponsiveContainer width="100%" height={Math.max(280, stackedData.length * 50)}>
          <BarChart
            data={stackedData}
            layout="vertical"
            margin={{ top: 8, right: 60, left: 8, bottom: 0 }}
          >
            <CartesianGrid horizontal={false} stroke={GRID_COLOR} strokeOpacity={0.5} />
            <XAxis type="number" tick={AXIS_STYLE} axisLine={false} tickLine={false} allowDecimals={false} />
            <YAxis
              dataKey="segmentLabel"
              type="category"
              tick={AXIS_STYLE}
              axisLine={false}
              tickLine={false}
              width={150}
            />
            <Tooltip
              contentStyle={TOOLTIP_STYLE}
              cursor={{ fill: 'oklch(0.21 0.11 265 / 0.04)' }}
              formatter={(v: number, name: string) => [`${v} inscritos`, name]}
            />
            <Bar dataKey="Membro" stackId="a" fill={memberColor} radius={[0, 0, 0, 0]} maxBarSize={26} />
            <Bar dataKey="Não Membro" stackId="a" fill={naoMemberColor} radius={[0, 3, 3, 0]} maxBarSize={26}>
              <LabelList
                dataKey="total"
                position="right"
                formatter={(v: number) => `${v} total`}
                style={{ fontSize: 11, fontFamily: 'var(--font-ibm-mono)', fill: 'oklch(0.52 0.04 254)' }}
              />
            </Bar>
          </BarChart>
        </ResponsiveContainer>
        <div className="flex items-center gap-4 mt-3 text-[11px] font-mono text-muted-foreground">
          <span className="flex items-center gap-1.5">
            <span className="inline-block w-3 h-3 rounded-sm" style={{ background: memberColor }} />
            Membros
          </span>
          <span className="flex items-center gap-1.5">
            <span className="inline-block w-3 h-3 rounded-sm" style={{ background: naoMemberColor }} />
            Não-Membros
          </span>
        </div>
      </div>

      {/* MBR-03 — Adesão % por segmento (ordenado desc) */}
      <div className="bg-card border border-border rounded-lg p-5 shadow-sm">
        <ChartLabel>Taxa de Adesão por Tipo de Empresa (segmentos com ≥ 3 inscritos)</ChartLabel>
        {adesaoData.length === 0 ? (
          <EmptyChart />
        ) : (
          <ResponsiveContainer width="100%" height={Math.max(220, adesaoData.length * 42)}>
            <BarChart
              data={adesaoData}
              layout="vertical"
              margin={{ top: 8, right: 70, left: 8, bottom: 0 }}
            >
              <CartesianGrid horizontal={false} stroke={GRID_COLOR} strokeOpacity={0.5} />
              <XAxis
                type="number"
                domain={[0, 100]}
                tick={AXIS_STYLE}
                axisLine={false}
                tickLine={false}
                tickFormatter={(v) => `${v}%`}
              />
              <YAxis
                dataKey="segmentLabel"
                type="category"
                tick={AXIS_STYLE}
                axisLine={false}
                tickLine={false}
                width={150}
              />
              <Tooltip
                contentStyle={TOOLTIP_STYLE}
                cursor={{ fill: 'oklch(0.21 0.11 265 / 0.04)' }}
                formatter={(_v: number, _name: string, ctx: { payload?: { pct?: number; total?: number } }) => [
                  `${ctx?.payload?.pct ?? 0}% (${ctx?.payload?.total ?? 0} inscritos)`,
                  'Adesão',
                ]}
              />
              <Bar dataKey="pct" name="Adesão" radius={[0, 3, 3, 0]} maxBarSize={22}>
                {adesaoData.map((_, i) => (
                  <Cell key={i} fill={memberColor} />
                ))}
                <LabelList
                  dataKey="label"
                  position="right"
                  style={{ fontSize: 11, fontFamily: 'var(--font-ibm-mono)', fill: 'oklch(0.52 0.04 254)' }}
                />
              </Bar>
            </BarChart>
          </ResponsiveContainer>
        )}
      </div>

      {/* Tabela detalhada (MBR-01 alternativa numérica) */}
      <div className="bg-card border border-border rounded-lg overflow-hidden">
        <div className="px-5 py-3 border-b border-border">
          <p className="text-[10px] font-mono tracking-[0.20em] text-muted-foreground uppercase">
            Detalhamento por Segmento
          </p>
        </div>
        <Table>
          <TableHeader>
            <TableRow className="bg-muted/40 hover:bg-muted/40">
              <TableHead>Segmento</TableHead>
              <TableHead className="text-right">Membros</TableHead>
              <TableHead className="text-right">Não-Membros</TableHead>
              <TableHead className="text-right">Total</TableHead>
              <TableHead className="text-right">Adesão</TableHead>
            </TableRow>
          </TableHeader>
          <TableBody>
            {rows.map(r => (
              <TableRow key={r.segment}>
                <TableCell className="font-medium">{SEGMENT_LABELS[r.segment] ?? r.segment}</TableCell>
                <TableCell className="text-right font-mono tabular-nums text-sm">{r.membro_count}</TableCell>
                <TableCell className="text-right font-mono tabular-nums text-sm">{r.nao_membro_count}</TableCell>
                <TableCell className="text-right font-mono tabular-nums text-sm">{r.total}</TableCell>
                <TableCell className="text-right font-mono tabular-nums text-sm text-primary">
                  {r.membership_pct.toLocaleString('pt-BR', { minimumFractionDigits: 1, maximumFractionDigits: 1 })}%
                </TableCell>
              </TableRow>
            ))}
          </TableBody>
        </Table>
      </div>
    </div>
  )
}

function ChartLabel({ children }: { children: React.ReactNode }) {
  return (
    <p className="text-[10px] font-mono tracking-[0.20em] text-muted-foreground uppercase mb-4">
      {children}
    </p>
  )
}

function EmptyChart({ height = 180 }: { height?: number }) {
  return (
    <div className="flex flex-col items-center justify-center gap-2" style={{ height }}>
      <div className="w-6 h-6 rounded-full border-2 border-dashed border-border" />
      <p className="text-[11px] font-mono text-muted-foreground/40">sem dados ainda</p>
    </div>
  )
}
```

CRÍTICO:
- Stacked bars (MBR-01/MBR-02): `stackId="a"` em ambos os `<Bar>`, layout horizontal (`layout="vertical"`), com LabelList mostrando total à direita.
- Ranking de adesão (MBR-03): filtro `total >= 3` evita ruído de segmentos com 1-2 inscritos; ordenado desc por `membership_pct`; LabelList mostra `${pct}%`.
- Tabela detalhada complementa MBR-01/MBR-02 com valores numéricos exatos.
- `SEGMENT_LABELS` mapeia enum interno → texto PT-BR legível.
  </action>
  <verify>
    <automated>cd abvcap-congress && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File abvcap-congress/app/dashboard/membros/membros-charts.tsx contains `'use client'` as first non-comment line
    - File contains exact string `stackId="a"` (stacked bars)
    - File contains exact string `Membros vs Não-Membros por Tipo de Empresa`
    - File contains exact string `Taxa de Adesão por Tipo de Empresa`
    - File contains exact string `r.total >= 3` (filtro de ruído)
    - File contains exact string `b.membership_pct - a.membership_pct` (ordenação desc)
    - File contains exact string `SEGMENT_LABELS`
    - File contains all 8 keys in SEGMENT_LABELS: GP, LP, FUNDO, CORPORATIVO, GOVERNO, ACADEMIA, OUTRO, SEM_SEGMENTO
    - File contains exact string `Detalhamento por Segmento` (tabela)
    - File contains exact string `layout="vertical"`
    - File imports `MemberAnalysisRow` from `@/lib/database.types`
    - `cd abvcap-congress && npm run build` completes successfully
  </acceptance_criteria>
  <done>Charts component com stacked bars (MBR-01/02), ranking de adesão (MBR-03) e tabela detalhada; build limpo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → RPC get_member_analysis | Service role; editionId vem de cookie httpOnly validado |
| Browser → /dashboard/membros | Sessão Supabase Auth; requireAdmin() no Server Component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Information Disclosure | /dashboard/membros sem auth | mitigate | requireAdmin() no início do Server Component |
| T-03-18 | Information Disclosure | RPC retorna dados de outra edição | mitigate | editionId vem de cookie httpOnly validado em /api/edition/select; RPC filtra por p_edition_id |
| T-03-19 | DoS | RPC roda em tabela grande | accept | get_member_analysis tem GROUP BY com WHERE indexado (idx_participants_edition_membership da migration 010) |
</threat_model>

<verification>
- `cd abvcap-congress && npm run build` completes successfully
- Manual: clicar "Análise de Membros" no sidebar abre `/dashboard/membros` sem erro
- Manual: KPIs no topo mostram totais coerentes (Membros + Não Membros = Total Inscritos)
- Manual: Stacked bars mostram cada segmento com membros (teal) empilhado com não-membros (navy)
- Manual: Ranking de adesão exclui segmentos com <3 inscritos e ordena desc
- Manual: Tabela detalhada mostra valores numéricos coerentes com os gráficos
- Manual: Trocar edição no sidebar atualiza todos os dados desta página via router.refresh()
</verification>

<success_criteria>
- MBR-01 satisfeito: breakdown visual de membros vs não-membros por tipo de empresa
- MBR-02 satisfeito: detalhamento por segmento dentro de cada grupo (via stacked bars + tabela)
- MBR-03 satisfeito: ranking de adesão % por segmento (ordenado desc, filtro de ruído)
- Build passa sem TypeScript errors
- Página acessível pelo sidebar
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-core/03-04-SUMMARY.md` documentando:
- 3 visões implementadas (stacked, ranking, tabela)
- Filtro de ruído (total >= 3) aplicado ao ranking
- Decisão sobre representar MBR-02 (breakdown por segmento dentro de cada grupo) via stacked bars + tabela
</output>
